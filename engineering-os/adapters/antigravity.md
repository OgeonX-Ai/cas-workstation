# Antigravity Adapter

Status: manual, capability-gated.

Antigravity IDE is installed, but no CLI or global instruction file was verified.
Do not install a speculative global adapter. For repository sessions, place or
reference the canonical contract from the repository's existing `AGENTS.md` or
`.agents` rules. Use separate top-level sessions for cheap and strong models.
Child-agent model override, nested delegation, and background execution remain
disabled until a live UI/runtime probe records evidence in the compatibility
matrix.
