---
phase: 01-stable-workstation-and-recovery
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GsdOrchestrator/Workflows/Models/WorkflowModels.cs
  - src/GsdOrchestrator/Workflows/GsdStateMachine.cs
  - src/GsdOrchestrator/Checkpointing/FileCheckpointStore.cs
  - src/GsdOrchestrator.Tests/GsdStateMachineTests.cs
  - src/GsdOrchestrator.Tests/GsdStateMachineAdditionalTests.cs
  - src/GsdOrchestrator.Tests/States/CheckpointStoreSchemaVersionTests.cs
autonomous: false
requirements:
  - STAB-04
must_haves:
  truths:
    - "D-07: Recoverable failed executable state and attempt metadata remain separate from the operator terminal outcome."
    - "D-08: ResumeAsync re-enters the failed state once with prior history and a bounded retry count."
    - "D-09: Checkpoint schema evolution is explicit and unsupported versions fail clearly."
    - "D-10: A failing regression test proves the current terminal-Failed resume no-op before implementation."
    - "D-11: Work occurs only in C:\\PersonalRepo\\worktrees\\gsd-loop-stability, never the dirty source checkout."
    - "D-12: Full dotnet build and tests are mandatory evidence."
  artifacts:
    - path: src/GsdOrchestrator/Workflows/Models/WorkflowModels.cs
      provides: Recoverable failure state and attempt metadata
      contains: "RetryCount"
    - path: src/GsdOrchestrator/Workflows/GsdStateMachine.cs
      provides: Failed-state resume behavior
      contains: "ResumeAsync"
    - path: src/GsdOrchestrator.Tests/GsdStateMachineTests.cs
      provides: Terminal-failure recovery regression coverage
      contains: "ResumeAsync"
  key_links:
    - from: src/GsdOrchestrator/Workflows/GsdStateMachine.cs
      to: src/GsdOrchestrator/Workflows/Models/WorkflowModels.cs
      via: recoverable failed-state metadata
      pattern: "Resume|Retry|Failed"
---

<objective>
Make interrupted failures resumable without replaying completed states or treating terminal `Failed` as a no-op.

Purpose: establish trustworthy recovery before watch mode and later durable scheduling depend on it.
Output: explicit failure metadata, bounded resume semantics, schema-safe checkpoints, and regression tests.
</objective>

<threat_model>
  <threat id="T-01-02" severity="high">Tampered, stale, or ambiguous checkpoints could replay side effects or bypass recovery limits.</threat>
  <mitigation>Validate schema/version, persist the failed executable state and retry count, reject unsupported data, and re-enter only through a bounded explicit resume path.</mitigation>
  <verification>Unit tests cover terminal failure, retry exhaustion, completed-state non-replay, and unsupported schema.</verification>
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Create clean child worktree and add RED recovery tests</name>
  <files>src/GsdOrchestrator.Tests/GsdStateMachineTests.cs, src/GsdOrchestrator.Tests/GsdStateMachineAdditionalTests.cs, src/GsdOrchestrator.Tests/States/CheckpointStoreSchemaVersionTests.cs</files>
  <read_first>
    - C:/PersonalRepo/portfolio/gsd-orchestrator/CLAUDE.md
    - C:/PersonalRepo/portfolio/gsd-orchestrator/src/GsdOrchestrator/Workflows/GsdStateMachine.cs
    - C:/PersonalRepo/portfolio/gsd-orchestrator/src/GsdOrchestrator/Workflows/Models/WorkflowModels.cs
    - C:/PersonalRepo/portfolio/gsd-orchestrator/src/GsdOrchestrator/Checkpointing/FileCheckpointStore.cs
    - C:/PersonalRepo/portfolio/gsd-orchestrator/src/GsdOrchestrator.Tests/GsdStateMachineTests.cs
  </read_first>
  <action>Create or reuse branch `codex/loop-stability` in worktree `C:\PersonalRepo\worktrees\gsd-loop-stability` from the nested repo's current committed `main`; abort if that target is dirty or points elsewhere. Add tests that inject a handler failure after at least one completed transition, assert the persisted operator outcome is Failed, invoke ResumeAsync, and expect the failed executable state to run exactly once while completed states do not replay. Add retry-limit and unsupported-checkpoint-version tests. Run focused tests before production changes and preserve RED output.</action>
  <acceptance_criteria>
    - Dirty `C:\PersonalRepo\portfolio\gsd-orchestrator` files are unchanged.
    - At least one new test fails because ResumeAsync currently skips terminal Failed.
    - Tests explicitly assert prior history retention, one re-entry, bounded RetryCount, and unsupported version rejection.
  </acceptance_criteria>
  <verify><automated>dotnet test src/GsdOrchestrator.Tests/GsdOrchestrator.Tests.csproj --filter "FullyQualifiedName~Resume|FullyQualifiedName~CheckpointStoreSchemaVersion"</automated></verify>
  <done>RED evidence reproduces STAB-04 in the clean child worktree.</done>
</task>

<task type="execute">
  <name>Task 2: Implement explicit recoverable failure metadata and resume</name>
  <files>src/GsdOrchestrator/Workflows/Models/WorkflowModels.cs, src/GsdOrchestrator/Workflows/GsdStateMachine.cs, src/GsdOrchestrator/Checkpointing/FileCheckpointStore.cs</files>
  <read_first>
    - src/GsdOrchestrator.Tests/GsdStateMachineTests.cs
    - src/GsdOrchestrator.Tests/GsdStateMachineAdditionalTests.cs
    - src/GsdOrchestrator.Tests/States/CheckpointStoreSchemaVersionTests.cs
    - src/GsdOrchestrator/Workflows/Models/WorkflowModels.cs
    - src/GsdOrchestrator/Workflows/GsdStateMachine.cs
    - src/GsdOrchestrator/Checkpointing/FileCheckpointStore.cs
  </read_first>
  <action>Add explicit context fields for the failed executable state, attempt/retry count, and sanitized failure reason while preserving transition history. On handler failure, capture the current executable state before reporting terminal Failed. In ResumeAsync, validate that recoverable metadata exists, reject retry exhaustion, restore the executable state, increment the bounded counter, clear only transient terminal fields, save atomically, and execute from that state. Do not replay prior completed handlers. Update checkpoint schema/version handling only as required and provide backward-compatible defaults for valid older nonfailed checkpoints.</action>
  <acceptance_criteria>
    - Focused recovery/schema tests exit 0.
    - Resume of Done remains terminal and does not execute a handler.
    - Resume of nonrecoverable/unsupported Failed returns a clear error rather than silently succeeding.
    - A recoverable Failed context re-enters exactly the recorded state and retains earlier history.
  </acceptance_criteria>
  <verify><automated>dotnet test src/GsdOrchestrator.Tests/GsdOrchestrator.Tests.cs --filter "FullyQualifiedName~Resume|FullyQualifiedName~CheckpointStoreSchemaVersion"</automated></verify>
  <done>STAB-04 recovery semantics are GREEN.</done>
</task>

<task type="execute">
  <name>Task 3: Run full control-plane regression gate</name>
  <files>src/GsdOrchestrator/Workflows/Models/WorkflowModels.cs, src/GsdOrchestrator/Workflows/GsdStateMachine.cs, src/GsdOrchestrator/Checkpointing/FileCheckpointStore.cs</files>
  <read_first>
    - src/GsdOrchestrator/GsdOrchestrator.csproj
    - src/GsdOrchestrator.Tests/GsdOrchestrator.Tests.csproj
    - .planning/phases/01-stable-workstation-and-recovery/01-VALIDATION.md
  </read_first>
  <action>Run restore if required, then Release build and the full xUnit suite. Treat missing packages as inconclusive and permit one explicit restore retry; do not retry deterministic test failures unchanged. Record command, cwd, exit code, duration, and sanitized summary.</action>
  <acceptance_criteria>
    - `dotnet build src/GsdOrchestrator.sln --configuration Release` exits 0.
    - Full `dotnet test` exits 0 with no skipped mandatory recovery test.
    - Evidence contains no tokens or raw external payloads.
  </acceptance_criteria>
  <verify><automated>dotnet build src/GsdOrchestrator.sln --configuration Release &amp;&amp; dotnet test src/GsdOrchestrator.Tests/GsdOrchestrator.Tests.csproj --configuration Release</automated></verify>
  <done>Recovery changes pass the complete control-plane gate.</done>
</task>

</tasks>

<verification>Focused RED/GREEN recovery tests and the full Release build/test suite must pass in the child worktree.</verification>

<success_criteria>
- A terminal failure can be resumed at the failed executable state exactly once per attempt.
- Retry exhaustion and unsupported checkpoints fail clearly.
- Completed history is retained and not replayed.
</success_criteria>

<output>After completion, create `01-02-SUMMARY.md` with child worktree/branch, RED/GREEN evidence, schema decision, and full test result.</output>
