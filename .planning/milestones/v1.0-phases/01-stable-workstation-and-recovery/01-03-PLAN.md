---
phase: 01-stable-workstation-and-recovery
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - src/GsdOrchestrator/Program.cs
  - src/GsdOrchestrator/Workflows/Services/WatchCoordinator.cs
  - src/GsdOrchestrator/Workflows/Services/FileWatchStateStore.cs
  - src/GsdOrchestrator/Workflows/Services/IWatchStateStore.cs
  - src/GsdOrchestrator.Tests/WatchCoordinatorTests.cs
  - src/GsdOrchestrator.Tests/FileWatchStateStoreTests.cs
autonomous: false
requirements:
  - STAB-03
must_haves:
  truths:
    - "D-04: Each interval makes one finite sequential pass over all configured repositories."
    - "D-05: A repository failure is recorded and cannot starve later repositories in that interval."
    - "D-06: Success-only deduplication is persisted atomically behind an interface; failures remain eligible."
    - "D-10: Two-repository and first-repo-failure tests are RED before implementation."
    - "D-11: This plan continues only in the clean gsd-loop-stability child worktree after 01-02."
    - "D-12: Focused and full dotnet tests are completion evidence."
  artifacts:
    - path: src/GsdOrchestrator/Workflows/Services/WatchCoordinator.cs
      provides: Finite per-interval multi-repository polling
    - path: src/GsdOrchestrator/Workflows/Services/FileWatchStateStore.cs
      provides: Atomic restart-safe success-only deduplication
    - path: src/GsdOrchestrator.Tests/WatchCoordinatorTests.cs
      provides: Multi-repository starvation/failure isolation regression tests
  key_links:
    - from: src/GsdOrchestrator/Program.cs
      to: src/GsdOrchestrator/Workflows/Services/WatchCoordinator.cs
      via: finite polling pass inside outer delay loop
      pattern: "Poll.*Once|WatchCoordinator"
---

<objective>
Replace first-repository starvation and volatile deduplication with a finite, failure-isolated, restart-safe watch pass.

Purpose: prove every configured repository is processed before Phase 3 introduces generalized concurrency.
Output: coordinator/store boundaries, Program wiring, and deterministic xUnit coverage.
</objective>

<threat_model>
  <threat id="T-01-03" severity="high">Restart or partial failure could duplicate commits/comments/PRs or permanently suppress failed work.</threat>
  <mitigation>Persist only successful idempotency keys atomically, isolate per-repo faults, honor cancellation, and make each interval finite.</mitigation>
  <verification>Tests cover restart, first-repo failure, cancelled work, successful dedupe, and path sanitization.</verification>
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add RED coordinator and watch-state tests</name>
  <files>src/GsdOrchestrator.Tests/WatchCoordinatorTests.cs, src/GsdOrchestrator.Tests/FileWatchStateStoreTests.cs</files>
  <read_first>
    - src/GsdOrchestrator/Program.cs
    - src/GsdOrchestrator/Checkpointing/FileCheckpointStore.cs
    - src/GsdOrchestrator/Workflows/Models/RepoConfigLoader.cs
    - src/GsdOrchestrator.Tests/MultiRepoConfigTests.cs
    - src/GsdOrchestrator.Tests/States/CheckpointStoreTests.cs
  </read_first>
  <action>In the existing clean child worktree, add tests for two configured repositories visited in order during one finite pass; first-repo exception still permits second-repo polling; cancellation stops before the next pass; successful issue keys survive store recreation; failed/cancelled keys are absent; duplicate successful keys are skipped; and state writes are atomic and namespaced. Run focused tests before production types exist and preserve RED evidence.</action>
  <acceptance_criteria>
    - Tests contain two distinct repo identities and assert both calls within one interval.
    - A first-repo exception test asserts the second repo still runs.
    - Persistence tests distinguish success from failed/cancelled outcomes.
    - Focused command exits non-zero before implementation.
  </acceptance_criteria>
  <verify><automated>dotnet test src/GsdOrchestrator.Tests/GsdOrchestrator.Tests.csproj --filter "FullyQualifiedName~WatchCoordinator|FullyQualifiedName~FileWatchStateStore"</automated></verify>
  <done>RED tests reproduce STAB-03 starvation and volatility.</done>
</task>

<task type="execute">
  <name>Task 2: Implement finite sequential polling and atomic success state</name>
  <files>src/GsdOrchestrator/Workflows/Services/WatchCoordinator.cs, src/GsdOrchestrator/Workflows/Services/IWatchStateStore.cs, src/GsdOrchestrator/Workflows/Services/FileWatchStateStore.cs</files>
  <read_first>
    - src/GsdOrchestrator.Tests/WatchCoordinatorTests.cs
    - src/GsdOrchestrator.Tests/FileWatchStateStoreTests.cs
    - src/GsdOrchestrator/Program.cs
    - src/GsdOrchestrator/Checkpointing/FileCheckpointStore.cs
    - src/GsdOrchestrator/Workflows/Models/WorkflowModels.cs
  </read_first>
  <action>Create a finite coordinator method that accepts configured repos, a per-repo poll delegate/service, state store, logger, and cancellation token; iterates repos sequentially; catches/logs each repo failure without terminating the interval; and returns a structured interval result. Define `IWatchStateStore` operations to test and record successful idempotency keys. Implement a file store under the configured state directory using sanitized repo/key namespaces and temp-file atomic replacement patterned after FileCheckpointStore. Record a key only after successful workflow completion; never record failure, cancellation, or retryable error.</action>
  <acceptance_criteria>
    - Focused watch/store tests exit 0.
    - Coordinator contains no unbounded delay/while loop.
    - File store uses temp write plus atomic replace/move and sanitizes key-derived paths.
    - No method marks failed or cancelled work as successfully processed.
  </acceptance_criteria>
  <verify><automated>dotnet test src/GsdOrchestrator.Tests/GsdOrchestrator.Tests.csproj --filter "FullyQualifiedName~WatchCoordinator|FullyQualifiedName~FileWatchStateStore"</automated></verify>
  <done>Finite polling and restart-safe deduplication are GREEN.</done>
</task>

<task type="execute">
  <name>Task 3: Wire Program outer loop and run full regression</name>
  <files>src/GsdOrchestrator/Program.cs</files>
  <read_first>
    - src/GsdOrchestrator/Program.cs
    - src/GsdOrchestrator/Workflows/Services/WatchCoordinator.cs
    - src/GsdOrchestrator/Workflows/Services/IWatchStateStore.cs
    - src/GsdOrchestrator/Workflows/Services/FileWatchStateStore.cs
    - src/GsdOrchestrator.Tests/WatchCoordinatorTests.cs
  </read_first>
  <action>Register the coordinator and file watch-state store through DI. Replace the per-repo awaited infinite watcher with one outer cancellation-aware loop: call one finite all-repo pass, then await the configured interval delay. Preserve CLI flags, per-repo rate limits, structured logging, and cancellation. Run focused tests, Release build, and full tests.</action>
  <acceptance_criteria>
    - Program has exactly one outer watch loop and invokes a finite all-repo pass.
    - Two-repository/failure-isolation tests exit 0.
    - Release build and full tests exit 0.
  </acceptance_criteria>
  <verify><automated>dotnet build src/GsdOrchestrator.sln --configuration Release &amp;&amp; dotnet test src/GsdOrchestrator.Tests/GsdOrchestrator.Tests.csproj --configuration Release</automated></verify>
  <done>STAB-03 works end-to-end without repo starvation or volatile success state.</done>
</task>

</tasks>

<verification>Focused watch/store tests and the full Release build/test suite pass in the same child worktree created by 01-02.</verification>

<success_criteria>
- Every configured repo is visited in one finite sequential interval.
- One repo failure cannot prevent later repos from running.
- Successful dedupe survives restart; failed/cancelled work remains eligible.
</success_criteria>

<output>After completion, create `01-03-SUMMARY.md` with RED/GREEN evidence and full test result.</output>
