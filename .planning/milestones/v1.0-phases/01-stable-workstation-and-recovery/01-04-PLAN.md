---
phase: 01-stable-workstation-and-recovery
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - .planning/phases/01-stable-workstation-and-recovery/01-EVIDENCE.md
autonomous: true
requirements:
  - STAB-01
  - STAB-02
  - STAB-03
  - STAB-04
must_haves:
  truths:
    - "D-01 through D-03 are proven by root contract and doctor evidence."
    - "D-04 through D-06 are proven by two-repository, failure-isolation, and restart-deduplication tests."
    - "D-07 through D-09 are proven by failed-state recovery and checkpoint-schema tests."
    - "D-10: Evidence records the RED test name and GREEN result for every known defect."
    - "D-11: Root and nested implementation evidence comes from separate clean worktrees."
    - "D-12: Real PowerShell and dotnet commands are all green before phase readiness."
  artifacts:
    - path: .planning/phases/01-stable-workstation-and-recovery/01-EVIDENCE.md
      provides: Sanitized cross-workstream validation record
      contains: "STAB-04"
  key_links:
    - from: .planning/phases/01-stable-workstation-and-recovery/01-EVIDENCE.md
      to: .planning/REQUIREMENTS.md
      via: STAB requirement evidence mapping
      pattern: "STAB-0[1-4]"
---

<objective>
Prove all Phase 1 outcomes across the isolated root and gsd worktrees and record one sanitized evidence bundle.

Purpose: prevent planning/implementation artifacts from being mistaken for working recovery.
Output: 01-EVIDENCE.md with commands, results, requirement mapping, and residual risks.
</objective>

<threat_model>
  <threat id="T-01-04" severity="high">A meta-verification step could report success from stale output, skip a repository gate, or trigger external side effects.</threat>
  <mitigation>Run commands in the recorded owning worktrees, capture current HEAD/cwd/time/exit code, fail closed on missing gates, and prohibit push/deploy/merge/global config mutation.</mitigation>
  <verification>Evidence names every mandatory command and maps each STAB requirement to current outputs.</verification>
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Run root workstation gates</name>
  <files>.planning/phases/01-stable-workstation-and-recovery/01-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/01-stable-workstation-and-recovery/01-01-SUMMARY.md
    - .planning/phases/01-stable-workstation-and-recovery/01-VALIDATION.md
    - tests/Workstation.Contract.Tests.ps1
    - stack.manifest.json
    - schemas/doctor.schema.json
  </read_first>
  <action>From `C:\PersonalRepo\worktrees\loop-engineering`, run the contract test with a 60-second timeout, parse the manifest, and run the read-only doctor smoke. Create 01-EVIDENCE.md and record UTC timestamp, worktree path, branch, HEAD, exact command, duration, exit code, sanitized summary, and STAB-01/STAB-02 mapping. Verify that global MCP/client configuration timestamps are unchanged. Do not run setup/start/apply.</action>
  <acceptance_criteria>
    - Root contract test exits 0.
    - Manifest parses and doctor output conforms to its schema or records an inconclusive missing-tool condition.
    - Evidence contains STAB-01 and STAB-02 with current command results.
  </acceptance_criteria>
  <verify><automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\Workstation.Contract.Tests.ps1</automated></verify>
  <done>Root evidence is current, sanitized, and green.</done>
</task>

<task type="execute">
  <name>Task 2: Run control-plane focused and full gates</name>
  <files>.planning/phases/01-stable-workstation-and-recovery/01-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/01-stable-workstation-and-recovery/01-02-SUMMARY.md
    - .planning/phases/01-stable-workstation-and-recovery/01-03-SUMMARY.md
    - C:/PersonalRepo/worktrees/gsd-loop-stability/src/GsdOrchestrator.Tests/GsdStateMachineTests.cs
    - C:/PersonalRepo/worktrees/gsd-loop-stability/src/GsdOrchestrator.Tests/WatchCoordinatorTests.cs
  </read_first>
  <action>From `C:\PersonalRepo\worktrees\gsd-loop-stability`, record branch/HEAD and run focused Resume/Checkpoint tests, focused WatchCoordinator/FileWatchStateStore tests, Release build, and full xUnit suite under the validation timeouts. Append exact commands, durations, exit codes, sanitized summaries, and STAB-03/STAB-04 mapping to 01-EVIDENCE.md. Missing worktree, summaries, tests, or tools is inconclusive and blocks completion.</action>
  <acceptance_criteria>
    - Both focused commands exit 0.
    - Release build and full test suite exit 0.
    - Evidence demonstrates two-repo failure isolation, restart dedupe, and failed-step resume.
  </acceptance_criteria>
  <verify><automated>dotnet build C:\PersonalRepo\worktrees\gsd-loop-stability\src\GsdOrchestrator.sln --configuration Release &amp;&amp; dotnet test C:\PersonalRepo\worktrees\gsd-loop-stability\src\GsdOrchestrator.Tests\GsdOrchestrator.Tests.csproj --configuration Release</automated></verify>
  <done>Control-plane evidence is current, sanitized, and green.</done>
</task>

<task type="execute">
  <name>Task 3: Apply the Phase 1 evidence gate</name>
  <files>.planning/phases/01-stable-workstation-and-recovery/01-EVIDENCE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/01-stable-workstation-and-recovery/01-CONTEXT.md
    - .planning/phases/01-stable-workstation-and-recovery/01-EVIDENCE.md
  </read_first>
  <action>Add a final matrix for STAB-01 through STAB-04 and roadmap success criteria 1 through 4. Each row must cite a current command/result and RED/GREEN regression name. Mark the phase evidence `passed` only when every row is passed; otherwise mark `failed` or `inconclusive` with the exact missing evidence. Do not push, deploy, merge, post messages, or mutate global MCP configuration.</action>
  <acceptance_criteria>
    - All four STAB IDs and all four roadmap criteria appear exactly once in the final matrix.
    - No failed/inconclusive row coexists with overall `passed`.
    - Evidence contains no secrets, tokens, raw prompts, or full external payloads.
  </acceptance_criteria>
  <verify><automated>powershell.exe -NoProfile -Command "$p='.planning/phases/01-stable-workstation-and-recovery/01-EVIDENCE.md'; $c=Get-Content $p -Raw; @('STAB-01','STAB-02','STAB-03','STAB-04') | ForEach-Object { if($c -notmatch [regex]::Escape($_)){ throw \"Missing $_\" } }"</automated></verify>
  <done>Phase 1 has one fail-closed evidence decision covering every requirement and success criterion.</done>
</task>

</tasks>

<verification>Every mandatory root and nested command is rerun from the recorded worktree/HEAD; summaries alone are insufficient.</verification>

<success_criteria>
- All STAB requirements and roadmap success criteria have current machine-readable evidence.
- Any missing/failed gate blocks overall pass.
- No external action or global configuration mutation occurs.
</success_criteria>

<output>After completion, create `01-04-SUMMARY.md` referencing 01-EVIDENCE.md and the final pass/fail decision.</output>
