---
phase: 01-stable-workstation-and-recovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Workstation.Contract.Tests.ps1
  - stack.manifest.json
  - scripts/Cas.Workstation.psm1
  - doctor.ps1
  - schemas/doctor.schema.json
  - README.md
  - docs/support-matrix.md
autonomous: true
requirements:
  - STAB-01
  - STAB-02
must_haves:
  truths:
    - "D-01: The manifest resolves rootPath C:\\PersonalRepo, reposRoot portfolio, and profile C:\\Users\\KimHarjamaki."
    - "D-02: MCP generation consumes sharedMcpServer.args and targets Promptimprover\\universal-refiner\\dist\\src\\index.js."
    - "D-03: The full profile and doctor include cas-contracts, cas-evals, cas-reference-product, and cas-platform."
    - "D-10: Contract assertions fail before the path/inventory fix and pass afterward."
    - "D-11: This plan modifies only the isolated root cas-workstation worktree."
    - "D-12: Completion uses executable contract/doctor checks, not source-string inspection alone."
  artifacts:
    - path: tests/Workstation.Contract.Tests.ps1
      provides: Dependency-free workstation contract regression gate
      contains: "C:\\PersonalRepo"
    - path: stack.manifest.json
      provides: Correct workstation and repository contract
      contains: "cas-reference-product"
    - path: scripts/Cas.Workstation.psm1
      provides: Manifest-driven MCP and doctor behavior
      contains: "sharedMcpServer"
  key_links:
    - from: stack.manifest.json
      to: scripts/Cas.Workstation.psm1
      via: sharedMcpServer.args and paths.reposRoot
      pattern: "sharedMcpServer|reposRoot"
---

<objective>
Make the root workstation contract truthful and regression-protected without touching global client configuration.

Purpose: establish exact Windows paths, active Promptimprover runtime, and complete component health before loop concurrency is introduced.
Output: a deterministic PowerShell contract gate plus corrected manifest, module, doctor schema/docs.
</objective>

<threat_model>
  <threat id="T-01-01" severity="high">Manifest-controlled paths could escape the workspace or point clients at a stale/untrusted executable.</threat>
  <mitigation>Canonicalize repo/runtime paths under C:\PersonalRepo, use sharedMcpServer.args as the single source, reject missing files in doctor, and never apply global configuration in tests.</mitigation>
  <verification>Contract tests use temporary roots and prove exact values; doctor smoke remains read-only.</verification>
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add RED workstation contract tests</name>
  <files>tests/Workstation.Contract.Tests.ps1</files>
  <read_first>
    - stack.manifest.json
    - scripts/Cas.Workstation.psm1
    - doctor.ps1
    - schemas/doctor.schema.json
    - .planning/phases/01-stable-workstation-and-recovery/01-CONTEXT.md
  </read_first>
  <action>Create a dependency-free PowerShell test runner that imports the module, reads the manifest, and exits non-zero unless: defaults.rootPath is exactly `C:\PersonalRepo`; defaults.configPath is exactly `C:\Users\KimHarjamaki\.cas`; paths.reposRoot is `portfolio`; full profile includes `Promptimprover`, `autogen`, `gsd-orchestrator`, `cas-contracts`, `cas-evals`, `cas-reference-product`, and `cas-platform`; sharedMcpServer.args contains exactly one absolute script target ending `Promptimprover\universal-refiner\dist\src\index.js`; and doctor output reports a missing runtime target as unhealthy. Use temporary directories and mocks/fixtures only; do not run setup or write global MCP files. Run it before product changes and preserve the failing output as RED evidence.</action>
  <acceptance_criteria>
    - `tests/Workstation.Contract.Tests.ps1` exists and contains assertions for `C:\PersonalRepo`, `C:\Users\KimHarjamaki`, `portfolio`, and all seven required repositories.
    - The pre-fix command exits non-zero for at least the stale root, stale MCP entry, and missing repository inventory.
    - The test creates no files outside its temporary test directory.
  </acceptance_criteria>
  <verify><automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\Workstation.Contract.Tests.ps1</automated></verify>
  <done>RED evidence proves the committed baseline violates STAB-01/STAB-02.</done>
</task>

<task type="execute">
  <name>Task 2: Make manifest, MCP generation, and doctor GREEN</name>
  <files>stack.manifest.json, scripts/Cas.Workstation.psm1, doctor.ps1, schemas/doctor.schema.json</files>
  <read_first>
    - tests/Workstation.Contract.Tests.ps1
    - stack.manifest.json
    - scripts/Cas.Workstation.psm1
    - doctor.ps1
    - schemas/doctor.schema.json
  </read_first>
  <action>Set manifest defaults to `C:\PersonalRepo`, `C:\Users\KimHarjamaki\.cas`, and `portfolio`. Add the four missing CAS repositories to the repository catalog and full profile with their existing Coding-Autopilot-System GitHub URLs and `main` branches. Set sharedMcpServer.args to `C:\PersonalRepo\portfolio\Promptimprover\universal-refiner\dist\src\index.js`. Refactor MCP generation to copy command/args/transport from `Manifest.sharedMcpServer` rather than constructing `Promptimprover\dist\index.js`. Extend doctor output and schema only as necessary to validate every required repo and the exact MCP script target. Preserve doctor.ps1 profile normalization and guard clauses.</action>
  <acceptance_criteria>
    - Contract tests exit 0.
    - `stack.manifest.json` parses and contains the exact approved paths and required repo IDs.
    - `Cas.Workstation.psm1` no longer contains a constructed `Promptimprover") "dist\index.js"` target.
    - Doctor reports a missing Promptimprover script as unhealthy without modifying global client files.
  </acceptance_criteria>
  <verify><automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\Workstation.Contract.Tests.ps1</automated></verify>
  <done>STAB-01/STAB-02 behavior is GREEN and manifest-driven.</done>
</task>

<task type="execute">
  <name>Task 3: Align operator documentation and run read-only smoke</name>
  <files>README.md, docs/support-matrix.md</files>
  <read_first>
    - README.md
    - docs/support-matrix.md
    - stack.manifest.json
    - tests/Workstation.Contract.Tests.ps1
  </read_first>
  <action>Update only stale operator examples to `cas.ps1 setup|doctor|start`, `C:\PersonalRepo`, `portfolio`, and the complete loop component list. Run the contract test and a doctor invocation that performs checks only; do not run setup/apply, start services, or change user configuration.</action>
  <acceptance_criteria>
    - README and support matrix contain `C:\PersonalRepo` and do not instruct `C:\CodingAutopilotSystem`.
    - Contract test exits 0 after documentation changes.
    - Doctor smoke produces a machine-readable or structured report and does not apply configuration.
  </acceptance_criteria>
  <verify><automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\Workstation.Contract.Tests.ps1</automated></verify>
  <done>Operator docs and executable root contract agree.</done>
</task>

</tasks>

<verification>
1. Run `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\Workstation.Contract.Tests.ps1` with a 60-second timeout.
2. Parse `stack.manifest.json` using PowerShell `ConvertFrom-Json`.
3. Confirm no global MCP file timestamp changed during tests.
</verification>

<success_criteria>
- STAB-01 and STAB-02 are proven by executable tests.
- Paths and repository inventory have one manifest source of truth.
- The active Promptimprover Universal Refiner entry point is verified by doctor.
</success_criteria>

<output>After completion, create `01-01-SUMMARY.md` with RED/GREEN commands, changed files, and sanitized doctor evidence.</output>

