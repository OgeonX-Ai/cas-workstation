---
phase: 02-typed-goal-and-lifecycle-contract
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
autonomous: false
requirements: [GOAL-01, GOAL-02, GOAL-03]
files_modified:
  - scripts/build-registry.mjs
  - scripts/validate-registry.mjs
  - tests/registry.test.mjs
  - docs/VERSIONING.md
  - README.md
---

<objective>Make the v1 goal contract discoverable, documented, and regression-protected across the contract registry.</objective>

<threat_model>A valid local schema that is omitted from distribution or ambiguously versioned cannot be safely consumed.</threat_model>

<tasks>
<task type="execute">
<name>Task 1: Extend registry and documentation for parallel versions</name>
<read_first>scripts/build-registry.mjs, scripts/validate-registry.mjs, tests/registry.test.mjs, docs/VERSIONING.md, README.md</read_first>
<action>Discover v0.1 and v1.0 recursively, preserve immutable version URLs, document migration and required goal limits, and add registry coverage.</action>
<acceptance_criteria>Registry contains both versions without replacing v0.1; migration makes the breaking boundary explicit.</acceptance_criteria>
<verify><automated>npm run build:registry &amp;&amp; npm run validate:registry &amp;&amp; npm test</automated></verify>
</task>
</tasks>

<verification>Registry build/validation and full tests pass.</verification>
<success_criteria>Consumers can discover and validate the v1 goal contract while existing v0.1 clients remain supported.</success_criteria>
<output>Create 02-02-SUMMARY.md and 02-VERIFICATION.md.</output>
