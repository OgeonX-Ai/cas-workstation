---
phase: 02-typed-goal-and-lifecycle-contract
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [GOAL-01, GOAL-02, GOAL-03]
files_modified:
  - schemas/v1.0/*.schema.json
  - examples/v1.0/*.json
  - tests/goal-contract.test.mjs
  - scripts/lib.mjs
---

<objective>Create a strict v1.0 lifecycle and goal contract while retaining v0.1 compatibility.</objective>

<threat_model>Invalid or unbounded goals must fail schema validation before any runtime dispatch; metadata and traces must not drift across records.</threat_model>

<tasks>
<task type="execute">
<name>Task 1: Add RED goal contract tests</name>
<read_first>AGENTS.md, docs/VERSIONING.md, schemas/v0.1/common.schema.json, schemas/v0.1/work-request.schema.json, tests/contracts.test.mjs, scripts/lib.mjs</read_first>
<action>Add tests proving missing required goal fields, zero/unbounded limits, incomplete lifecycle metadata, and malformed trace context fail; assert documented defaults in the valid fixture.</action>
<acceptance_criteria>Tests fail because v1.0 schemas and examples do not exist.</acceptance_criteria>
<verify><automated>node --test tests/goal-contract.test.mjs</automated></verify>
</task>
<task type="execute">
<name>Task 2: Add v1.0 schemas, examples, and multi-version validation</name>
<read_first>tests/goal-contract.test.mjs, scripts/lib.mjs, all schemas/v0.1 and examples/v0.1 files</read_first>
<action>Publish a full v1.0 lifecycle set. Require the approved goal fields and bounded integer limits, reuse v1 common metadata, update helpers to discover both versions, and keep v0.1 unchanged.</action>
<acceptance_criteria>Focused and full tests pass; v0.1 examples still validate; every v1 example carries identical trace metadata.</acceptance_criteria>
<verify><automated>npm test</automated></verify>
</task>
</tasks>

<verification>`npm test` must pass from the isolated contract worktree.</verification>
<success_criteria>Incomplete/unbounded goals are rejected; accepted goals expose all policies and limits; lifecycle metadata is strict and consistent.</success_criteria>
<output>Create 02-01-SUMMARY.md with RED/GREEN and compatibility evidence.</output>
