---
phase: 09-systemic-ci-and-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/cas-contracts/.github/workflows/codeql.yml
  - portfolio/cas-contracts/.github/workflows/pages.yml
  - portfolio/cas-contracts/.github/workflows/pr-lint.yml
  - portfolio/cas-contracts/.github/workflows/stale.yml
autonomous: true
requirements: [CI-01, CI-02, CI-03]
must_haves:
  truths:
    - "CAS Contracts workflow tokens are least privilege and jobs are bounded."
    - "TypeScript CodeQL configuration matches the contract tooling content."
    - "The existing action-update policy remains authoritative."
  artifacts:
    - path: "portfolio/cas-contracts/.github/workflows/codeql.yml"
      provides: "Bounded TypeScript analysis"
    - path: "portfolio/cas-contracts/.github/dependabot.yml"
      provides: "Action update policy"
  key_links:
    - from: "portfolio/cas-contracts/.github/dependabot.yml"
      to: "portfolio/cas-contracts/.github/workflows/*.yml"
      via: "github-actions updates"
      pattern: "package-ecosystem: github-actions"
---

<objective>
Close the remaining CAS Contracts workflow gaps without touching registry publication behavior reserved for Phase 10.

Purpose: Bound workflow authority and execution while preserving independent repository history.
Output: Hardened CodeQL, PR lint, and stale workflows plus one atomic commit.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-systemic-ci-and-security-hardening/09-CONTEXT.md
@docs/improvement-backlog.md
@portfolio/cas-contracts/AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Harden CAS Contracts workflow boundaries</name>
  <files>portfolio/cas-contracts/.github/workflows/codeql.yml, portfolio/cas-contracts/.github/workflows/pages.yml, portfolio/cas-contracts/.github/workflows/pr-lint.yml, portfolio/cas-contracts/.github/workflows/stale.yml</files>
  <action>Use the existing clean checkout and record its `fix/id-migrate-github-pages` branch and HEAD before edits. Never reset, rebase, delete, recreate, or force-switch branches. Add least-privilege permissions and proportionate job timeouts per the locked decisions, including a timeout on the Pages deploy job without changing publication behavior. Keep CodeQL on TypeScript because tracked tooling supports it. Preserve `.github/dependabot.yml` as CI-03 evidence. Do not edit registry generation, schema IDs, publication paths, or compatibility behavior because those belong to Phase 10.</action>
  <verify><automated>powershell -NoProfile -Command "$r='C:\PersonalRepo\portfolio\cas-contracts'; foreach($n in @('codeql.yml','pr-lint.yml','stale.yml')){$c=Get-Content -Raw \"$r\.github\workflows\$n\"; if($c-notmatch'(?m)^permissions:'-or$c-notmatch'timeout-minutes:'){throw \"Gap remains in $n\"}}; if((Get-Content -Raw \"$r\.github\workflows\codeql.yml\")-notmatch'typescript'){throw 'CodeQL language drift'}; if((Get-Content -Raw \"$r\.github\dependabot.yml\")-notmatch'github-actions'){throw 'Action update policy missing'}"</automated></verify>
  <done>The three evidence-backed workflow gaps are closed without registry scope changes.</done>
</task>
<task type="auto">
  <name>Task 2: Validate CAS Contracts and commit workflow-only changes</name>
  <files>portfolio/cas-contracts/.github/workflows/codeql.yml, portfolio/cas-contracts/.github/workflows/pages.yml, portfolio/cas-contracts/.github/workflows/pr-lint.yml, portfolio/cas-contracts/.github/workflows/stale.yml</files>
  <action>Run the native npm validation scripts exposed by `package.json` (`npm test` and `npm run build` when present), parse every workflow YAML file and assert each `jobs.*` entry has `timeout-minutes`, then create one atomic commit `ci: harden workflow permissions and timeouts`. Exclude all schema, registry, generated artifact, and unrelated branch changes. Do not push, merge, deploy, or amend.</action>
  <verify><automated>powershell -NoProfile -Command "Set-Location C:\PersonalRepo\portfolio\cas-contracts; npm.cmd test; if($LASTEXITCODE){exit $LASTEXITCODE}; npm.cmd run build --if-present; if($LASTEXITCODE){exit $LASTEXITCODE}; git diff --check HEAD^ HEAD; $bad=git diff-tree --no-commit-id --name-only -r HEAD|Where-Object{$_-notin @('.github/workflows/codeql.yml','.github/workflows/pr-lint.yml','.github/workflows/stale.yml')}; if($bad){throw \"Out-of-scope commit files: $bad\"}"</automated></verify>
  <done>Native checks pass and one atomic CAS Contracts workflow commit exists.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Pull request to contract CI | Untrusted contributions execute validation with repository token context. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Elevation of Privilege | workflow tokens | mitigate | Explicit read-only defaults and narrow write grants. |
| T-09-04 | Denial of Service | workflow jobs | mitigate | Job-specific timeouts. |
| T-09-SC | Tampering | Actions dependencies | mitigate | Existing Dependabot github-actions policy remains enabled. |
</threat_model>
<verification>Static audit, npm tests, and npm build complete locally without secrets.</verification>
<success_criteria>CAS Contracts satisfies Phase 9 controls without pre-empting Phase 10.</success_criteria>
<output>Create `.planning/phases/09-systemic-ci-and-security-hardening/09-02-SUMMARY.md` when done.</output>
