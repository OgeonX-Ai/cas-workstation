---
phase: 09-systemic-ci-and-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/autogen/.github/workflows/codeql.yml
  - portfolio/autogen/.github/workflows/pages.yml
  - portfolio/autogen/.github/workflows/pr-lint.yml
  - portfolio/autogen/.github/workflows/stale.yml
autonomous: true
requirements: [CI-01, CI-02, CI-03]
must_haves:
  truths:
    - "Every active autogen workflow has least-privilege permissions and a bounded job runtime."
    - "Autogen CodeQL scans only languages supported by tracked repository content."
    - "GitHub Actions updates remain covered by the existing Dependabot policy."
  artifacts:
    - path: "portfolio/autogen/.github/workflows/codeql.yml"
      provides: "Bounded Python and JavaScript CodeQL analysis"
    - path: "portfolio/autogen/.github/workflows/pages.yml"
      provides: "Bounded least-privilege Pages publication"
    - path: "portfolio/autogen/.github/dependabot.yml"
      provides: "GitHub Actions update policy"
  key_links:
    - from: "portfolio/autogen/.github/dependabot.yml"
      to: "portfolio/autogen/.github/workflows/*.yml"
      via: "github-actions package ecosystem"
      pattern: "package-ecosystem: github-actions"
---

<objective>
Close the evidence-backed workflow gaps in the autogen repository while preserving its existing clean `ci/dependabot-github-actions` commit.

Purpose: Satisfy CI-01 through CI-03 for the MAF worker repository without paid services, deployment, or branch rewriting.
Output: Least-privilege, timeout-bounded workflows and one atomic autogen commit.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-systemic-ci-and-security-hardening/09-CONTEXT.md
@docs/improvement-backlog.md
@portfolio/autogen/AGENTS.md
@portfolio/autogen/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden autogen workflow authority and runtime bounds</name>
  <files>portfolio/autogen/.github/workflows/codeql.yml, portfolio/autogen/.github/workflows/pages.yml, portfolio/autogen/.github/workflows/pr-lint.yml, portfolio/autogen/.github/workflows/stale.yml</files>
  <action>Work only in the existing clean `ci/dependabot-github-actions` checkout; first record `git status --short`, HEAD, and the existing `51140dd` commit. Do not reset, rebase, force checkout, delete, or recreate any branch. Per the locked least-privilege and timeout decisions, add explicit top-level or job-level permissions matching actual API use and proportionate `timeout-minutes` to every currently unbounded job. Keep Pages deployment permissions isolated to its deployment job, pull-request read access isolated to PR lint, and issues write access isolated to stale handling. Retain CodeQL languages only where tracked `.py`, `.js`, or `.ts` files justify them. Do not change action versions because `.github/dependabot.yml` already manages the update path per CI-03.</action>
  <verify><automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $repo='C:\PersonalRepo\portfolio\autogen'; $files=Get-ChildItem \"$repo\.github\workflows\*.yml\"; foreach($f in $files){$c=Get-Content -Raw $f; if($c -notmatch '(?m)^permissions:' -or $c -notmatch 'timeout-minutes:'){throw \"Unbounded or permissionless workflow: $($f.Name)\"}}; $d=Get-Content -Raw \"$repo\.github\dependabot.yml\"; if($d -notmatch 'package-ecosystem:\s*[\"'']?github-actions'){throw 'Dependabot github-actions policy missing'}"</automated></verify>
  <done>All active autogen workflows declare explicit minimum permissions and job timeouts; CodeQL languages remain evidence-backed; Dependabot policy is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Run native autogen gates and commit atomically</name>
  <files>portfolio/autogen/.github/workflows/codeql.yml, portfolio/autogen/.github/workflows/pages.yml, portfolio/autogen/.github/workflows/pr-lint.yml, portfolio/autogen/.github/workflows/stale.yml</files>
  <action>Run the repository's local test suite without API keys or paid model calls, parse every workflow YAML file and assert every `jobs.*` entry has `timeout-minutes`, inspect the staged diff for workflow-only scope, and create exactly one atomic commit in autogen with message `ci: harden workflow permissions and timeouts`. Do not push, merge, deploy, amend existing commits, or include unrelated files. Record branch, pre-change HEAD, resulting commit SHA, commands, and exits in the plan summary.</action>
  <verify><automated>powershell -NoProfile -Command "Set-Location C:\PersonalRepo\portfolio\autogen; python -m pytest -q; if($LASTEXITCODE){exit $LASTEXITCODE}; git diff --check HEAD^ HEAD; if($LASTEXITCODE){exit $LASTEXITCODE}; $changed=git diff-tree --no-commit-id --name-only -r HEAD; if($changed | Where-Object {$_ -notlike '.github/workflows/*'}){throw 'Atomic commit contains non-workflow files'}"</automated></verify>
  <done>Native tests pass and one workflow-only autogen commit exists on the preserved feature branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub event to workflow token | Untrusted repository events activate jobs with scoped credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | workflow `permissions` | mitigate | Grant only permissions used by each job. |
| T-09-02 | Denial of Service | workflow jobs | mitigate | Apply proportionate `timeout-minutes`. |
| T-09-SC | Tampering | GitHub Actions dependencies | mitigate | Preserve automated Dependabot updates for the github-actions ecosystem. |
</threat_model>

<verification>All audit checks and `python -m pytest -q` pass with no paid API calls.</verification>
<success_criteria>Autogen satisfies CI-01, CI-02, and CI-03 with one atomic repo-local commit.</success_criteria>
<output>Create `.planning/phases/09-systemic-ci-and-security-hardening/09-01-SUMMARY.md` when done.</output>
