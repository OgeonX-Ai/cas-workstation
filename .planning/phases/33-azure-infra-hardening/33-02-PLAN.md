---
phase: 33-azure-infra-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/cloud-security-service-model/bicepconfig.json
  - portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/identity.bicep
  - portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/logging-siem.bicep
  - portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/network-hubspoke.bicep
  - portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/policy-assignments.bicep
  - portfolio/cloud-security-service-model/docs/adr/001-policy-assignment-enforcement-mode.md
autonomous: true
requirements:
  - REQ-1.4.14
must_haves:
  - "bicepconfig.json use-recent-api-versions rule is not 'off' (warning or error) in portfolio/cloud-security-service-model"
  - "az bicep build --file portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/main.bicep exits 0"
  - "az bicep lint reports zero use-recent-api-versions findings across all bicep files in impl/azure/landing-zone/bicep/"
  - "docs/adr/001-policy-assignment-enforcement-mode.md exists with Context/Decision/Consequences sections recording the DoNotEnforce decision"
must_haves_p4_decision: "DoNotEnforce enforcementMode on the allowed-locations policy assignment is explicitly reaffirmed as intentional (reference/stub landing zone, not a deployed environment) and recorded, not silently left ambiguous"
---

<objective>
Close out the two remaining Phase 33 hardening items scoped to cloud-security-service-model:
P2 (Bicep linting / API version pinning, with REAL violations to fix — unlike cas-platform,
this repo's `use-recent-api-versions` rule was also merged as `"off"` via PR #10, but four
resources here genuinely fail the check once re-enabled) and P4 (record the intentional
`DoNotEnforce` policy assignment decision as a numbered ADR in this repo, following the
repo's own pre-existing `docs/adr/` convention).
</objective>

<context>
Repo: `portfolio/cloud-security-service-model` (Coding-Autopilot-System/cloud-security-service-model
on GitHub). Local checkout's `main` is 2 commits behind `origin/main` — branch from
`origin/main`, not local stale `main`.

Important framing correction found during research: this repo's own `CLAUDE.md` describes
it as "a docs-only repo... No code, no builds, no test suites." That description predates
(or was never updated for) the `impl/azure/landing-zone/bicep/` directory, which contains
five real, buildable Bicep files (`main.bicep` + `modules/identity.bicep`,
`keyvault.bicep`, `logging-siem.bicep`, `network-hubspoke.bicep`,
`policy-assignments.bicep`). The directory's own README
(`impl/azure/landing-zone/README.md`) labels this "Azure Landing Zone (Stub)" and calls the
Bicep files "Bicep stubs" — this is a reference/example landing zone, not a deployed
environment for any real subscription. Treat it accordingly: harden the linting and pin
real API versions (this makes the reference implementation trustworthy as an example to
copy from), but do not invent environment-specific parameter files, CI wiring, or treat a
`DoNotEnforce` policy as an active production gap — it is a stub landing zone.

Verified current state on `origin/main` (HEAD `c17f2ad`) via a detached worktree, running
`az bicep build` and `az bicep lint --diagnostics-format sarif`:

- `bicepconfig.json` (repo root, no leading dot) already sets
  `"use-recent-api-versions": { "level": "off" }` — merged via PR #10
  ("ci: scan actions with codeql and add bicep linter config"), same content as
  cas-platform's config (rule content is otherwise identical between the two repos).
- With the rule temporarily flipped to `"warning"` in a scratch copy, `az bicep lint` on
  every file under `impl/azure/landing-zone/bicep/` surfaced REAL findings (not
  hypothetical — confirmed via live linter run at plan time):
  - `impl/azure/landing-zone/bicep/modules/identity.bicep`:
    `Microsoft.ManagedIdentity/userAssignedIdentities@2023-01-31` is 1252 days old.
    Acceptable versions per the linter: `2025-01-31-preview`, `2024-11-30`.
  - `impl/azure/landing-zone/bicep/modules/logging-siem.bicep`:
    `Microsoft.OperationalInsights/workspaces@2023-09-01` is 1039 days old.
    Acceptable versions: `2025-07-01`, `2025-02-01`.
  - `impl/azure/landing-zone/bicep/modules/network-hubspoke.bicep`: BOTH resources fail —
    `Microsoft.Network/networkSecurityGroups@2024-05-01` and
    `Microsoft.Network/virtualNetworks@2024-05-01` are each 796 days old.
    Acceptable versions for both: `2025-05-01`, `2025-03-01`, `2025-01-01`, `2024-10-01`.
  - `impl/azure/landing-zone/bicep/modules/policy-assignments.bicep`:
    `Microsoft.Authorization/policyAssignments@2024-04-01` is 826 days old.
    Acceptable versions: `2025-03-01`, `2025-01-01`.
  - `impl/azure/landing-zone/bicep/modules/keyvault.bicep`
    (`Microsoft.KeyVault/vaults@2024-11-01`) had ZERO findings — already within the
    freshness window. Do not touch this file's API version.
- `impl/azure/landing-zone/bicep/main.bicep` itself declares no versioned resources
  directly (it only references modules), so its only finding is the transitive one
  surfaced from `identity.bicep`'s module reference during a whole-template lint pass —
  fixing `identity.bicep` directly resolves it.
- The `policy-assignments.bicep` module sets `enforcementMode: 'DoNotEnforce'` with
  `metadata.rolloutState: 'audit'` on a single `allowed-locations` policy assignment. There
  is no other policy assignment in this module. No existing ADR file exists yet in this
  repo (`docs/adr/` contains only the README with authoring rules) — this will be
  `001-*.md`.
- `docs/adr/README.md` (present in this repo) mandates: sequential numbering
  (`001-record-architecture-decisions.md` style), and every ADR documents Context,
  Decision, and Consequences before implementation. Follow this exact structure.
- No CI workflow in this repo invokes `bicep build`/`bicep lint` today — this plan is
  local-validation-only, matching the phase constraint (no az deployment, no CI wiring
  required this phase).
</context>

<tasks>

<task>
<name>1. Enable use-recent-api-versions in bicepconfig.json</name>
<read_first>
- portfolio/cloud-security-service-model/bicepconfig.json
</read_first>
<files>
- portfolio/cloud-security-service-model/bicepconfig.json
</files>
<action>
Create a new branch from `origin/main`: `git fetch origin && git checkout -b fix/bicep-lint-api-version-pinning origin/main`.

Edit `bicepconfig.json` at the repo root. Change exactly one rule entry:

Before:
```json
"use-recent-api-versions": { "level": "off" },
```

After:
```json
"use-recent-api-versions": { "level": "warning" },
```

Use `"warning"`, not `"error"` — this repo has no CI gate to enforce a hard failure yet,
and `"warning"` avoids introducing a first-time hard-fail surface. Do not change any other
rule. Do not create a second `.bicepconfig.json` (dotted name) alongside the existing
undotted `bicepconfig.json` — this would create ambiguous config resolution.
</action>
<acceptance_criteria>
- `git diff` on `bicepconfig.json` shows exactly one line changed.
- File remains valid JSON: `python -c "import json; json.load(open('portfolio/cloud-security-service-model/bicepconfig.json'))"` exits 0.
</acceptance_criteria>
</task>

<task>
<name>2. Pin identity.bicep to a current API version</name>
<read_first>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/identity.bicep
</read_first>
<files>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/identity.bicep
</files>
<action>
Change line 4 from:
```bicep
resource identity 'Microsoft.ManagedIdentity/userAssignedIdentities@2023-01-31' = {
```
to:
```bicep
resource identity 'Microsoft.ManagedIdentity/userAssignedIdentities@2024-11-30' = {
```
Use `2024-11-30` (the stable, non-preview acceptable version) rather than
`2025-01-31-preview` — prefer GA API versions over preview versions when both satisfy the
freshness rule, since this module has no dependency on any preview-only feature. No other
line in this file changes; the resource schema (`name`, `location`, `tags`) is unaffected
by this version bump.
</action>
<acceptance_criteria>
- `az bicep build --file impl/azure/landing-zone/bicep/modules/identity.bicep --stdout` exits 0.
- `az bicep lint --file impl/azure/landing-zone/bicep/modules/identity.bicep --diagnostics-format sarif` contains zero `use-recent-api-versions` results.
- `grep -n "userAssignedIdentities@2024-11-30" impl/azure/landing-zone/bicep/modules/identity.bicep` matches exactly once.
</acceptance_criteria>
</task>

<task>
<name>3. Pin logging-siem.bicep to a current API version</name>
<read_first>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/logging-siem.bicep
</read_first>
<files>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/logging-siem.bicep
</files>
<action>
Change line 4 from:
```bicep
resource workspace 'Microsoft.OperationalInsights/workspaces@2023-09-01' = {
```
to:
```bicep
resource workspace 'Microsoft.OperationalInsights/workspaces@2025-02-01' = {
```
Use `2025-02-01` (matches the version already proven working in cas-platform's own
`observability.bicep`, giving consistency across the portfolio's two repos using this
resource type) rather than `2025-07-01`. No other line changes; properties
(`retentionInDays`, `publicNetworkAccessForIngestion`, `publicNetworkAccessForQuery`,
`features`, `workspaceCapping`) are unaffected by this version bump.
</action>
<acceptance_criteria>
- `az bicep build --file impl/azure/landing-zone/bicep/modules/logging-siem.bicep --stdout` exits 0.
- `az bicep lint --file impl/azure/landing-zone/bicep/modules/logging-siem.bicep --diagnostics-format sarif` contains zero `use-recent-api-versions` results.
- `grep -n "workspaces@2025-02-01" impl/azure/landing-zone/bicep/modules/logging-siem.bicep` matches exactly once.
</acceptance_criteria>
</task>

<task>
<name>4. Pin network-hubspoke.bicep (two resources) to a current API version</name>
<read_first>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/network-hubspoke.bicep
</read_first>
<files>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/network-hubspoke.bicep
</files>
<action>
Change line 4 from:
```bicep
resource nsg 'Microsoft.Network/networkSecurityGroups@2024-05-01' = {
```
to:
```bicep
resource nsg 'Microsoft.Network/networkSecurityGroups@2024-10-01' = {
```
Change line 26 from:
```bicep
resource virtualNetwork 'Microsoft.Network/virtualNetworks@2024-05-01' = {
```
to:
```bicep
resource virtualNetwork 'Microsoft.Network/virtualNetworks@2024-10-01' = {
```
Use `2024-10-01` for both (the oldest of the four acceptable versions the linter listed,
chosen deliberately over the newest `2025-05-01` to minimize schema drift risk on a
network module — networking API versions occasionally rename or restructure properties
between minor releases, and `2024-10-01` is the smallest possible jump that still
satisfies the freshness rule). No other property changes; `securityRules`,
`addressSpace`, `subnets`, and `privateEndpointNetworkPolicies` are unaffected.
</action>
<acceptance_criteria>
- `az bicep build --file impl/azure/landing-zone/bicep/modules/network-hubspoke.bicep --stdout` exits 0.
- `az bicep lint --file impl/azure/landing-zone/bicep/modules/network-hubspoke.bicep --diagnostics-format sarif` contains zero `use-recent-api-versions` results.
- `grep -n "@2024-10-01" impl/azure/landing-zone/bicep/modules/network-hubspoke.bicep` matches exactly twice.
</acceptance_criteria>
</task>

<task>
<name>5. Pin policy-assignments.bicep to a current API version (version bump only — enforcementMode is handled in Task 6)</name>
<read_first>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/policy-assignments.bicep
</read_first>
<files>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/policy-assignments.bicep
</files>
<action>
Change line 3 from:
```bicep
resource allowedLocations 'Microsoft.Authorization/policyAssignments@2024-04-01' = {
```
to:
```bicep
resource allowedLocations 'Microsoft.Authorization/policyAssignments@2025-01-01' = {
```
Use `2025-01-01` rather than `2025-03-01` — prefer the older of the two acceptable
versions since this is a policy assignment resource where property schema stability
across the smallest safe jump is preferable. Do not change `enforcementMode`,
`metadata`, `policyDefinitionId`, or `parameters` in this task — that decision belongs to
Task 6 and must be made deliberately, not as a side effect of an API version bump.
</action>
<acceptance_criteria>
- `az bicep build --file impl/azure/landing-zone/bicep/modules/policy-assignments.bicep --stdout` exits 0.
- `az bicep lint --file impl/azure/landing-zone/bicep/modules/policy-assignments.bicep --diagnostics-format sarif` contains zero `use-recent-api-versions` results.
- `grep -n "policyAssignments@2025-01-01" impl/azure/landing-zone/bicep/modules/policy-assignments.bicep` matches exactly once.
- `grep -n "DoNotEnforce" impl/azure/landing-zone/bicep/modules/policy-assignments.bicep` still matches exactly once (proves Task 5 did not touch enforcement mode).
</acceptance_criteria>
</task>

<task>
<name>6. Record the P4 DoNotEnforce decision as ADR-001</name>
<read_first>
- portfolio/cloud-security-service-model/docs/adr/README.md
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/modules/policy-assignments.bicep
- portfolio/cloud-security-service-model/impl/azure/landing-zone/README.md
</read_first>
<files>
- portfolio/cloud-security-service-model/docs/adr/001-policy-assignment-enforcement-mode.md
</files>
<action>
Decision made for this plan: KEEP `enforcementMode: 'DoNotEnforce'` as-is. Do not flip it
to `Default` (enforce). Rationale to record: the `impl/azure/landing-zone/bicep/` module
tree is an explicitly labeled reference "stub" landing zone (per its own README), not a
deployed environment tied to any real subscription's resources. Flipping to enforcement
mode on a stub/example module would misrepresent the artifact as production-hardening
guidance rather than a syntactically-correct illustration of the pattern, and — because
enforcement mode changes are a live-policy behavioral change with no `az deployment`
validation permitted this phase — could not be safely verified end-to-end here anyway.

Create `docs/adr/001-policy-assignment-enforcement-mode.md` following the README's
required structure (Context, Decision, Consequences) and sequential naming rule:

```markdown
# ADR 001: Policy Assignment Enforcement Mode (DoNotEnforce)

## Status
Accepted

## Context
The `impl/azure/landing-zone/bicep/modules/policy-assignments.bicep` module assigns the
built-in "Allowed locations" policy definition
(`e56962a6-4747-49cd-b67b-bf8b01975c4c`) with `enforcementMode: 'DoNotEnforce'` and
`metadata.rolloutState: 'audit'`. Phase 33 (Azure Infra Hardening) reviewed this setting
to confirm it is an intentional design choice rather than an unreviewed gap.

The `impl/azure/landing-zone/` directory is explicitly documented (see its own README) as
an "Azure Landing Zone (Stub)" — a reference implementation demonstrating the pattern of
identity, network, logging, Key Vault, and policy modules working together, not a
deployed environment bound to any real subscription, management group, or workload.

## Decision
Keep `enforcementMode: 'DoNotEnforce'` (audit-only) for the `allowedLocations` policy
assignment in this stub landing zone. Do not flip to `Default` (enforcing) mode as part
of Phase 33.

This is a deliberate, reviewed exception, not an oversight: enforcing mode on a reference
module (a) misrepresents a teaching/example artifact as hardened production guidance, and
(b) an enforcement-mode change is a live-policy behavior change that this phase's
constraints (no `az deployment`, validation via `bicep build`/`bicep lint` only) cannot
safely verify end-to-end before merge.

## Consequences
- Any team copying this stub landing zone into a real subscription MUST explicitly
  revisit `enforcementMode` and set it to `Default` (or remove `DoNotEnforce` entirely)
  before treating the copied module as a production control. This ADR is the durable
  record of that follow-up obligation.
- No immediate change to deployed infrastructure results from this ADR, because this
  module has never been deployed as production infrastructure (stub/reference only).
- If this landing zone stub is ever promoted to an actively deployed reference
  environment (e.g. a real demo subscription), enforcement mode must be reassessed at
  that time and this ADR superseded by a new one recording that transition.

## Related
- `impl/azure/landing-zone/bicep/modules/policy-assignments.bicep`
- `impl/azure/landing-zone/README.md`
- Phase 33 context: `.planning/phases/33-azure-infra-hardening/33-CONTEXT.md` (in the
  `PersonalRepo` root workspace, not this repository)
```

Adjust only if `docs/adr/README.md`'s numbering convention differs from `001-` at the time
of execution (i.e. if another ADR was merged in the interim, use the next free number and
rename the file accordingly).
</action>
<acceptance_criteria>
- File `docs/adr/001-policy-assignment-enforcement-mode.md` exists (or next sequential
  number if `001` was taken in the interim).
- File contains top-level headings `## Context`, `## Decision`, `## Consequences` (grep
  confirms all three present).
- `npx markdownlint-cli docs/adr/001-policy-assignment-enforcement-mode.md` (per this
  repo's CLAUDE.md convention: "Run markdownlint before committing") exits 0 against the
  repo's `.markdownlint.json` config.
- `enforcementMode: 'DoNotEnforce'` in `policy-assignments.bicep` is unchanged from its
  pre-plan value (grep match count unchanged at exactly 1, confirmed already in Task 5's
  acceptance criteria).
</acceptance_criteria>
</task>

<task>
<name>7. Full-template build and lint gate</name>
<read_first>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/main.bicep
</read_first>
<files>
- portfolio/cloud-security-service-model/impl/azure/landing-zone/bicep/main.bicep
</files>
<action>
Run from the repo root:
```powershell
az bicep build --file impl/azure/landing-zone/bicep/main.bicep
az bicep lint --file impl/azure/landing-zone/bicep/main.bicep
az bicep lint --file impl/azure/landing-zone/bicep/modules/keyvault.bicep
```
Include `keyvault.bicep` explicitly even though it required no version bump, to confirm
it still passes cleanly after the rule was enabled (regression check on the one module
this plan does not modify). No `az deployment` command of any kind — local
validation only per the environment constraint.
</action>
<acceptance_criteria>
- `az bicep build --file impl/azure/landing-zone/bicep/main.bicep` exits 0 with no stderr.
- `az bicep lint --file impl/azure/landing-zone/bicep/main.bicep` exits 0 with zero
  `use-recent-api-versions` findings.
- `az bicep lint --file impl/azure/landing-zone/bicep/modules/keyvault.bicep` exits 0
  with zero findings (confirms no regression on the untouched module).
- `git status --short` shows changes limited to: `bicepconfig.json`, the four modified
  `.bicep` module files named in Tasks 2-5, and the new ADR file from Task 6 — no
  unrelated diffs (e.g. no accidental edits to `keyvault.bicep`, `main.bicep`, or any
  `docs/` file outside `docs/adr/`).
</acceptance_criteria>
</task>

<task>
<name>8. Commit and open PR (no merge)</name>
<read_first>
- portfolio/cloud-security-service-model/CONTRIBUTING.md
</read_first>
<files>
- (git operations only, no further file edits)
</files>
<action>
Stage only the files this plan touched (bicepconfig.json, the four bicep modules, the new
ADR file). Commit with a message describing both changes together or as logically split
commits (either is acceptable; prefer one commit for the lint/API-version work and a
second for the ADR, since they are reviewable as independent concerns). Push the branch
and open a PR against `main` using `gh pr create` referencing that this addresses the P2
and P4 items from Phase 33. Do NOT merge. Do NOT change branch protection settings. Note
per this repo's CLAUDE.md: "All PRs should use `.github/pull_request_template.md`" — use
`gh pr create` in a way that respects the existing template if the repo has one configured
(check `.github/pull_request_template.md` presence first; if absent, a plain description is
fine).
</action>
<acceptance_criteria>
- `gh pr create` succeeds and returns a PR URL.
- `gh pr view <number> --json state` shows `"OPEN"`.
- No `git push --force` used; no `enforce_admins` or branch protection API calls made.
</acceptance_criteria>
</task>

</tasks>

<threat_model>
Main risk: this repo's own CLAUDE.md claims "docs-only... no builds" while
`impl/azure/landing-zone/bicep/` contains real, buildable Bicep — an execution agent that
trusts the stale CLAUDE.md framing over the actual directory contents could skip this
plan's Bicep tasks entirely, believing there is nothing to build. This plan's <context>
section explicitly calls out and overrides that stale framing; Task 1's branch-from-
origin/main step and every subsequent task's `az bicep build`/`lint` invocation are the
concrete evidence that this is buildable infrastructure requiring the same rigor as
cas-platform.
Second risk: treating the P4 `DoNotEnforce` policy as a security gap requiring enforcement
flip, when it is in fact appropriate for a stub/reference module — flipping enforcement
mode on example code masquerading it as hardened guidance would be actively misleading to
anyone copying the stub, and (separately) is a live-policy behavior change this phase
cannot validate end-to-end without `az deployment` access, which is blocked this phase.
Task 6 documents this reasoning explicitly rather than silently leaving the mode ambiguous
or flipping it without validation.
Third risk: API version bumps on `network-hubspoke.bicep` (NSG + VNet) carry the highest
schema-drift risk of the four version changes in this plan, since networking resource
schemas evolve faster than identity/logging resources. Mitigation: Task 4 deliberately
selects the OLDEST acceptable version (`2024-10-01`) rather than the newest, minimizing
the size of the jump from the currently pinned `2024-05-01`, and the acceptance criteria
require a clean `az bicep build` to catch any schema break before merge (though full
behavioral confirmation would still require `az deployment what-if`, which is out of scope
this phase — call this out explicitly if a future phase adds deployment validation).
Fourth risk: silently reintroducing stale linter config by branching from local stale
`main` instead of `origin/main`, missing PR #10's already-merged `bicepconfig.json` and
building on top of an older baseline — Task 1 explicitly branches from `origin/main` to
avoid this.
</threat_model>
