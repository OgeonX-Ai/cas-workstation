---
phase: 09-systemic-ci-and-security-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/gsd-orchestrator/.github/workflows/ci.yml
  - portfolio/gsd-orchestrator/.github/workflows/codeql.yml
  - portfolio/gsd-orchestrator/.github/workflows/pages.yml
  - portfolio/gsd-orchestrator/.github/workflows/pr-lint.yml
  - portfolio/gsd-orchestrator/.github/workflows/stale.yml
autonomous: true
requirements: [CI-01, CI-02, CI-03]
must_haves:
  truths:
    - "Every orchestrator workflow has explicit minimum permissions and bounded jobs."
    - "CodeQL languages correspond to tracked C# and Python content."
    - "The existing Dependabot feature commit remains in branch history."
  artifacts:
    - path: "portfolio/gsd-orchestrator/.github/workflows/ci.yml"
      provides: "Bounded .NET CI"
    - path: "portfolio/gsd-orchestrator/.github/workflows/codeql.yml"
      provides: "Bounded C# and Python analysis"
    - path: "portfolio/gsd-orchestrator/.github/dependabot.yml"
      provides: "Action update policy"
  key_links:
    - from: "portfolio/gsd-orchestrator/.github/workflows/ci.yml"
      to: "portfolio/gsd-orchestrator/src/GsdOrchestrator.sln"
      via: "dotnet build and test"
      pattern: "dotnet (build|test)"
---

<objective>
Harden the orchestrator's workflows on top of its existing clean Dependabot branch.

Purpose: Ensure the authoritative control-plane repository runs with bounded time and minimum token authority.
Output: Five hardened workflows and one atomic orchestrator commit preserving `7c9e868`.
</objective>
<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-systemic-ci-and-security-hardening/09-CONTEXT.md
@portfolio/gsd-orchestrator/AGENTS.md
@portfolio/gsd-orchestrator/CLAUDE.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Harden orchestrator workflow permissions and timeouts</name>
  <files>portfolio/gsd-orchestrator/.github/workflows/ci.yml, portfolio/gsd-orchestrator/.github/workflows/codeql.yml, portfolio/gsd-orchestrator/.github/workflows/pages.yml, portfolio/gsd-orchestrator/.github/workflows/pr-lint.yml, portfolio/gsd-orchestrator/.github/workflows/stale.yml</files>
  <action>Work on the existing clean `ci/dependabot-github-actions` branch and record status, HEAD, and the existing `7c9e868` commit before editing. Do not reset, rebase, amend, delete, recreate, or force checkout branches. Add explicit least-privilege permissions and proportionate timeouts to every active job. Keep C# and Python CodeQL only after confirming tracked `.cs` and `.py` files. Isolate Pages, PR-lint, stale, and security-event write grants to the jobs that require them. Preserve Dependabot action updates and do not pin manually in this phase.</action>
  <verify><automated>powershell -NoProfile -Command "$r='C:\PersonalRepo\portfolio\gsd-orchestrator'; foreach($f in Get-ChildItem \"$r\.github\workflows\*.yml\"){$c=Get-Content -Raw $f; if($c-notmatch'(?m)^permissions:'-or$c-notmatch'timeout-minutes:'){throw \"Policy gap: $($f.Name)\"}}; $q=Get-Content -Raw \"$r\.github\workflows\codeql.yml\"; if($q-notmatch'csharp'-or$q-notmatch'python'){throw 'CodeQL languages changed without evidence'}; git -C $r merge-base --is-ancestor 7c9e868 HEAD; if($LASTEXITCODE){throw 'Existing Dependabot commit not preserved'}"</automated></verify>
  <done>All orchestrator workflows are least privilege and bounded; supported CodeQL languages and prior feature history are preserved.</done>
</task>
<task type="auto">
  <name>Task 2: Run .NET gates and create atomic orchestrator commit</name>
  <files>portfolio/gsd-orchestrator/.github/workflows/ci.yml, portfolio/gsd-orchestrator/.github/workflows/codeql.yml, portfolio/gsd-orchestrator/.github/workflows/pages.yml, portfolio/gsd-orchestrator/.github/workflows/pr-lint.yml, portfolio/gsd-orchestrator/.github/workflows/stale.yml</files>
  <action>Run the exact native solution build and test commands from repo instructions. Parse every workflow YAML file and assert every `jobs.*` entry has `timeout-minutes`. Commit only workflow changes as `ci: harden workflow permissions and timeouts`. Do not invoke live GitHub, MCP, Claude, deployment, or paid API paths; do not push, merge, or amend. Record all evidence in the summary.</action>
  <verify><automated>powershell -NoProfile -Command "Set-Location C:\PersonalRepo\portfolio\gsd-orchestrator; dotnet build src/GsdOrchestrator.sln; if($LASTEXITCODE){exit $LASTEXITCODE}; dotnet test src/GsdOrchestrator.Tests/GsdOrchestrator.Tests.csproj --no-build; if($LASTEXITCODE){exit $LASTEXITCODE}; git diff --check HEAD^ HEAD; $bad=git diff-tree --no-commit-id --name-only -r HEAD|Where-Object{$_-notlike'.github/workflows/*'}; if($bad){throw \"Non-workflow files committed: $bad\"}"</automated></verify>
  <done>Build and tests pass and one atomic workflow-only commit follows the preserved Dependabot commit.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GitHub events to control-plane CI | Untrusted event data executes around authoritative orchestration code. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Elevation of Privilege | all workflow tokens | mitigate | Explicit read defaults and job-local write grants. |
| T-09-08 | Denial of Service | all workflow jobs | mitigate | Proportionate job timeouts. |
| T-09-SC | Tampering | Actions dependencies | mitigate | Preserve verified Dependabot update policy and commit ancestry. |
</threat_model>
<verification>Policy audit, .NET build, .NET tests, ancestry check, and commit-scope check pass.</verification>
<success_criteria>GSD orchestrator satisfies CI-01 through CI-03 with prior branch work intact.</success_criteria>
<output>Create `.planning/phases/09-systemic-ci-and-security-hardening/09-04-SUMMARY.md` when done.</output>
