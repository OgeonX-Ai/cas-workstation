---
phase: 09-systemic-ci-and-security-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/cas-reference-product/.github/workflows/ci.yml
  - portfolio/cas-reference-product/.github/workflows/codeql.yml
  - portfolio/cas-reference-product/.github/workflows/pages.yml
autonomous: true
requirements: [CI-01, CI-02, CI-03]
must_haves:
  truths:
    - "Reference-product CI, CodeQL, and Pages jobs terminate within declared bounds."
    - "CodeQL uses least-privilege permissions and scans Python content."
    - "Dependabot manages action updates without paid services."
  artifacts:
    - path: "portfolio/cas-reference-product/.github/workflows/ci.yml"
      provides: "Bounded native validation"
    - path: "portfolio/cas-reference-product/.github/workflows/codeql.yml"
      provides: "Least-privilege bounded Python CodeQL"
  key_links:
    - from: "portfolio/cas-reference-product/.github/workflows/codeql.yml"
      to: "portfolio/cas-reference-product/app"
      via: "Python CodeQL scan"
      pattern: "python"
---

<objective>
Bound and least-privilege the reference product workflows without changing Foundry, identity, application, or deployment behavior.

Purpose: Close the remaining Phase 9 workflow findings in the reference implementation.
Output: Three hardened workflow files and one atomic repository commit.
</objective>
<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-systemic-ci-and-security-hardening/09-CONTEXT.md
@portfolio/cas-reference-product/AGENTS.md
@portfolio/cas-reference-product/CLAUDE.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Bound reference-product workflows</name>
  <files>portfolio/cas-reference-product/.github/workflows/ci.yml, portfolio/cas-reference-product/.github/workflows/codeql.yml, portfolio/cas-reference-product/.github/workflows/pages.yml</files>
  <action>Start from the clean `main` checkout after recording status and HEAD. If `ci/phase-09-workflow-hardening` exists, verify it is clean and descends from current `main`; stop on divergence. Otherwise create it non-destructively. Never reset, rebase, delete, overwrite, or force checkout branches. Add proportionate timeouts to every job, set top-level permissions to `contents: read`, move `packages: write` to the docker job only, and keep explicit least-privilege CodeQL permissions. Keep Python as the CodeQL language. Preserve the existing Dependabot github-actions policy. Do not alter application code, Foundry configuration, managed identity, Pages content, or deployment triggers.</action>
  <verify><automated>powershell -NoProfile -Command "$r='C:\PersonalRepo\portfolio\cas-reference-product'; foreach($n in @('ci.yml','codeql.yml','pages.yml')){$c=Get-Content -Raw \"$r\.github\workflows\$n\"; if($c-notmatch'timeout-minutes:'){throw \"Missing timeout: $n\"}}; $q=Get-Content -Raw \"$r\.github\workflows\codeql.yml\"; if($q-notmatch'(?m)^permissions:'-or$q-notmatch'python'){throw 'CodeQL permissions/language invalid'}; if((Get-Content -Raw \"$r\.github\dependabot.yml\")-notmatch'github-actions'){throw 'Dependabot policy missing'}"</automated></verify>
  <done>All active reference-product jobs are bounded and CodeQL is least privilege with the correct language.</done>
</task>
<task type="auto">
  <name>Task 2: Run offline reference-product validation and commit</name>
  <files>portfolio/cas-reference-product/.github/workflows/ci.yml, portfolio/cas-reference-product/.github/workflows/codeql.yml, portfolio/cas-reference-product/.github/workflows/pages.yml</files>
  <action>Run the repository's Python tests in offline/local mode with no Azure deployment and no paid API calls. Parse every workflow YAML file and assert every `jobs.*` entry has a timeout; assert only the docker job has `packages: write`. Create one atomic commit `ci: harden workflow permissions and timeouts`; do not push, merge, deploy, or include non-workflow files. Capture exact branch and SHA evidence.</action>
  <verify><automated>powershell -NoProfile -Command "Set-Location C:\PersonalRepo\portfolio\cas-reference-product; python -m pytest -q; if($LASTEXITCODE){exit $LASTEXITCODE}; git diff --check HEAD^ HEAD; $bad=git diff-tree --no-commit-id --name-only -r HEAD|Where-Object{$_-notlike'.github/workflows/*'}; if($bad){throw \"Non-workflow files committed: $bad\"}"</automated></verify>
  <done>Offline tests pass and one workflow-only commit exists on the dedicated feature branch.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Repository event to CI/Pages | Event-controlled inputs reach jobs with GitHub token access. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Elevation of Privilege | CodeQL workflow | mitigate | Explicit security-events write and contents read only. |
| T-09-06 | Denial of Service | CI and Pages jobs | mitigate | Proportionate timeouts on every job. |
| T-09-SC | Tampering | Actions dependencies | mitigate | Dependabot manages github-actions updates. |
</threat_model>
<verification>Offline pytest and workflow policy checks pass.</verification>
<success_criteria>Reference product meets CI-01 through CI-03 without runtime or Azure changes.</success_criteria>
<output>Create `.planning/phases/09-systemic-ci-and-security-hardening/09-03-SUMMARY.md` when done.</output>
