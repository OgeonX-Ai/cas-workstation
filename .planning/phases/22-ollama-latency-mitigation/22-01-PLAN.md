# Phase 22 Plan: Ollama Latency Mitigation

**Status:** Planned

## Objective
Verify asynchronous API interactions and keep-alive caching for Ollama local models to drastically reduce router latency, ensuring p95 latency stays under 2.5s.

## Tasks
1. `[ ]` **Verify Benchmark Async Migration**: Ensure `benchmark-ollama-router.ps1` utilizes runspaces to perform parallel requests. (Already implemented in source).
2. `[ ]` **Verify Router API Migration**: Ensure `classify-engineering-task.ps1` uses `Invoke-RestMethod` and `keep_alive=5m`. (Already implemented in source).
3. `[ ]` **Run Latency Benchmarks**: Execute `benchmark-ollama-router.ps1` to produce `ollama-benchmark.json`. Prove it meets the `< 2500ms` p95 requirement and `> 90%` accuracy requirement.
4. `[ ]` **Enable Policy**: Once benchmarks pass, update `engineering-os\router\ollama-policy.json` to set `enabled: true`.
5. `[ ]` **System Verification**: Run `verify-engineering-os.ps1` to assert that all cross-repository checks pass with Ollama routing enabled.

## Planning Metadata
- sdlcProfile: standard
- riskClass: medium
- requiredVerifiers: benchmark-ollama-router.ps1, verify-engineering-os.ps1
- evidenceLocations: engineering-os/router/ollama-benchmark.json
- overrideState: none
