---
phase: 31-org-ci-supply-chain-hardening
plan: 06
type: execute
wave: 3
depends_on: ["31-02", "31-03", "31-04", "31-05"]
files_modified:
  - .planning/phases/31-org-ci-supply-chain-hardening/31-VERIFICATION.md
autonomous: true
requirements: [REQ-1.4.10]

must_haves:
  truths:
    - "scripts/workflow-lint.ps1 runs clean (exit 0, zero findings) against every one of the 13 portfolio repos' PR branch content"
    - "Every repo with an open PR from this phase is confirmed still open (not merged, not closed) since merges are blocked by environment policy"
    - "The verification report explicitly lists, per repo, which branch was linted and the exit code/finding count, so the result is auditable rather than a bare pass/fail claim"
  artifacts:
    - path: ".planning/phases/31-org-ci-supply-chain-hardening/31-VERIFICATION.md"
      provides: "Per-repo workflow-lint results and open-PR confirmation for all 13 portfolio repos"
      min_lines: 40
  key_links:
    - from: "31-VERIFICATION.md"
      to: "scripts/workflow-lint.ps1 (31-01) output"
      via: "recorded per-repo exit codes and finding counts"
      pattern: "workflow-lint"
---

<objective>
Prove REQ-1.4.10 org-wide by running `scripts/workflow-lint.ps1` (built in 31-01) against the
PR-branch content of all 13 portfolio repos remediated in 31-02 through 31-05, and produce a
single verification report.

Purpose: 31-02 through 31-05 each verify their own batch in isolation. This plan is the
phase-level closing check: it re-fetches every repo's remediation branch (not a locally
re-derived guess) and re-runs the same linter used throughout the phase, so the final claim
"REQ-1.4.10 satisfied org-wide" is backed by one consistent, reproducible sweep rather than four
separate partial confirmations. It also confirms none of the PRs were accidentally merged or
closed, since merges are blocked by environment policy and any merge would indicate a policy
violation worth flagging.

Output: `.planning/phases/31-org-ci-supply-chain-hardening/31-VERIFICATION.md` — a per-repo
table of branch name, workflow-lint exit code, finding count, and PR state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-org-ci-supply-chain-hardening/31-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- The 13 repos and their expected remediation branch names, per 31-02..31-05. -->
<!-- autopilot-demo has no branch (confirmed already-compliant in 31-02 Task 3). -->

| Repo | Expected branch (or "none - audited compliant") | Default branch |
|---|---|---|
| Promptimprover | ci/sha-pin-and-least-privilege | master |
| autogen | ci/sha-pin-and-least-privilege | main |
| autopilot-core | ci/sha-pin-actions | main |
| autopilot-demo | none - audited compliant (31-02 Task 3) | main |
| cas-contracts | ci/sha-pin-and-least-privilege | main |
| cas-evals | ci/sha-pin-actions | main |
| cas-platform | ci/sha-pin-actions | main |
| cas-reference-product | ci/sha-pin-actions | main |
| cas-workstation | ci/sha-pin-actions | main |
| cloud-security-service-model | ci/sha-pin-actions | main |
| gsd-orchestrator | ci/sha-pin-and-least-privilege | main |
| org-dotgithub | ci/sha-pin-actions | main (repo is Coding-Autopilot-System/.github) |
| ci-autopilot | ci/sha-pin-and-coverage-gate | main |

If any executor plan (31-02..31-05) used a different branch name than listed here (e.g. because
an earlier task's automation adjusted for a name collision), use the actual branch created —
check `git -C portfolio/<repo> branch -a` and each plan's SUMMARY.md for the real name rather
than assuming this table is authoritative if it conflicts with what was actually pushed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run workflow-lint against every repo's remediation branch</name>
  <files>.planning/phases/31-org-ci-supply-chain-hardening/31-VERIFICATION.md</files>
  <action>
    For each of the 13 repos, read that repo's plan SUMMARY (31-02-SUMMARY.md,
    31-03-SUMMARY.md, 31-04-SUMMARY.md, or 31-05-SUMMARY.md) to confirm the actual branch name
    pushed, then for each repo: `git -C portfolio/<repo> fetch origin <branch>` and
    `git -C portfolio/<repo> checkout <branch>` (or use `git -C portfolio/<repo> show
    <branch>:.github/workflows/<file>` per-file if you prefer not to change the working tree's
    checked-out branch — either approach is acceptable as long as the content linted is
    genuinely the remote branch's content, not local working-tree state that might differ).
    Run `pwsh -File scripts/workflow-lint.ps1 -Path portfolio/<repo> -Json` against that
    checked-out content and record the exit code and parsed finding count. For autopilot-demo,
    run the same lint against `origin/main` directly (no branch — confirmed compliant with no
    changes needed in 31-02). Restore each repo's working tree to whatever branch it was on
    before this task started once done reading it, so this verification pass does not leave
    repos on unexpected checkouts.
  </action>
  <verify>
    <automated>pwsh -File scripts/workflow-lint.ps1 -Path portfolio -Json | Out-Null; echo "exit=$LASTEXITCODE (expected non-zero here only if run against unremediated main - this validates the linter still detects the pre-remediation baseline on main, confirming the branches' compliance is real and not a lint tautology)"