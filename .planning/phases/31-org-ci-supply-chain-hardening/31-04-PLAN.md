---
phase: 31-org-ci-supply-chain-hardening
plan: 04
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - portfolio/cas-workstation/.github/workflows/codeql.yml
  - portfolio/cas-workstation/.github/workflows/pages.yml
  - portfolio/cas-workstation/.github/workflows/pr-lint.yml
  - portfolio/cas-workstation/.github/workflows/stale.yml
  - portfolio/cloud-security-service-model/.github/workflows/codeql.yml
  - portfolio/cloud-security-service-model/.github/workflows/pages.yml
  - portfolio/cloud-security-service-model/.github/workflows/pr-lint.yml
  - portfolio/cloud-security-service-model/.github/workflows/stale.yml
  - portfolio/gsd-orchestrator/.github/workflows/ci.yml
  - portfolio/gsd-orchestrator/.github/workflows/codeql.yml
  - portfolio/gsd-orchestrator/.github/workflows/pages.yml
  - portfolio/gsd-orchestrator/.github/workflows/pr-lint.yml
  - portfolio/gsd-orchestrator/.github/workflows/stale.yml
  - portfolio/org-dotgithub/.github/workflows/codeql.yml
  - portfolio/org-dotgithub/.github/workflows/pages.yml
  - portfolio/org-dotgithub/.github/workflows/pr-lint.yml
  - portfolio/org-dotgithub/.github/workflows/stale.yml
autonomous: true
requirements: [REQ-1.4.10]

must_haves:
  truths:
    - "cas-workstation, cloud-security-service-model, gsd-orchestrator, org-dotgithub each have a PR open against origin/main with every third-party action pinned to a commit SHA"
    - "gsd-orchestrator's four workflows currently missing permissions: (ci.yml, codeql.yml, pr-lint.yml, stale.yml) gain least-privilege blocks matching each workflow's actual token usage"
    - "cas-workstation's and cloud-security-service-model's CodeQL matrices remain language: ['actions'] (already correct per S2 - this plan does not regress them)"
  artifacts:
    - path: "portfolio/cas-workstation/.github/workflows/*.yml"
      provides: "SHA-pinned actions on the 4 files needing it"
    - path: "portfolio/cloud-security-service-model/.github/workflows/*.yml"
      provides: "SHA-pinned actions on the 4 files needing it"
    - path: "portfolio/gsd-orchestrator/.github/workflows/*.yml"
      provides: "SHA-pinned actions + permissions blocks on all 5 workflow files"
    - path: "portfolio/org-dotgithub/.github/workflows/*.yml"
      provides: "SHA-pinned actions on the 4 files needing it"
  key_links:
    - from: "each remediated workflow file"
      to: "scripts/workflow-lint.ps1 (from 31-01)"
      via: "pwsh -File scripts/workflow-lint.ps1 -Path <repo>"
      pattern: "workflow-lint: clean"
---

<objective>
Remediate GitHub Actions supply-chain and least-privilege gaps (S1, S2, S3 from
docs/improvement-backlog.md; REQ-1.4.10) in four portfolio repos: cas-workstation,
cloud-security-service-model, gsd-orchestrator, org-dotgithub.

Purpose: Same rationale as 31-02/31-03. cas-workstation and cloud-security-service-model are
the two repos S2 explicitly names as having had mismatched CodeQL languages — both are already
fixed to `language: ['actions']` in the current tree (verified via git log:
cas-workstation#14 "harden github workflows with least-privilege permissions and timeouts",
cloud-security-service-model "add codeowners and harden workflow permissions") — this plan must
not regress that fix while pinning their remaining unpinned actions. gsd-orchestrator is
missing `permissions:` on 4 of its 6 workflows entirely.

Output: Updated workflow YAML in 4 repos, each pushed to a new branch off `origin/main` and
opened as a PR (no merge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-org-ci-supply-chain-hardening/31-CONTEXT.md

<interfaces>
<!-- Resolved commit SHAs for every third-party action used in this batch's workflows. -->
<!-- Verified 2026-07-06 via `gh api repos/<owner>/<action>/commits/<tag>`. Use these exact -->
<!-- SHAs - do not re-resolve, do not guess. -->

| Action + tag | Pinned form |
|---|---|
| actions/checkout@v4 | actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4 |
| actions/checkout@v6 | actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6 |
| actions/setup-python@v5 | actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5 |
| actions/setup-dotnet@v5 | actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5 |
| actions/upload-pages-artifact@v3 | actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3 |
| actions/deploy-pages@v4 | actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4 |
| actions/upload-artifact@v4 | actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4 |
| actions/stale@v8 | actions/stale@1160a2240286f5da8ec72b1c0816ce2481aabf84 # v8 |
| github/codeql-action/init@v3 | github/codeql-action/init@411c4c9a36b3fca4d674f06b6396b2c6d23522c6 # v3 |
| github/codeql-action/autobuild@v3 | github/codeql-action/autobuild@411c4c9a36b3fca4d674f06b6396b2c6d23522c6 # v3 |
| github/codeql-action/analyze@v3 | github/codeql-action/analyze@411c4c9a36b3fca4d674f06b6396b2c6d23522c6 # v3 |
| amannn/action-semantic-pull-request@v5 | amannn/action-semantic-pull-request@e32d7e603df1aa1ba07e981f2a23455dee596825 # v5 |

<!-- Per-workflow file states, discovered 2026-07-06. Only listed files need edits within each repo. -->

cas-workstation (4 files, all need SHA-pinning only - CodeQL matrix already correct
`['actions']`, permissions/timeout already present on all - DO NOT change the language matrix):
- codeql.yml: checkout@v4, codeql-action/{init,autobuild,analyze}@v3
- pages.yml: checkout@v4, setup-python@v5, upload-pages-artifact@v3, deploy-pages@v4
- pr-lint.yml: amannn/action-semantic-pull-request@v5
- stale.yml: actions/stale@v8
(quality.yml already fully SHA-pinned per audit — do not touch)

cloud-security-service-model (4 files, all need SHA-pinning only - CodeQL matrix already
correct `['actions']`, permissions/timeout already present on all - DO NOT change the language
matrix):
- codeql.yml: checkout@v4, codeql-action/{init,autobuild,analyze}@v3
- pages.yml: checkout@v4, setup-python@v5, upload-pages-artifact@v3, deploy-pages@v4
- pr-lint.yml: amannn/action-semantic-pull-request@v5
- stale.yml: actions/stale@v8
(ci.yml and static.yml already fully SHA-pinned per audit — do not touch)

gsd-orchestrator (5 files - SHA-pinning on all + missing permissions: on ci.yml, codeql.yml,
pr-lint.yml, stale.yml):
- ci.yml: checkout@v6, setup-dotnet@v5, upload-artifact@v4 (NO permissions: - add top-level
  `permissions:\n  contents: read` — this workflow only checks out and builds/tests .NET code
  and uploads a build artifact, no write access needed)
- codeql.yml: checkout@v4, codeql-action/{init,autobuild,analyze}@v3 (NO permissions: - add
  top-level `permissions:\n  contents: read` plus job-level
  `permissions:\n  actions: read\n  contents: read\n  security-events: write` under the
  analyze job, mirroring portfolio/cas-platform/.github/workflows/codeql.yml exactly)
- pages.yml: checkout@v4, setup-python@v5, upload-pages-artifact@v3, deploy-pages@v4
  (permissions: already present)
- pr-lint.yml: amannn/action-semantic-pull-request@v5 (NO permissions: - add
  `permissions:\n  pull-requests: read`)
- stale.yml: actions/stale@v8 (NO permissions: - add
  `permissions:\n  issues: write\n  pull-requests: write`)
(contract-registry-live.yml already fully SHA-pinned with permissions per audit — do not touch)

org-dotgithub (4 files, all need SHA-pinning only - permissions/timeout already present on all):
- codeql.yml: checkout@v4, codeql-action/{init,autobuild,analyze}@v3
- pages.yml: checkout@v4, setup-python@v5, upload-pages-artifact@v3, deploy-pages@v4
- pr-lint.yml: amannn/action-semantic-pull-request@v5
- stale.yml: actions/stale@v8
(Note: this repo's remote is `Coding-Autopilot-System/.github` — the org profile repo — use
this exact remote name, not "org-dotgithub", when running `gh pr create --repo`.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate cas-workstation and cloud-security-service-model workflows</name>
  <files>portfolio/cas-workstation/.github/workflows/*.yml, portfolio/cloud-security-service-model/.github/workflows/*.yml</files>
  <action>
    In portfolio/cas-workstation: replace every unpinned `uses:` line across codeql.yml,
    pages.yml, pr-lint.yml, stale.yml with its pinned form from the SHA table. Do NOT modify
    the CodeQL `strategy.matrix.language` value — it is already `['actions']` (fixed in commit
    e2d0905), correct per S2, and must not regress. Do not touch quality.yml (already fully
    SHA-pinned). Fetch `origin/main`, branch `ci/sha-pin-actions` off the fetched tip (not the
    currently-checked-out `chore/governance-hardening` branch), commit
    `ci: pin third-party actions to commit SHAs (REQ-1.4.10)`, push, open a PR targeting `main`.

    In portfolio/cloud-security-service-model: replace every unpinned `uses:` line across
    codeql.yml, pages.yml, pr-lint.yml, stale.yml with its pinned form from the SHA table. Do
    NOT modify the CodeQL `strategy.matrix.language` value — already `['actions']`, correct per
    S2, must not regress. Do not touch ci.yml or static.yml (already fully SHA-pinned). Fetch
    `origin/main`, branch `ci/sha-pin-actions` off the fetched tip (not the currently-checked-out
    `chore/governance-hardening` branch), commit `ci: pin third-party actions to commit SHAs
    (REQ-1.4.10)`, push, open a PR targeting `main`. Do NOT merge either repo. Do NOT modify
    branch protection.
  </action>
  <verify>
    <automated>pwsh -File scripts/workflow-lint.ps1 -Path portfolio/cas-workstation -Json; pwsh -File scripts/workflow-lint.ps1 -Path portfolio/cloud-security-service-model -Json</automated>
  </verify>
  <done>Both repos' workflow-lint runs against the new branch content report zero findings. Both CodeQL `language:` matrix values remain `['actions']` (grep confirms post-edit). Both PRs are open (not merged).</done>
</task>

<task type="auto">
  <name>Task 2: Remediate gsd-orchestrator workflows</name>
  <files>portfolio/gsd-orchestrator/.github/workflows/ci.yml, portfolio/gsd-orchestrator/.github/workflows/codeql.yml, portfolio/gsd-orchestrator/.github/workflows/pages.yml, portfolio/gsd-orchestrator/.github/workflows/pr-lint.yml, portfolio/gsd-orchestrator/.github/workflows/stale.yml</files>
  <action>
    Replace every unpinned `uses:` line across ci.yml, codeql.yml, pages.yml, pr-lint.yml,
    stale.yml with its pinned form from the SHA table above, preserving trailing `# vX`
    comments and existing indentation. Additionally add a `permissions:` block to the four
    files that lack one entirely: ci.yml gets top-level `permissions:\n  contents: read`;
    codeql.yml gets top-level `permissions:\n  contents: read` plus job-level
    `permissions:\n  actions: read\n  contents: read\n  security-events: write` under the
    analyze job (mirror portfolio/cas-platform/.github/workflows/codeql.yml's structure
    exactly); pr-lint.yml gets `permissions:\n  pull-requests: read`; stale.yml gets
    `permissions:\n  issues: write\n  pull-requests: write`. Do not touch
    contract-registry-live.yml (already fully SHA-pinned with permissions). Do not add
    timeout-minutes anywhere — already present on all jobs across these 5 files.

    Fetch `origin/main`, create branch `ci/sha-pin-and-least-privilege` off the fetched tip
    (not the currently-checked-out `ci/dependabot-github-actions` branch), commit
    `ci: pin third-party actions to commit SHAs and add least-privilege permissions
    (REQ-1.4.10)`, push, and open a PR via `gh pr create` targeting `main`. Do NOT merge. Do
    NOT modify branch protection.
  </action>
  <verify>
    <automated>pwsh -File scripts/workflow-lint.ps1 -Path portfolio/gsd-orchestrator -Json</automated>
  </verify>
  <done>gsd-orchestrator's workflow-lint run against the new branch content reports zero findings across all three check classes. PR is open (not merged).</done>
</task>

<task type="auto">
  <name>Task 3: Remediate org-dotgithub workflows</name>
  <files>portfolio/org-dotgithub/.github/workflows/*.yml</files>
  <action>
    Replace every unpinned `uses:` line across codeql.yml, pages.yml, pr-lint.yml, stale.yml
    with its pinned form from the SHA table above. No permissions/timeout changes needed
    anywhere in this repo (already present on all jobs per audit).

    Fetch `origin/main`, create branch `ci/sha-pin-actions` off the fetched tip (not the
    currently-checked-out `chore/governance-hardening` branch), commit
    `ci: pin third-party actions to commit SHAs (REQ-1.4.10)`, push, and open a PR via
    `gh pr create --repo Coding-Autopilot-System/.github` targeting `main` (this repo's remote
    is the org's `.github` profile repo, not literally named `org-dotgithub` on GitHub). Do NOT
    merge. Do NOT modify branch protection.
  </action>
  <verify>
    <automated>pwsh -File scripts/workflow-lint.ps1 -Path portfolio/org-dotgithub -Json</automated>
  </verify>
  <done>org-dotgithub's workflow-lint run against the new branch content reports zero findings. PR is open (not merged) against Coding-Autopilot-System/.github.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR branch -> origin/main | New branches are pushed and PRs opened but never merged; branch protection and default-branch content untouched |
| cas-workstation / cloud-security-service-model CodeQL matrices | Already-correct `language: ['actions']` values must survive this plan's edits unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-10 | Tampering | Third-party action commit SHA resolution | mitigate | Every SHA resolved via `gh api repos/<owner>/<action>/commits/<tag>` on 2026-07-06, matching SHAs already running successfully elsewhere in the portfolio |
| T-31-11 | Elevation of Privilege | Regression of already-correct CodeQL language matrices in cas-workstation / cloud-security-service-model during unrelated SHA-pinning edits | mitigate | Task 1 explicitly instructs not to touch `strategy.matrix.language` and the verify step re-checks both values post-edit via grep |
| T-31-12 | Denial of Service | New `permissions:` blocks added to gsd-orchestrator's ci/codeql/pr-lint/stale workflows breaking token-dependent steps (e.g. .NET build artifact upload) | mitigate | ci.yml's new permissions block is scoped to what it actually does (checkout + build + upload-artifact needs only `contents: read`); codeql.yml mirrors the already-working cas-platform pattern; pr-lint.yml/stale.yml mirror each action's documented minimum-scope requirement |
| T-31-13 | Spoofing | Wrong `--repo` target when opening org-dotgithub's PR (local dir name vs. actual GitHub repo name) | mitigate | Task 3 explicitly names the correct remote (`Coding-Autopilot-System/.github`) rather than assuming the local directory name matches |
</threat_model>

<verification>
For each of cas-workstation, cloud-security-service-model, gsd-orchestrator, org-dotgithub: `pwsh -File scripts/workflow-lint.ps1 -Path portfolio/<repo> -Json` against the PR branch content reports zero findings, and `gh pr list` (with the correct `--repo` target) shows the new PR open. cas-workstation's and cloud-security-service-model's CodeQL `language:` matrix values are unchanged from `['actions']`.
</verification>

<success_criteria>
4 repos remediated with open PRs closing all workflow-lint findings; two already-correct CodeQL matrices confirmed unregressed.
</success_criteria>

<output>
Create `.planning/phases/31-org-ci-supply-chain-hardening/31-04-SUMMARY.md` when done
</output>
