---
phase: 26-test-coverage-automation
plan: 03
type: execute
wave: 2
depends_on:
  - 26-01
  - 26-02
files_modified:
  - .planning/phases/26-test-coverage-automation/26-COVERAGE-REPORT.md
autonomous: false
requirements:
  - REQ-1.4.1
  - REQ-1.4.4
user_setup: []

must_haves:
  truths:
    - "Both repo PRs (26-01, 26-02) are open with green CI showing the ratcheted branch gate passing"
    - "A single coverage report records baseline, raised value, and deferred items for both repos"
    - "The ratchet baselines in both ci.yml files are confirmed as no-regression gates"
    - "A human has confirmed the gates fail on regression and pass at baseline"
  artifacts:
    - path: ".planning/phases/26-test-coverage-automation/26-COVERAGE-REPORT.md"
      provides: "Cross-repo branch-coverage baseline, ratchet values, and deferred-item ledger"
      contains: "branch"
  key_links:
    - from: "26-COVERAGE-REPORT.md"
      to: "26-01-SUMMARY.md and 26-02-SUMMARY.md"
      via: "aggregates measured baselines and deferred items"
      pattern: "branch"
---

<objective>
Verify both wave-1 coverage-gate PRs land green, then aggregate the measured baselines,
raised thresholds, and deferred (still-uncovered) branches into one phase coverage report
that documents REQ-1.4.1 (enforced branch gate) and REQ-1.4.4 (retroactive test generation)
outcomes and the explicit ratchet-vs-100% delta.

Purpose: REQ-1.4.1 demands an enforced 100% branch gate; the ratchet strategy in the phase
constraints permits gating at measured-baseline-no-regression when 100% is not reachable this
phase, PROVIDED the delta is recorded as explicit deferred items. This plan produces that
audit-grade record and confirms the gates behave (fail on regression, pass at baseline).

Output: .planning/phases/26-test-coverage-automation/26-COVERAGE-REPORT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/26-CONTEXT.md
@.planning/phases/26-test-coverage-automation/26-01-SUMMARY.md
@.planning/phases/26-test-coverage-automation/26-02-SUMMARY.md

<environment_rules>
This plan writes ONLY to the root repo .planning/ tree (a doc). Do NOT modify the sub-repos.
Verification of the sub-repo PRs is read-only: use `gh pr view` / `gh pr checks` against the
two feat/phase-26-coverage-gates PRs. NEVER merge, NEVER touch branch protection.
</environment_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate both SUMMARYs and verify PR CI status into a coverage report</name>
  <files>.planning/phases/26-test-coverage-automation/26-COVERAGE-REPORT.md</files>
  <read_first>
    - .planning/phases/26-test-coverage-automation/26-01-SUMMARY.md (orchestrator baseline/raised/deferred)
    - .planning/phases/26-test-coverage-automation/26-02-SUMMARY.md (autogen baseline/raised/deferred)
    - .planning/REQUIREMENTS.md (REQ-1.4.1, REQ-1.4.4 wording)
  </read_first>
  <action>
    Confirm both wave-1 PRs exist and CI is green:
    `gh pr list --repo Coding-Autopilot-System/gsd-orchestrator --head feat/phase-26-coverage-gates`
    and the same for `autogen`; then `gh pr checks <pr>` for each to confirm the CI job with the
    branch-coverage gate is passing. Record the PR URLs and check status.
    Write `.planning/phases/26-test-coverage-automation/26-COVERAGE-REPORT.md` with a table per
    repo: measured Task-1 branch baseline, final raised branch value, the enforced gate value
    written to ci.yml (`$Baseline` for orchestrator, `--cov-fail-under` for autogen), metric
    (branch-rate), and the explicit list of still-uncovered branches carried as DEFERRED items.
    Add a "REQ Coverage" section stating: REQ-1.4.1 satisfied as a no-regression branch gate
    (ratchet), with the 100%-branch delta enumerated as deferred; REQ-1.4.4 satisfied by the N
    new tests added across both repos (cite counts from the SUMMARYs). Keep the file ASCII-only.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-test-coverage-automation/26-COVERAGE-REPORT.md && grep -qi "branch" .planning/phases/26-test-coverage-automation/26-COVERAGE-REPORT.md && grep -q "REQ-1.4.1" .planning/phases/26-test-coverage-automation/26-COVERAGE-REPORT.md && grep -q "REQ-1.4.4" .planning/phases/26-test-coverage-automation/26-COVERAGE-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - 26-COVERAGE-REPORT.md exists and is ASCII-only.
    - It contains a per-repo table with columns: measured baseline branch %, raised branch %, enforced gate value, deferred branches.
    - Both PR URLs and their CI check status are recorded.
    - A "REQ Coverage" section maps REQ-1.4.1 and REQ-1.4.4 to concrete evidence (gate value + new-test counts).
  </acceptance_criteria>
  <done>Coverage report written aggregating both repos' baselines, raised thresholds, deferred items, PR status, and REQ mapping.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of branch-coverage gates and deferred-item ledger</name>
  <action>Present the two coverage-gate PRs and 26-COVERAGE-REPORT.md to the operator for verification per the how-to-verify steps below; do not proceed until an explicit approval or issue list is returned.</action>
  <what-built>
    Two branch-coverage CI gates (gsd-orchestrator .NET/coverlet, autogen Python/pytest-cov),
    each ratcheted to a measured no-regression baseline, with new meaningful tests raising the
    number, plus an aggregated 26-COVERAGE-REPORT.md recording baselines and deferred items.
  </what-built>
  <how-to-verify>
    1. Open the two PRs (URLs in 26-COVERAGE-REPORT.md) and confirm the CI branch-coverage gate check is GREEN.
    2. In each PR's ci.yml diff, confirm the gate reads branch coverage (orchestrator: `branch-rate`; autogen: `--cov-branch` + `--cov-fail-under`), NOT line-rate against 100%.
    3. Optional regression proof: locally on one repo's branch, delete a recently-added test and re-run the coverage command; confirm the gate now FAILS below baseline. Restore the test after.
    4. Read 26-COVERAGE-REPORT.md and confirm the deferred (still-uncovered) branches are enumerated explicitly, not hand-waved.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., gate still on line-rate, deferred items missing, a PR check red).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sub-repo CI status -> phase report | The report asserts gates are green; a false "green" (skipped/misconfigured job) would misrepresent phase completion |
| SUMMARY numbers -> report | Baselines/deferred items are copied from wave-1 SUMMARYs; transcription error could hide a gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Spoofing | "Green" CI claim | mitigate | Report must cite `gh pr checks` output per PR; human checkpoint independently opens both PRs and confirms the branch-coverage job (not a skipped/placeholder job) is green |
| T-26-08 | Repudiation | Deferred-item ledger | mitigate | Report enumerates specific uncovered branches from SUMMARYs so the ratchet-vs-100% delta is auditable at milestone v1.4 close (Phase 35), not silently dropped |
| T-26-SC | Tampering | package installs | accept | This plan installs nothing (doc-only in root repo); no package-legitimacy checkpoint required |
</threat_model>

<verification>
- 26-COVERAGE-REPORT.md exists, ASCII-only, references both SUMMARYs.
- Both PR CI branch-coverage checks confirmed green via gh.
- Deferred branches enumerated explicitly.
- Human checkpoint approved.
</verification>

<success_criteria>
- One audit-grade coverage report captures both repos' branch baseline, raised value, enforced gate, and deferred delta.
- REQ-1.4.1 documented as an enforced no-regression branch gate; REQ-1.4.4 documented via new-test counts.
- Both PRs green; nothing merged.
</success_criteria>

<output>
Create `.planning/phases/26-test-coverage-automation/26-03-SUMMARY.md` when done.
</output>
