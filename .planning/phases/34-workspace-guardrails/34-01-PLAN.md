---
phase: 34-workspace-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/workspace-health.ps1
  - tests/Workspace.Health.Tests.ps1
  - .gitignore
  - .refiner/blackboard.snapshot.json
  - GLOBAL_AGENTS.md
autonomous: true
requirements: [REQ-1.4.12]
must_haves:
  truths:
    - "Running workspace-health.ps1 on the current (dirty) tree reports the .refiner/blackboard.json churn is gone because it is gitignored, and reports the WSL-style credential.helper as a finding"
    - "Running workspace-health.ps1 against a synthetic fixture repo with an orphaned untracked file, an unpushed commit, and a stale stack.manifest.json version produces non-zero exit and a finding row for each"
    - "Pester can execute tests/Workspace.Health.Tests.ps1 and it passes against the real (fixed) tree"
    - "A .ps1 file containing a non-ASCII character (e.g. an em-dash or curly quote) is flagged by the sweep"
  artifacts:
    - path: "scripts/workspace-health.ps1"
      provides: "Extended sweep: PR-age via gh, credential.helper normalization check, stack.manifest.json version assertions, non-ASCII .ps1 guard, .refiner/blackboard.json gitignore-awareness"
      contains: "function Add-Finding"
    - path: "tests/Workspace.Health.Tests.ps1"
      provides: "Synthetic red-fixture Pester coverage for workspace-health.ps1"
      contains: "Describe"
    - path: ".gitignore"
      provides: ".refiner/blackboard.json ignored going forward"
      contains: ".refiner/blackboard.json"
    - path: ".refiner/blackboard.snapshot.json"
      provides: "One-time committed snapshot of blackboard state before ignoring live churn"
  key_links:
    - from: "scripts/workspace-health.ps1"
      to: "gh pr list"
      via: "Invoke-CasCommandCapture-style shellout, gated by gh presence"
      pattern: "gh pr list"
    - from: "scripts/workspace-health.ps1"
      to: "stack.manifest.json"
      via: "Get-Content + ConvertFrom-Json version comparison against tool minimumVersion entries"
      pattern: "stack\\.manifest\\.json"
---

<objective>
Extend `scripts/workspace-health.ps1` (do not rewrite — the existing 5 checks: dirty tree, off-default-branch, unpushed, gitlink-no-gitmodules, stale-worktree stay intact) with the six sweep extensions identified in the 2026-07-06 survey, resolve the `.refiner/blackboard.json` continuous-dirty problem at its source, and add Pester coverage with synthetic red fixtures so the sweep's own correctness is provable — this is REQ-1.4.12's falsifier.

Purpose: close the "unbounded drift" failure class (10 unpushed commits, untracked GLOBAL_AGENTS.md, stale worktrees, continuously-dirty refiner state, WSL-style credential helper) with automated, testable detection instead of manual survey.
Output: extended `workspace-health.ps1`, `tests/Workspace.Health.Tests.ps1` with red-fixture coverage, `.refiner/blackboard.json` gitignored with a one-time snapshot, GLOBAL_AGENTS.md prevention note for WSL worktrees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-workspace-guardrails/34-CONTEXT.md
@.planning/REQUIREMENTS.md
@scripts/workspace-health.ps1
@stack.manifest.json
@GLOBAL_AGENTS.md
</context>

<interfaces>
Current `scripts/workspace-health.ps1` structure the executor must preserve and extend (do not restructure the existing 5 checks — append new checks as additional numbered sections in the same `foreach ($repo in Get-Repos $Root)` loop, plus new standalone checks outside the per-repo loop where the check is workspace-wide, e.g. PR-age and the non-ASCII guard):

```powershell
function Add-Finding([string]$Repo, [string]$Check, [string]$Detail) { ... }
function Get-Repos([string]$RootPath) { ... }   # returns @{Name; Path} for root + portfolio/* + gemini-nano
# Checks 1-5 already implemented: dirty, off-default-branch, unpushed/no-upstream, gitlink-no-gitmodules, stale-worktree / worktree-unix-path
if ($Json) { $findings | ConvertTo-Json -Depth 3 } elseif ($findings.Count -gt 0) { ... }
if ($findings.Count -gt 0) { exit 1 }
exit 0
```

`Get-Repos` currently returns `Name`/`Path` pairs only — no remote org/owner. The new PR-age check needs the GitHub `owner/repo` slug per repo; derive it from `git -C $repo.Path remote get-url origin` (parse `github.com[:/]([^/]+)/([^/.]+)` via regex) rather than adding a new field to `Get-Repos`, to avoid touching the function's existing contract used elsewhere in this file.

From `stack.manifest.json` (already read via `ConvertFrom-Json` pattern elsewhere in the codebase, e.g. `doctor.ps1` and `scripts/Cas.Workstation.psm1`):
```
$manifest.tools[].id, .command, .versionArgs, .versionPattern, .minimumVersion
```
Reuse this shape for the new stack.manifest.json version-assertion check: for each tool entry, run `& $tool.command @($tool.versionArgs) 2>$null`, extract via `$tool.versionPattern`, compare against `$tool.minimumVersion` using the same semver-ish comparison already implemented in `scripts/Cas.Workstation.psm1` function `Compare-CasVersion` — import that module (`Import-Module (Join-Path $Root 'scripts\Cas.Workstation.psm1') -Force`) rather than re-implementing version comparison.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the .refiner/blackboard.json continuous-dirty source problem</name>
  <files>.gitignore, .refiner/blackboard.snapshot.json</files>
  <read_first>.gitignore, .refiner/blackboard.json (34309 bytes, tracked, re-dirties every run per CONTEXT.md), git log --oneline -- .refiner/blackboard.json (shows repeated "chore(refiner): update blackboard state" commits)</read_first>
  <action>
Per CONTEXT.md decision ("gitignore + snapshot approach"):
1. Copy the current tracked `.refiner/blackboard.json` to a new committed file `.refiner/blackboard.snapshot.json` (plain file copy, same content, this is the permanent point-in-time record of refiner state at guardrail-introduction time).
2. Add `.refiner/blackboard.json` to `.gitignore` under a new `# Refiner runtime state (re-dirties continuously; see .refiner/blackboard.snapshot.json for last committed state)` comment section — append, do not reorder existing sections.
3. Run `git rm --cached .refiner/blackboard.json` to untrack it going forward (working file stays on disk untouched).
4. Do NOT touch `.universal-refiner.json.bak` — out of scope, not mentioned in CONTEXT.md.
  </action>
  <verify>
    <automated>cd C:\PersonalRepo; git check-ignore -v .refiner/blackboard.json; if ($LASTEXITCODE -ne 0) { exit 1 }; Test-Path .refiner/blackboard.snapshot.json</automated>
  </verify>
  <done>`.refiner/blackboard.json` is gitignored and untracked (git check-ignore succeeds, git ls-files no longer lists it); `.refiner/blackboard.snapshot.json` exists and is tracked with the pre-ignore content.</done>
</task>

<task type="auto">
  <name>Task 2: Extend workspace-health.ps1 with the six sweep extensions</name>
  <files>scripts/workspace-health.ps1</files>
  <read_first>scripts/workspace-health.ps1 (full file, 129 lines — extend, do not rewrite), scripts/Cas.Workstation.psm1 (Compare-CasVersion, Get-CasToolStatus for version-check pattern reuse), stack.manifest.json (tools[] shape)</read_first>
  <action>
Add six new checks to `scripts/workspace-health.ps1`, keeping the existing `Add-Finding`/`Get-Repos`/`$findings` structure and the existing 5 checks untouched. New checks, in order, appended after check 5 (stale-worktree) inside the same per-repo loop unless noted as workspace-wide:

1. **PR-age check (per repo, workspace-wide section after the loop, since it needs `gh` and org context, not local git):** For each repo returned by `Get-Repos`, derive `owner/repo` from `git -C $repo.Path remote get-url origin` (regex `github\.com[:/]([^/]+)/([^/.]+)(\.git)?$`). Skip repos where origin does not match (e.g. no remote). Run `gh pr list --repo "$owner/$name" --state open --json number,createdAt --limit 100 2>$null` (guard with `if (Get-Command gh -ErrorAction SilentlyContinue)` — skip this whole check with no finding if `gh` is absent, do not fail the sweep). For each PR, compute age in days from `createdAt`; if `> 7`, `Add-Finding $repo.Name 'stale-pr' "PR #$number open $ageDays d (threshold 7d)"`. This is REQ-1.4.9's falsifier per CONTEXT.md line 27.
2. **Credential helper WSL-path normalization (per repo, inside the loop):** `$helper = & git -C $repo.Path config --get credential.helper 2>$null`. If `$helper -match '/mnt/[a-z]/'`, `Add-Finding $repo.Name 'credential-helper-wsl-path' "credential.helper='$helper' uses WSL /mnt/ path; run 'git config credential.helper manager' to normalize to Windows Credential Manager"`.
3. **stack.manifest.json version assertions (workspace-wide, root only — run once, not per-repo):** Only applies when `$repo.Name -eq 'root'`. Import `scripts\Cas.Workstation.psm1` via `Import-Module (Join-Path $Root 'scripts\Cas.Workstation.psm1') -Force` and call the existing `Get-CasToolStatus` per tool in `$manifest.tools` (load manifest via `Get-Content (Join-Path $Root 'stack.manifest.json') -Raw | ConvertFrom-Json`). For each tool where `.status` is not `'ok'` (i.e. missing or below `minimumVersion` per the module's own `Compare-CasVersion` logic already used inside `Get-CasToolStatus`), `Add-Finding 'root' 'stack-manifest-version' "tool '$($tool.displayName)' status=$($status.status) (required >= $($tool.minimumVersion))"`.
4. **Non-ASCII `.ps1` guard (workspace-wide, root only, scans all repos' tracked `.ps1` files via root's own working tree plus each sub-repo path since portfolio/* are gitignored from root's perspective but still need scanning):** For each repo from `Get-Repos`, `Get-ChildItem -Path $repo.Path -Filter *.ps1 -Recurse -File -ErrorAction SilentlyContinue | Where-Object { $_.FullName -notmatch '\\node_modules\\|\\bin\\|\\obj\\|\\.git\\' }`. For each file, read raw bytes and check for any byte `> 0x7E` (non-ASCII) — use `[System.IO.File]::ReadAllText($_.FullName)` then `$_.ToCharArray() | Where-Object { [int]$_ -gt 126 }`; if any found, `Add-Finding $repo.Name 'non-ascii-ps1' "$($_.FullName) contains non-ASCII character(s) - PS 5.1 ANSI parsing hazard"` (report only the first offending file per repo to keep output compact, plus a count).
5. **`.refiner/blackboard.json` no longer a dirty-tree false-positive:** No new check needed — Task 1's gitignore entry means the existing check 1 (dirty) naturally stops flagging it. Add a code comment above check 1 noting `.refiner/blackboard.json is gitignored (see Task 34-01/1); this comment documents why it no longer appears as a false-positive dirty finding.`
6. **Housekeeping classification note for `antigravity-export/` and `evidence/`:** These are NOT gitignored and NOT tracked (confirmed empty git status for both — they must already be gitignored or truly empty; verify with `git status --porcelain --ignored antigravity-export evidence` during implementation). Add a check: if either directory exists and is NOT covered by `.gitignore` (`git check-ignore` returns non-zero) and has no tracked files under it, `Add-Finding 'root' 'unclassified-housekeeping-dir' "'$dirName' exists, is untracked and not gitignored - classify as keep (add README + track) or archive/scratch (add to .gitignore)"`. This surfaces the classification decision as a finding rather than the planner making the keep/archive call — CONTEXT.md defers the actual classification to the sweep's report, not to this plan.

Keep all new `Add-Finding` calls using the same `[pscustomobject]@{ Repo; Check; Detail }` shape. Update the script's `.DESCRIPTION` comment block to list all 11 checks (5 original + 6 new).
  </action>
  <verify>
    <automated>pwsh -File scripts\workspace-health.ps1 -Root C:\PersonalRepo; echo "exit=$LASTEXITCODE"</automated>
  </verify>
  <done>Script runs to completion without terminating errors on the real workspace, reports findings for at least the credential-helper-wsl-path check (known-true today per CONTEXT.md survey) and the stale-pr check if any org PR is >7 days old, and exits non-zero while any real finding exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pester tests with synthetic red fixtures + non-ASCII guard test</name>
  <files>tests/Workspace.Health.Tests.ps1</files>
  <behavior>
    - Test 1 (green baseline): a freshly `git init`'d synthetic repo with one commit, clean tree, on its default branch, no upstream configured -> sweep run scoped to that fixture root reports only the expected 'no-upstream' finding (not dirty/off-default-branch/etc.), proving the sweep does not false-positive on a minimal clean repo.
    - Test 2 (orphaned untracked file): same fixture + one untracked file written to disk -> sweep reports a 'dirty' finding referencing that repo.
    - Test 3 (unpushed commit): fixture repo with a fake local `origin` remote (a second bare repo on disk) configured as upstream, then one additional local commit not pushed -> sweep reports an 'unpushed' finding with count 1.
    - Test 4 (lying/stale stack.manifest.json): a synthetic `stack.manifest.json` with a tool entry whose `minimumVersion` is set absurdly high (e.g. "999.0.0") copied to a temp root alongside `scripts/Cas.Workstation.psm1` -> sweep's stack-manifest-version check reports a finding for that tool.
    - Test 5 (non-ASCII .ps1 guard): a synthetic `.ps1` file containing an em-dash (`—`) character written to the fixture repo -> sweep reports a 'non-ascii-ps1' finding for that file.
    - Test 6 (credential-helper WSL path): fixture repo with `git config credential.helper '!"/mnt/c/Program Files/GitHub CLI/gh.exe" auth git-credential'` -> sweep reports 'credential-helper-wsl-path' finding.
    - All fixtures built under `$env:TEMP` with unique GUID-suffixed directories, cleaned up in `AfterAll`/`finally` blocks — never touch the real repo tree.
  </behavior>
  <action>
Write `tests/Workspace.Health.Tests.ps1` as a real Pester 5 `Describe`/`Context`/`It` suite (matching the `Invoke-Pester -Path tests/*.Tests.ps1 -CI` invocation already used in `.github/workflows/ci.yml` — note the two existing test files in `tests/` are custom assert-based scripts, not Pester `Describe` blocks; this new file should use idiomatic Pester `Describe`/`It`/`Should` syntax since it is being added fresh and Pester is already a declared CI dependency).

Structure:
- `BeforeAll`: create a unique temp root (`Join-Path $env:TEMP "wh-test-$([guid]::NewGuid().ToString('N'))"`), `git init` a synthetic repo inside it, configure `user.email`/`user.name` locally (`-c` flags or local config, never touch global config), create one commit.
- Each `It` block builds on a copy of the base fixture (fresh clone/copy per test via `Copy-Item -Recurse` to avoid cross-test contamination) or resets state in a nested `BeforeEach`.
- Invoke the real script under test: `& $PSScriptRoot\..\scripts\workspace-health.ps1 -Root $fixtureRoot -Json | ConvertFrom-Json` and assert on the returned findings array (`Check` property values) with `Should -Contain`.
- `AfterAll`: `Remove-Item -Recurse -Force $tempRoot -ErrorAction SilentlyContinue`.
- Guard every git command used to build fixtures with `-ErrorAction SilentlyContinue` or `2>$null` consistent with the script under test's own `$ErrorActionPreference = 'Continue'` convention.
- File must be strict ASCII only (no em-dashes, smart quotes, or non-ASCII characters anywhere) since this file itself will be scanned by the Task 2 non-ASCII guard.
  </action>
  <verify>
    <automated>pwsh -Command "Install-Module Pester -MinimumVersion 5.0.0 -Force -SkipPublisherCheck -Scope CurrentUser -ErrorAction SilentlyContinue; Invoke-Pester -Path tests/Workspace.Health.Tests.ps1 -CI"</automated>
  </verify>
  <done>All 6 It blocks pass; running `Invoke-Pester -Path tests/Workspace.Health.Tests.ps1 -CI` exits 0 with 6/6 tests passed and zero failed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace-health.ps1 -> gh CLI | Sweep shells out to `gh pr list` against live GitHub org data; untrusted/unavailable network or auth state must not crash the sweep |
| workspace-health.ps1 -> filesystem scan | Non-ASCII guard reads arbitrary `.ps1` file contents across all repos, including ones not authored by this workflow |
| Pester fixtures -> real repo tree | Synthetic red fixtures must never write into or mutate the actual C:\PersonalRepo tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Denial of Service | workspace-health.ps1 gh pr list call | mitigate | Guard with `Get-Command gh -ErrorAction SilentlyContinue`; wrap in try/catch so an auth failure or rate limit produces zero findings for that check rather than terminating the whole sweep (consistent with existing `$ErrorActionPreference = 'Continue'` convention) |
| T-34-02 | Tampering | Synthetic Pester fixtures writing to disk | mitigate | All fixtures built exclusively under `$env:TEMP` with GUID-suffixed unique dirs; cleaned up in AfterAll; never reference `$Root`/`C:\PersonalRepo` as a write target |
| T-34-03 | Information Disclosure | Non-ASCII guard / findings output | accept | Findings only report file paths and check names, not file contents; low sensitivity, sweep output already intended to be visible in CI logs |
| T-34-04 | Repudiation | `.refiner/blackboard.json` untracking loses historical audit trail | mitigate | Snapshot committed once (`.refiner/blackboard.snapshot.json`) before untracking preserves a point-in-time record; full history remains in git log for the file up to the untrack commit |
</threat_model>

<verification>
Run `pwsh -File scripts\workspace-health.ps1 -Root C:\PersonalRepo` and confirm it completes without terminating errors and the `.refiner/blackboard.json` dirty finding is gone (superseded by the gitignore). Run `Invoke-Pester -Path tests/Workspace.Health.Tests.ps1 -CI` and confirm 0 failures.
</verification>

<success_criteria>
- `.refiner/blackboard.json` is gitignored, untracked, and a one-time snapshot is committed.
- `scripts/workspace-health.ps1` implements all 6 sweep extensions from CONTEXT.md alongside the pre-existing 5 checks, without regressing any of them.
- `tests/Workspace.Health.Tests.ps1` exists with Pester `Describe`/`It` coverage for at least: clean baseline, orphaned untracked file, unpushed commit, stale/lying stack.manifest.json version, non-ASCII `.ps1` guard, and WSL-path credential.helper — all passing.
- GLOBAL_AGENTS.md unchanged in this plan (prevention note deferred to 34-02 alongside the WSL-worktree note, per constraint's plan split).
</success_criteria>

<output>
Create `.planning/phases/34-workspace-guardrails/34-01-SUMMARY.md` when done
</output>
