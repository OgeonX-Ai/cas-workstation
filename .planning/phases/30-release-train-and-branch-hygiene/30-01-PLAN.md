---
phase: 30-release-train-and-branch-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [REQ-1.4.9]
must_haves:
  truths:
    - "The org-dotgithub (.github) PR is merged into its default branch"
    - "Branch protection enforce_admins is verified enabled after the merge"
    - "No .github PRs older than 7 days remain open"
  artifacts: []
  key_links:
    - from: "merge-train"
      to: "Coding-Autopilot-System/.github default branch"
      via: "gh pr merge --squash --delete-branch"
      pattern: "gh pr merge .* --squash"
---

<objective>
Land the shared-template PR first. The `.github` (org-dotgithub) repo carries org-wide workflow templates that downstream CI reruns pick up, so per the runbook and CONTEXT locked merge order it MUST merge before any other repo's PR. This is Wave 1 — it runs alone, gating Wave 2.

Purpose: Establish the shared workflow-template baseline and prove the temp-relax enforce_admins procedure works end-to-end (relax → merge → immediately re-enable → verify) before fanning out across 12 repos in Wave 2.
Output: `.github` PR merged, its branch deleted, branch protection verified restored. No file artifacts (git/GitHub operations only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-release-train-and-branch-hygiene/30-CONTEXT.md
@docs/merge-train-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect and merge the org-dotgithub (.github) PR</name>
  <read_first>
    - docs/merge-train-runbook.md (per-repo procedure + safety rules)
    - .planning/phases/30-release-train-and-branch-hygiene/30-CONTEXT.md (locked merge order, self-authored handling)
  </read_first>
  <files>none (GitHub operations only)</files>
  <action>
    Repo is `Coding-Autopilot-System/.github` (org-dotgithub), open PR on branch `chore/governance-hardening` (1 open PR per CONTEXT table).

    1. Resolve the PR number: run `gh pr list --repo Coding-Autopilot-System/.github --state open --json number,headRefName,author,mergeable,reviewDecision`. Capture the number as PR_NUM.
    2. Check CI status: run `gh pr checks $PR_NUM --repo Coding-Autopilot-System/.github`. If any REQUIRED check is failing/red, STOP — do NOT merge, do NOT attempt to fix CI in this phase. Report the red PR as a blocker in the SUMMARY and end the task. Green checks OR no required checks = proceed.
    3. Determine merge path from the PR author/reviewDecision:
       - If the PR is reviewed/approved (reviewDecision APPROVED) or dependabot-authored: normal merge — `gh pr merge $PR_NUM --repo Coding-Autopilot-System/.github --squash --delete-branch`.
       - If self-authored and blocked by branch protection: use the temp-relax procedure exactly, in this order and same sitting:
         a. `gh api -X DELETE "repos/Coding-Autopilot-System/.github/branches/main/protection/enforce_admins"`
         b. `gh pr merge $PR_NUM --repo Coding-Autopilot-System/.github --squash --delete-branch --admin`
         c. `gh api -X POST "repos/Coding-Autopilot-System/.github/branches/main/protection/enforce_admins"` (RE-ENABLE IMMEDIATELY — never leave protection relaxed between steps)
    4. If any gh/git command is denied by a permission classifier, STOP that item and report it as a blocker in the SUMMARY — never work around a denial.

    Note: the .github repo default branch is `main`. Use `main` in the protection API paths above.
  </action>
  <verify>
    <automated>gh pr list --repo Coding-Autopilot-System/.github --state open --json number --jq 'length'</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --repo Coding-Autopilot-System/.github --state open --json number --jq 'length'` returns `0`
    - The merged head branch is deleted (not present in `gh api repos/Coding-Autopilot-System/.github/branches --jq '.[].name'`)
  </acceptance_criteria>
  <done>The single open .github PR is squash-merged, its branch deleted, and zero open PRs remain on the repo. If CI was red, the PR is left open and reported as a blocker instead.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify branch protection restored on .github</name>
  <what-built>The temp-relax enforce_admins procedure was applied (if the PR was self-authored) and re-enabled immediately after merge.</what-built>
  <how-to-verify>
    1. Run: `gh api "repos/Coding-Autopilot-System/.github/branches/main/protection/enforce_admins" --jq .enabled`
    2. Confirm the output is exactly `true`. If it is `false` or errors, branch protection was left relaxed — this is a critical safety violation; re-enable with `gh api -X POST "repos/Coding-Autopilot-System/.github/branches/main/protection/enforce_admins"` and re-check before approving.
    3. Confirm no other repo's protection was touched (this plan only operates on .github).
  </how-to-verify>
  <acceptance_criteria>
    - `gh api "repos/Coding-Autopilot-System/.github/branches/main/protection/enforce_admins" --jq .enabled` returns `true`
  </acceptance_criteria>
  <resume-signal>Type "approved" once enforce_admins reads true, or describe the protection state if it does not.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → GitHub admin API | temp-relax of branch protection crosses into privileged repo-settings mutation |
| PR branch → default branch | untrusted/unreviewed code crosses into main via merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | enforce_admins temp-relax on .github | mitigate | Re-enable enforce_admins in the same sitting (Task 1 step 3c) and verify `.enabled == true` in blocking checkpoint Task 2 before plan completes |
| T-30-02 | Tampering | merging red-CI code into main | mitigate | Task 1 step 2 STOPs on any failing required check; red PRs are reported, never merged or "fixed" |
| T-30-03 | Repudiation | permission-classifier denial worked around | mitigate | Task 1 step 4 mandates STOP-and-report on any denial; no workarounds |
| T-30-SC | Tampering | n/a — no package installs in this phase | accept | Git/GitHub operations only; no npm/pip/cargo install tasks in the release train |
</threat_model>

<verification>
- `gh pr list --repo Coding-Autopilot-System/.github --state open --json number --jq 'length'` = 0
- `gh api "repos/Coding-Autopilot-System/.github/branches/main/protection/enforce_admins" --jq .enabled` = true
</verification>

<success_criteria>
- The org-dotgithub (.github) PR is merged, branch deleted, zero open PRs on the repo.
- Branch protection enforce_admins verified enabled after merge (blocking human checkpoint passed).
- Wave 2 is unblocked (shared templates now live on .github main).
</success_criteria>

<output>
Create `.planning/phases/30-release-train-and-branch-hygiene/30-01-SUMMARY.md` when done. Record: PR number merged, whether temp-relax was used, and the final enforce_admins state.
</output>
