---
phase: 30-release-train-and-branch-hygiene
plan: 03
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified: []
autonomous: true
requirements: [REQ-1.4.9, REQ-1.4.8]
must_haves:
  truths:
    - "Every portfolio repo checkout sits on its default branch, clean, ff-synced with origin"
    - "No local branch is deleted while it still has content absent from its default branch"
    - "Every codex/* worktree branch is pushed to its owning repo's origin before its worktree is removed"
    - "The 3 orphaned worktree dirs are relocated to scratch/, not destroyed"
    - "workspace-health.ps1 shows zero off-default-branch, zero worktree-unix-path, zero stale-worktree findings"
  artifacts:
    - path: ".planning/phases/30-release-train-and-branch-hygiene/30-03-TRAIN-REPORT.md"
      provides: "Final train report: merged PRs, reported blockers, worktree dispositions, sweep before/after"
  key_links:
    - from: "worktree codex/* branches"
      to: "owning repo origin backup branches"
      via: "git push origin <branch>"
      pattern: "push origin (codex|backup)/"
---

<objective>
Wave 3 of the release train: normalize every local checkout to its default branch, triage all
14 worktrees (backup-push unmerged codex/* branches, remove WSL-broken registrations, relocate
orphans), and verify the phase goal end-to-end (REQ-1.4.9, REQ-1.4.8 hygiene portion).

Purpose: after waves 1-2 landed everything on main, make local reality match remote reality and
leave zero silent work-holding artifacts.
Output: all repos on default branch; worktrees/ empty or intentionally kept; a train report; a
markedly smaller workspace-health finding set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-release-train-and-branch-hygiene/30-CONTEXT.md
@.planning/phases/30-release-train-and-branch-hygiene/30-LIVE-STATE.md
@.planning/quick/260706-h8b-workspace-integrity-hardening-batch/worktrees-audit.md

<facts>
- Wave 2 merged each repo's parked branch via PR; ONLY THEN is local branch deletion safe.
- All 14 dirs under C:\PersonalRepo\worktrees\ are registered with WSL /mnt/c/ gitdir paths -
  `git -C <wt>` FAILS on Windows. Use the owning repo's admin dir instead (Task E workaround):
  `git --git-dir=portfolio/<repo>/.git --work-tree=<windows-path> ...` or operate from the
  owning repo (`git -C portfolio/<repo> worktree ...` + `git worktree repair <windows-path>`).
- Worktree ownership map and branch names: see worktrees-audit.md (10 codex/* KEEP rows,
  1 cas-workstation-audit stale row, 3 MANUAL orphans with no git backing).
- Promptimprover local master is 1 ahead with the SAME content as merged PR #26 - reset, don't push.
- Windows-first: never write or register /mnt/ paths; PowerShell files ASCII-only.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize all 13 portfolio checkouts to default branch</name>
  <read_first>30-LIVE-STATE.md (divergence table); wave-2 SUMMARY (which PRs merged per repo)</read_first>
  <files>none (git operations only)</files>
  <action>
    For each repo in portfolio/ (default `main`, Promptimprover `master`):
    1. `git -C portfolio/<repo> fetch origin`
    2. SAFETY GATE per branch before deletion: `git -C portfolio/<repo> diff origin/<default>...<branch> --stat`
       MUST be empty (content landed via wave-2 squash merge). If NOT empty, the wave-2 merge for
       this repo did not land or was partial: KEEP the branch, do not switch state destructively,
       report as blocker in the train report, continue with other repos.
    3. `git -C portfolio/<repo> switch <default> && git -C portfolio/<repo> pull --ff-only`
    4. `git -C portfolio/<repo> branch -D <branch>` only if step-2 gate passed.
    5. Promptimprover ONLY: verify merged content present: `git -C portfolio/Promptimprover cat-file -e origin/master:dashboard/server.js`
       must succeed; then `git -C portfolio/Promptimprover switch master` (already on it) and
       `git -C portfolio/Promptimprover reset --hard origin/master`. If cat-file fails, PR #26 did
       not land - report blocker, do NOT reset.
  </action>
  <acceptance_criteria>
    - For every repo: `git -C portfolio/<repo> branch --show-current` prints the default branch
    - For every repo: `git -C portfolio/<repo> status --porcelain` is empty
    - For every repo: `git -C portfolio/<repo> rev-list --count origin/<default>..HEAD` = 0
    - No branch deleted where the step-2 diff gate was non-empty (blockers listed in train report instead)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Worktree triage - backup-push codex/* branches, remove registrations, relocate orphans</name>
  <read_first>.planning/quick/260706-h8b-workspace-integrity-hardening-batch/worktrees-audit.md (full table); 30-CONTEXT.md worktree notes</read_first>
  <files>none (git operations + directory moves)</files>
  <action>
    A. For each of the 10 KEEP worktrees holding an unmerged, unpushed codex/* branch (owning repo
       and branch per audit table):
       1. Push the branch from the OWNING repo: `git -C portfolio/<owner> push origin <codex-branch>:<codex-branch>`
          (branch objects live in the owner's object store; no need to enter the broken worktree).
       2. Verify: `git -C portfolio/<owner> ls-remote --heads origin <codex-branch>` returns the SHA.
       3. Repair then remove the worktree registration from the owner:
          `git -C portfolio/<owner> worktree repair C:\PersonalRepo\worktrees\<name>` (fixes /mnt/ path),
          then `git -C portfolio/<owner> worktree remove C:\PersonalRepo\worktrees\<name>` (NO --force
          unless status is clean-verified), then `git -C portfolio/<owner> worktree prune`.
          If remove refuses due to dirty tree: leave it, record in train report (never force a dirty tree).
    B. cas-workstation-audit (owner cas-workstation, branch audit/enterprise-hardening-20260611, clean, stale):
       same push-backup + repair + remove + prune procedure.
    C. The 3 orphans with no git backing (archive-loop-engineering, loop-engineering, v1.1-sdlc-engine):
       `Move-Item C:\PersonalRepo\worktrees\<name> C:\PersonalRepo\scratch\orphaned-worktrees\<name>`
       (scratch/ is gitignored). Do NOT delete.
    D. Record every disposition (pushed SHA / removed / left-dirty / relocated) in the train report.
  </action>
  <acceptance_criteria>
    - For each of the 11 pushed branches: `git -C portfolio/<owner> ls-remote --heads origin <branch>` non-empty BEFORE its worktree removal
    - `git -C portfolio/<owner> worktree list` no longer lists removed worktrees for each owner
    - C:\PersonalRepo\worktrees\ contains only entries explicitly recorded as left-dirty (target: empty)
    - C:\PersonalRepo\scratch\orphaned-worktrees\ contains the 3 orphan dirs
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification and train report</name>
  <read_first>scripts/workspace-health.ps1; 30-LIVE-STATE.md</read_first>
  <files>.planning/phases/30-release-train-and-branch-hygiene/30-03-TRAIN-REPORT.md</files>
  <action>
    1. Org PR check: for all 13 repos run
       `gh pr list --repo Coding-Autopilot-System/<name> --state open --json number,createdAt`.
       Expected: only the RED-reported PRs remain (autogen#6, cas-contracts#13, gsd-orchestrator#10),
       all younger than 7 days (REQ-1.4.9).
    2. Sweep: run `powershell -NoProfile -File C:\PersonalRepo\scripts\workspace-health.ps1 -Json`
       and compare against the pre-train baseline (29 findings): expect 0 off-default-branch,
       0 worktree-unix-path, 0 stale-worktree, 0 unpushed (root may show dirty from live
       .refiner/blackboard.json only).
    3. Write 30-03-TRAIN-REPORT.md: PRs merged (numbers + merge SHAs), PRs reported RED, protection
       relax/restore log (every repo where enforce_admins was touched, with final .enabled=true
       verification output), branch deletions, worktree dispositions, sweep before/after table,
       and any blockers.
    4. Do not commit - the orchestrator commits phase docs.
  </action>
  <acceptance_criteria>
    - 30-03-TRAIN-REPORT.md exists and contains sections: Merged, Reported RED, Protection Log, Worktrees, Sweep Before/After, Blockers
    - Recorded gh output shows open-PR set == the 3 RED PRs (or fewer), none older than 7 days
    - Recorded sweep output shows 0 findings in categories: off-default-branch, worktree-unix-path, stale-worktree, unpushed
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
- T1: Deleting a local branch whose content never landed (silent work loss) -> mitigated by the
  empty-diff gate in Task 1 step 2; blockers reported instead of deleted.
- T2: Removing a worktree holding an unpushed branch (work loss) -> mitigated by push-backup +
  ls-remote verification BEFORE removal; dirty trees are never force-removed.
- T3: reset --hard on Promptimprover master erasing unmerged content -> mitigated by cat-file
  presence check of the merged file before reset.
- T4: Destroying the 3 orphan dirs -> mitigated by relocation to scratch/ instead of deletion.
</threat_model>

<verification>
Phase-goal-backward: REQ-1.4.9 falsifier = step 1 of Task 3 (gh pr list across org).
REQ-1.4.8 hygiene falsifier = step 2 of Task 3 (sweep categories at zero).
</verification>
