---
phase: 40-pilot-cadence
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - scripts/register-pilot-cadence-task.ps1
autonomous: true
requirements: [REQ-1.5.4]
must_haves:
  truths:
    - "scripts/register-pilot-cadence-task.ps1, run twice, results in exactly one Windows Scheduled Task named CAS-PilotCadence, registered to run scripts/run-pilot-cadence.ps1 weekly"
    - "A deliberately seeded failing test on a throwaway, never-pushed gsd-orchestrator worktree commit causes scripts/run-pilot-cadence.ps1 to report a red gsd-orchestrator-fault-injection suite, and scripts/file-pilot-regression-issue.ps1 to file exactly one open issue on Coding-Autopilot-System/gsd-orchestrator titled 'pilot-cadence: gsd-orchestrator-fault-injection regression'"
    - "After the drill, the throwaway worktree and its unreferenced commit are removed, the drill's scratch evidence output is deleted, and the filed issue is closed with an explanatory comment, leaving no residue in the real evidence/pilot-cadence/ history or in the scheduled worktree"
  artifacts:
    - path: "scripts/register-pilot-cadence-task.ps1"
      provides: "Idempotent weekly Windows Task Scheduler registration for CAS-PilotCadence, invoking scripts/run-pilot-cadence.ps1"
      contains: "Get-ScheduledTask"
  key_links:
    - from: "scripts/register-pilot-cadence-task.ps1"
      to: "scripts/run-pilot-cadence.ps1"
      via: "Register-ScheduledTask action invoking powershell -File scripts\\run-pilot-cadence.ps1 -Root $Root"
      pattern: "Register-ScheduledTask"
---

<objective>
Register the weekly local Task Scheduler entry that runs the Plan 01 pilot-cadence runner unattended, then prove the regression-to-issue pipeline end-to-end with a self-cleaning falsifier drill: seed a deliberate, never-pushed test failure on a throwaway gsd-orchestrator worktree commit, confirm exactly one GitHub issue gets filed, then remove all drill residue (REQ-1.5.4).

Purpose: close REQ-1.5.4's two falsifiers -- a live weekly schedule producing evidence, and proof that a real regression actually produces a filed issue within one cycle -- without leaving synthetic failure data in the real evidence history or noise in a real repo's issue tracker.
Output: `scripts/register-pilot-cadence-task.ps1`, a live `CAS-PilotCadence` Windows Scheduled Task, and drill evidence (captured in the plan SUMMARY) proving the filed-then-closed issue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@scripts/register-workspace-health-task.ps1
@scripts/run-pilot-cadence.ps1
@scripts/file-pilot-regression-issue.ps1
</context>

<interfaces>
From scripts/register-workspace-health-task.ps1 (the proven idempotent pattern to mirror exactly, only changing the trigger cadence and target script): CmdletBinding with -TaskName, -Root, -At params; builds a New-ScheduledTaskAction invoking powershell.exe -NoProfile -WindowStyle Hidden -File "<script>" -Root "<root>" with -WorkingDirectory $Root; a New-ScheduledTaskTrigger for the schedule; a New-ScheduledTaskSettingsSet with -StartWhenAvailable -DontStopIfGoingOnBatteries -ExecutionTimeLimit; then checks Get-ScheduledTask -TaskName $TaskName -ErrorAction SilentlyContinue -- if found, Set-ScheduledTask to update in place and print an "Updated existing scheduled task" message; if not found, Register-ScheduledTask and print a "Registered scheduled task" message; exits 0.

From scripts/run-pilot-cadence.ps1 (Plan 01, already created): CmdletBinding params -Root, -EvidenceRoot, -LogRoot, -OnlySuites (string array), -GsdOrchestratorRef, -AutogenRef, -NoCommit (switch), -NoIssueFile (switch). Accepts any git-resolvable ref string for -GsdOrchestratorRef/-AutogenRef, including a raw commit SHA -- the internal Sync-ReadOnlyWorktree helper fetches that ref from the source repo and checks the dedicated worktree out detached at FETCH_HEAD, so pointing it at a throwaway, never-pushed commit works exactly like pointing it at origin/main.

From scripts/file-pilot-regression-issue.ps1 (Plan 01, already created): produces a stable issue title "pilot-cadence: <suite-id> regression" with no date component, used here to search for and confirm the drill's filed issue.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Weekly scheduler registration</name>
  <files>scripts/register-pilot-cadence-task.ps1</files>
  <action>
Create scripts/register-pilot-cadence-task.ps1, ASCII-only, modeled directly on scripts/register-workspace-health-task.ps1's structure (CmdletBinding params, action/trigger/settings construction, Get-ScheduledTask idempotency check, Set-ScheduledTask-to-update-in-place vs Register-ScheduledTask-if-absent branching, exit 0). Params: -TaskName (default 'CAS-PilotCadence'), -Root (default 'C:\PersonalRepo'), -DayOfWeek (default 'Sunday'), -At (default '09:00', deliberately after the existing daily workspace-health sweep's 08:00 slot to avoid resource contention).

Action: `powershell.exe -NoProfile -WindowStyle Hidden -File "$Root\scripts\run-pilot-cadence.ps1" -Root "$Root"` with -WorkingDirectory $Root (default params on the invoked script mean it runs all three suites, commits evidence via PR, and files issues on red suites -- exactly the unattended weekly behavior this phase requires). Trigger: New-ScheduledTaskTrigger -Weekly -DaysOfWeek $DayOfWeek -At $At. Settings: New-ScheduledTaskSettingsSet -StartWhenAvailable -DontStopIfGoingOnBatteries -ExecutionTimeLimit (New-TimeSpan -Hours 2) (fault-injection suites plus two dotnet/pytest restores can run long on a cold worktree, so allow more headroom than the sweep's 30-minute limit). Description: 'CAS pilot-cadence weekly evidence run (Phase 40, REQ-1.5.4) -- pilot scenarios + fault-injection suites, evidence via PR, regressions auto-file issues.'

Then actually run this script for real (not just create it) so the live scheduled task exists on this machine after the plan completes.
  </action>
  <verify>
    <automated>powershell -NoProfile -File scripts\register-pilot-cadence-task.ps1; powershell -NoProfile -File scripts\register-pilot-cadence-task.ps1; (Get-ScheduledTask -TaskName 'CAS-PilotCadence' -ErrorAction SilentlyContinue | Measure-Object).Count</automated>
  </verify>
  <acceptance_criteria>
    - Running the registration script twice produces exactly one 'CAS-PilotCadence' scheduled task (count = 1), second run prints the "Updated existing" message (idempotency proven live, mirroring register-workspace-health-task.ps1).
    - Task trigger is Weekly Sunday 09:00 with 2h execution limit; action invokes run-pilot-cadence.ps1 with -Root.
    - Script is ASCII-only (LC_ALL=C.UTF-8 grep -cP '[^\x00-\x7F]' = 0).
  </acceptance_criteria>
  <done>Idempotent weekly scheduler script committed AND the live task registered on this machine (Get-ScheduledTask shows Ready).</done>
</task>

<task type="auto">
  <name>Task 2: Seeded-regression falsifier drill (REQ-1.5.4 second falsifier), end-to-end with cleanup</name>
  <files>.planning/phases/40-pilot-cadence/40-drill-evidence.md</files>
  <read_first>
    - scripts/run-pilot-cadence.ps1 (the -GsdOrchestratorRef / -NoCommit switches used by this drill; Sync-ReadOnlyWorktree accepts any git-resolvable ref incl. a never-pushed local SHA)
    - scripts/file-pilot-regression-issue.ps1 (stable title convention "pilot-cadence: <suite-id> regression" and open-issue dedupe used to find and verify the filed issue)
  </read_first>
  <action>
    Prove the regression path end-to-end WITHOUT touching any repo's real branches:
    1. In a THROWAWAY local worktree of portfolio/gsd-orchestrator (created from origin/main at a temp path under %TEMP%, never pushed), seed a deliberately failing fault-injection test: append a test method asserting 1 -eq 2 into a new file src/GsdOrchestrator.Tests/DrillSeededFailure.cs and commit LOCALLY (message "drill(40-02): seeded failing test - never push"). Record the local commit SHA.
    2. Run `powershell -NoProfile -File scripts\run-pilot-cadence.ps1 -OnlySuites gsd-orchestrator-fault-injection -GsdOrchestratorRef <seeded-SHA> -NoCommit` - the suite must go RED and the runner must invoke file-pilot-regression-issue.ps1, filing EXACTLY ONE open issue titled "pilot-cadence: gsd-orchestrator-fault-injection regression" on Coding-Autopilot-System/gsd-orchestrator.
    3. Verify with `gh issue list --repo Coding-Autopilot-System/gsd-orchestrator --state open --search "pilot-cadence"` that exactly one matching issue exists; re-run step 2 once more and verify the dedupe path COMMENTS on the existing issue instead of filing a second one (issue count still 1).
    4. Cleanup: close the drill issue with a comment linking this plan ("drill verification - closing"); remove the throwaway worktree and prune (git worktree remove + prune from portfolio/gsd-orchestrator); verify the seeded SHA is unreachable from any real branch.
    5. Write 40-drill-evidence.md: seeded SHA, red-suite output excerpt, issue URL + dedupe evidence (comment link), cleanup confirmation (worktree list, issue state=closed). Commit via the root repo PR flow (branch feat/phase-40-pilot-cadence, same PR as Plan 01 artifacts if still open).
  </action>
  <verify>
    <automated>test -f .planning/phases/40-pilot-cadence/40-drill-evidence.md; grep -q "state.*closed" .planning/phases/40-pilot-cadence/40-drill-evidence.md</automated>
  </verify>
  <acceptance_criteria>
    - Evidence doc shows: red suite from the seeded SHA; exactly one issue filed with the stable title; second run deduped to a comment (no second issue); issue closed post-drill; throwaway worktree removed and SHA unreachable.
    - No push of the seeded commit anywhere; no real branch of gsd-orchestrator modified (git log origin/main unchanged).
  </acceptance_criteria>
  <done>REQ-1.5.4's "seeded regression auto-files an issue within one cycle" falsifier proven with committed evidence, and the drill fully cleaned up.</done>
</task>

</tasks>

<threat_model>
- T-40-05 Drill seeds escape to a real branch -> mitigated: throwaway worktree under %TEMP%, local-only commit, explicit unreachability check in cleanup, -NoCommit run.
- T-40-06 Drill issue spams the repo -> mitigated: stable-title dedupe verified as part of the drill itself; issue closed in cleanup.
- T-40-07 Scheduler drift/duplicates -> mitigated: idempotency proven by double-run in Task 1 verify.
</threat_model>

<verification>
Phase-goal-backward: REQ-1.5.4 falsifier 1 (weekly evidence) = live scheduled task (Task 1) + Plan 01's runner; falsifier 2 (seeded regression -> issue within one cycle) = Task 2's drill evidence doc.
</verification>

<success_criteria>
- [ ] CAS-PilotCadence task registered, idempotent, Ready.
- [ ] Drill evidence committed: red suite -> exactly one issue -> dedupe -> cleanup, zero contamination of real branches.
</success_criteria>

<output>
Summary at .planning/phases/40-pilot-cadence/40-02-SUMMARY.md per the standard template.
</output>
