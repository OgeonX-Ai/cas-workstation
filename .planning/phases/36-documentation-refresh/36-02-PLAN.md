---
phase: 36-documentation-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/cloud-security-service-model/README.md
  - portfolio/cas-evals/README.md
  - portfolio/cas-contracts/README.md
  - portfolio/cas-platform/README.md
autonomous: true
requirements:
  - "Phase 36 CONTEXT decisions D-01, D-02, D-03, D-04, D-05, D-06"
user_setup: []

must_haves:
  truths:
    - "cas-contracts README does not assert the dead schemas.coding-autopilot.dev $id as resolved fact while PR #18 (registry $id rewrite) is still open"
    - "cas-platform README documents Phase 33 bicep linting (.bicepconfig.json, pinned API versions) with correct landed-vs-open-PR framing"
    - "cloud-security-service-model README notes any Phase 33 P4 DoNotEnforce policy decision if that repo's docs reference policy enforcement mode"
    - "cas-evals README carries a freshness footer and accurately describes its CI gate (CLI exit-code based, not a coverage badge since no formal coverage gate exists there)"
    - "No repo in this plan gets a fake coverage badge — D-06 requires coverage badges only where a real gate exists"
  artifacts:
    - path: "portfolio/cas-contracts/README.md"
      provides: "Corrected registry $id framing (in-progress PR note) and freshness footer"
      contains: "docs-verified:"
    - path: "portfolio/cas-platform/README.md"
      provides: "Phase 33 bicep-lint section and freshness footer"
      contains: "docs-verified:"
    - path: "portfolio/cloud-security-service-model/README.md"
      provides: "Freshness footer and any P4 policy-mode note"
      contains: "docs-verified:"
    - path: "portfolio/cas-evals/README.md"
      provides: "Freshness footer, no fake coverage badge"
      contains: "docs-verified:"
  key_links:
    - from: "portfolio/cas-contracts/README.md"
      to: "cas-contracts PR #18 (fix/registry-resolvable-id)"
      via: "prose note distinguishing landed vs in-progress registry fix"
      pattern: "registry|\\$id"
    - from: "portfolio/cas-platform/README.md"
      to: "portfolio/cas-platform/.bicepconfig.json"
      via: "prose description of bicep lint gate"
      pattern: "bicepconfig|lint"
---

<objective>
Refresh the platform/governance/evidence cluster: cloud-security-service-model, cas-evals,
cas-contracts, cas-platform. The highest-risk item in this cluster is cas-contracts, whose
README currently states the schema `$id` registry as authoritative/resolvable while an open
PR (#18, `fix/registry-resolvable-id`) is actively rewriting that exact claim — this is the
canonical "plausible but false" case the b4e0868 lesson targets.

Purpose: prevent documentation from asserting unmerged Phase 32/33 work as landed, add the
mandatory freshness footer across all four, and keep D-06's no-fake-badges rule intact.

Output: Four updated README.md files, each carrying a verified freshness footer, with
zero-BLOCKER gsd-doc-verifier passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-documentation-refresh/36-CONTEXT.md
@.planning/ROADMAP.md

<repo_facts>
<!-- Verified 2026-07-06 via git/gh. Re-verify PR state at execution time -- these
     repos are actively receiving Phase 31/32/33 PRs. -->

cas-contracts:
  - default branch: main
  - Local checkout is on fix/registry-resolvable-id; origin has an OPEN PR #18
    "fix(registry): rewrite schema $id to the resolvable Pages registry URL"
    (per ROADMAP Phase 32: rewrites all 22 schema $id values from the dead
    schemas.coding-autopilot.dev to the live Pages registry URL).
  - Current README.md (as of context-gathering) states: "Released schemas are
    discoverable from https://coding-autopilot-system.github.io/cas-contracts/registry/index.json"
    AND separately "Authoritative schema identity remains under
    https://schemas.coding-autopilot.dev/; the GitHub Pages registry is the current
    live distribution endpoint." This second sentence is exactly the claim PR #18
    is fixing (schemas.coding-autopilot.dev has cname:null and 404s per ROADMAP
    Phase 32 notes). Do NOT silently keep or silently rewrite this without checking
    PR #18's actual merge state first via `gh pr view 18 --repo Coding-Autopilot-System/cas-contracts`.
    If PR #18 is still open: rephrase to state the Pages registry URL is authoritative
    NOW, and note the schemas.coding-autopilot.dev domain migration is in progress
    (PR #18) rather than asserting either the old or new claim as settled fact.
    If PR #18 has merged: describe the Pages URL as the sole authoritative $id per
    the merged fix, removing the old domain claim entirely.
  - CONTRIBUTING.md exists (31 lines).

cas-platform:
  - default branch: main
  - Local checkout on fix/bicep-lint-api-version-pinning; origin has OPEN PR #11
    "fix: enable use-recent-api-versions bicep lint rule" (Phase 33 P2).
  - Current README.md has no mention of `.bicepconfig.json` linting or the P4
    DoNotEnforce policy-assignment decision (that decision lives in
    cloud-security-service-model per 33-CONTEXT.md, not this repo — verify which
    repo actually owns the DoNotEnforce policy assignment before attributing it).
  - Add a short section describing `.bicepconfig.json` lint rules and API-version
    pinning, framed as in-progress (PR #11) unless verified merged.
  - CONTRIBUTING.md exists (13 lines) — thin, leave as-is.

cloud-security-service-model:
  - default branch: main
  - Local checkout on fix/bicep-lint-api-version-pinning; origin has OPEN PR #13
    "fix(bicep): pin API versions and record DoNotEnforce ADR (Phase 33 P2/P4)".
  - This repo is docs-only (no application code) per its own CLAUDE.md, but the
    Phase 33 PR title suggests a DoNotEnforce ADR record lives here (likely under
    `impl/` or `docs/`). Verify what P4 actually touches in THIS repo (grep for
    DoNotEnforce, ADR files) before writing a claim about it in the README.
  - Add a one-line pointer to the P4 policy-mode decision (or its tracking PR) in
    the "Quick navigation" or "ISO 27001 / CISSP mapping" section, without fabricating
    ADR content that isn't actually in the tree yet if the PR is unmerged.

cas-evals:
  - default branch: main
  - Local checkout on feat/registry-fetch-smoke-check with an untracked
    tests/test_registry_check.py — this is Phase 32-02 work (consumer registry-fetch
    CI job), likely not yet on origin/main. Verify before describing the registry-fetch
    smoke check as a shipped feature; if unmerged, phrase as in-progress.
  - README's CI gate description is CLI-exit-code based (python -m cas_evals.release
    --check exits non-zero on mandatory metric failure) — this is accurate and is NOT
    a coverage badge; do not add a coverage badge here per D-06 (no real coverage gate
    exists in this repo based on the skim).
</repo_facts>

<freshness_footer_spec>
Every refreshed README ends with a footer line, on its own line, after all other content:

  <!-- docs-verified: <repo-head-sha> <date> -->

`<repo-head-sha>` = the full 40-char SHA of the target repo's current default-branch HEAD
(via `git -C portfolio/<repo> rev-parse origin/main`). `<date>` = ISO date of the writing
session.
</freshness_footer_spec>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cas-contracts registry $id framing and refresh cas-platform bicep-lint section</name>
  <read_first>
    - portfolio/cas-contracts/README.md
    - portfolio/cas-contracts/docs/DISTRIBUTION.md
    - portfolio/cas-platform/README.md
    - portfolio/cas-platform/.bicepconfig.json (if present on origin/main)
  </read_first>
  <action>
    For cas-contracts: check PR #18's actual state via `gh pr view 18 --repo
    Coding-Autopilot-System/cas-contracts --json state,mergedAt`. Rewrite the "Adoption"
    section's two sentences about schema identity per the branching logic in
    `<repo_facts>` above — the goal is that the README never asserts a 404ing domain as
    "authoritative" without qualification, and never claims an unmerged PR's fix as
    already shipped. Preserve the rest of the README (Quickstart, Repository Layout,
    Status) unchanged.
    For cas-platform: check PR #11's actual state the same way. Add a "Validation and
    Linting" section (or extend "Validate locally") describing `.bicepconfig.json`-driven
    lint rules and pinned API versions, phrased per the verified PR state. Preserve all
    existing accurate content (v0.1 foundation, Reference product configuration, Security
    sections).
    Append the freshness footer to both files using each repo's real origin/main HEAD sha.
  </action>
  <verify>
    <automated>grep -c "docs-verified:" portfolio/cas-contracts/README.md portfolio/cas-platform/README.md</automated>
  </verify>
  <acceptance_criteria>
    - cas-contracts README's schema-identity claim matches the ACTUAL verified state of PR #18 (open vs merged) — no contradiction between two sentences claiming different authoritative URLs.
    - cas-platform README has a bicep-lint section reflecting the ACTUAL verified state of PR #11.
    - Both files end with a correct freshness footer.
  </acceptance_criteria>
  <done>cas-contracts no longer asserts a dead-domain schema $id as settled fact without qualification; cas-platform documents its bicep-lint gate accurately.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh cloud-security-service-model and cas-evals READMEs</name>
  <read_first>
    - portfolio/cloud-security-service-model/README.md
    - portfolio/cas-evals/README.md
  </read_first>
  <action>
    For cloud-security-service-model: grep the repo tree for "DoNotEnforce" and any ADR
    files related to Phase 33 P4 before writing anything about it. If found on the current
    tree, add a one-line pointer in "Quick navigation" to the policy-mode decision file. If
    not found on main (still only in the open PR #13), skip adding this pointer entirely
    rather than fabricating a link to content not yet present — do not guess a docs/ path.
    Append the freshness footer.
    For cas-evals: verify whether tests/test_registry_check.py / the registry-fetch smoke
    check has landed on origin/main via `gh pr view` for the relevant open PR (or checking
    origin/main tree directly). If unmerged, do not add any new feature section for it in
    this pass (that work belongs to whichever plan or a future refresh once merged) — the
    goal here is only the freshness footer plus confirming no fake coverage badge exists.
    Append the freshness footer.
  </action>
  <verify>
    <automated>grep -c "docs-verified:" portfolio/cloud-security-service-model/README.md portfolio/cas-evals/README.md