---
phase: 36-documentation-refresh
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "<per-repo> docs/wiki/Home.md, Architecture.md, Operations.md, Decisions.md"
  - "org-dotgithub profile/README.md + profile/VISION.md"
  - docs/VISION.md (root - already created by orchestrator)
autonomous: true
requirements: [Phase-36-CONTEXT decisions 7-10]
must_haves:
  truths:
    - "Each covered repo has docs/wiki/{Home,Architecture,Operations,Decisions}.md with verified claims only"
    - "Every Architecture.md contains at least one valid Mermaid diagram AND a codex:generate-image placeholder comment"
    - "org profile README links the VISION page and renders its top-level Mermaid correctly on GitHub"
    - "No GitHub native wiki tabs are used - docs-as-code only"
  artifacts:
    - path: "docs/VISION.md"
      provides: "Flagship shareable vision page (root) - SOURCE OF TRUTH for tone, framing, placeholder format"
---

<objective>
Give every portfolio repo an enterprise-grade docs-as-code wiki and publish the shareable
vision narrative (governed autonomy; the governance-with-speed thesis) to the org profile.
Root docs/VISION.md (already written) is the canonical source for tone, the
`codex:generate-image` placeholder format, and the Mermaid house style.
</objective>

<context>
@.planning/phases/36-documentation-refresh/36-CONTEXT.md (decisions 1-10; enterprise expansion)
@docs/VISION.md (flagship - reuse its diagrams/framing, adapted per repo)
@.planning/phases/36-documentation-refresh/36-01-PLAN.md and 36-02-PLAN.md (README refresh plans - wiki work must stay consistent with them)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiki trees for control/execution/governance core repos</name>
  <files>gsd-orchestrator, autogen, Promptimprover, cas-contracts, cas-evals - docs/wiki/*</files>
  <action>
    Per repo, branch docs/phase-36-refresh from origin/<default> (reuse if it exists from 36-01/02
    execution), add docs/wiki/: Home.md (role in the three-plane model, quickstart links),
    Architecture.md (repo-specific Mermaid: for gsd-orchestrator the goal state machine incl. the
    Phase 28 typed-failure retry/halt path; for autogen the worker fan-out + telemetry boundary;
    for Promptimprover the governance/blackboard flow; for cas-contracts the schema/registry
    publishing pipeline; for cas-evals the evidence-gate flow), Operations.md (run/test/coverage
    commands VERIFIED by executing them or reading CI), Decisions.md (index of real ADRs/summaries
    in that repo). Each Architecture.md: Mermaid first, then a codex:generate-image placeholder
    exactly in docs/VISION.md's format. Freshness footer per 36-CONTEXT decision 3. PR-only.
  </action>
  <read_first>docs/VISION.md; each repo's README + CI workflows + recent SUMMARYs</read_first>
  <acceptance_criteria>4 files per repo; mermaid block parses (mmdc or GitHub preview via PR diff); placeholder comment present; no claim contradicts the live tree; PR opened per repo</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wiki trees for autopilot/platform/support repos</name>
  <files>autopilot-core, autopilot-demo, ci-autopilot, cas-platform, cas-reference-product, cloud-security-service-model, cas-workstation - docs/wiki/*</files>
  <action>Same structure as Task 1, adapted: cas-platform/cloud-security Architecture.md must state the NO-AZURE deploy lock + bicep-ready posture explicitly; ci-autopilot documents the self-hosted runner + fixer loop; cas-workstation documents the workstation contract + workspace-health sweep. PR-only per repo.</action>
  <read_first>docs/VISION.md; per-repo README/CI; .planning/phases/33-azure-infra-hardening/*SUMMARY*; scripts/workspace-health.ps1 synopsis</read_first>
  <acceptance_criteria>4 files per repo; deploy-lock stated where applicable; PRs opened</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Org profile vision hub + agent-hierarchy page</name>
  <files>org-dotgithub profile/README.md, profile/VISION.md, docs/wiki/Agent-Hierarchy.md (root repo)</files>
  <action>
    org-dotgithub (PR): profile/VISION.md = adapted copy of root docs/VISION.md (org-audience
    edit, working links to all 13 repos); profile/README.md = short front-door with the three-plane
    Mermaid + link to VISION + repo map table.
    Root repo (direct commit): docs/wiki/Agent-Hierarchy.md documenting the CAS agent modularity
    model per 36-CONTEXT decision 10: orchestrator -> typed subagents (planner/executor/verifier/
    critic/doc-writer), model tiers (frontier/mid/small) with the cost rationale, which agent types
    can spawn sub-agents (catch-all agents with full toolsets) vs which deliberately cannot
    (specialized GSD agents - delegation depth as a governance decision), and the evidence trail
    each spawn leaves (.planning summaries).
  </action>
  <read_first>docs/VISION.md; engineering-os/models/claude.json; GLOBAL_AGENTS.md</read_first>
  <acceptance_criteria>profile renders on github.com/Coding-Autopilot-System (verify via PR preview); Agent-Hierarchy.md committed at root; all repo links resolve</acceptance_criteria>
</task>

</tasks>

<threat_model>
- T1 plausible-but-false doc claims -> every Operations.md command executed or CI-verified before writing; gsd-doc-verifier pass per 36-CONTEXT decision 2.
- T2 broken Mermaid on GitHub -> keep to flowchart/sequence subset GitHub renders; verify in PR preview.
- T3 conflict with 36-01/02 README branches -> reuse the same docs/phase-36-refresh branch per repo.
</threat_model>
