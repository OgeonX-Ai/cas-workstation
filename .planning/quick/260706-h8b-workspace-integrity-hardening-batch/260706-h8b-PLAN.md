---
phase: quick-260706-h8b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Task A — portfolio/gsd-orchestrator (own repo)
  - portfolio/gsd-orchestrator/.gitignore
  - portfolio/gsd-orchestrator/tests/**
  - portfolio/gsd-orchestrator/.github/workflows/ci.yml
  # Task B — portfolio/autogen (own repo)
  - portfolio/autogen/.gitignore
  - portfolio/autogen/tests/test_autogen.py
  - portfolio/autogen/autogen_dashboard/app.py
  - portfolio/autogen/.github/workflows/ci.yml
  # Task C — portfolio/Promptimprover (own repo)
  - portfolio/Promptimprover/dashboard/**
  - portfolio/Promptimprover/package.json
  - portfolio/Promptimprover/run-dashboard.ps1
  # Task D — gemini-nano (own repo)
  - gemini-nano/.gitignore
  # Task E — worktrees audit (report only)
  - .planning/quick/260706-h8b-workspace-integrity-hardening-batch/worktrees-audit.md
  # Task F — root repo (C:\PersonalRepo)
  - .gitattributes
  - .gitmodules
  - .github/workflows/ci.yml
  - .gitignore
  - GLOBAL_AGENTS.md
autonomous: true
requirements: [W1, W2, W3, W5, H1, H2, H3, H4, H5, H6]

must_haves:
  truths:
    - "gsd-orchestrator test suite is committed and pushed to its remote (survives disk failure)"
    - "autogen test suite is committed and pushed to its remote"
    - "Promptimprover dashboard/ tooling is committed and pushed"
    - "gemini-nano .refiner/ dirs are gitignored; repo clean and pushed if reachable"
    - "worktrees audit report exists listing every worktree with merge/clean status and prune decisions"
    - "Root repo has .gitattributes, .gitmodules for gemini-nano, and a Pester CI workflow"
    - "GLOBAL_AGENTS.md Workspace Layout matches the 14-repo reality in CLAUDE.md"
    - "Root clutter relocated into gitignored scratch/; rules.json moved out (never committed)"
    - "All untracked/modified root content committed in truthful atomic commits and master pushed to origin"
  artifacts:
    - path: "portfolio/gsd-orchestrator/tests"
      provides: "gsd-orchestrator test suite tracked in git"
    - path: "portfolio/autogen/tests/test_autogen.py"
      provides: "autogen test suite tracked in git"
    - path: ".planning/quick/260706-h8b-workspace-integrity-hardening-batch/worktrees-audit.md"
      provides: "worktrees audit report"
    - path: ".gitattributes"
      provides: "line-ending normalization at root"
    - path: ".gitmodules"
      provides: "gemini-nano submodule URL for fresh clones"
    - path: ".github/workflows/ci.yml"
      provides: "root Pester CI workflow"
  key_links:
    - from: ".gitmodules"
      to: "gemini-nano gitlink (160000)"
      via: "submodule path + url"
      pattern: "path = gemini-nano"
    - from: "GLOBAL_AGENTS.md"
      to: "CLAUDE.md context chain"
      via: "workspace layout parity"
      pattern: "cas-evals|cas-contracts|cas-platform|autopilot-core"
---

<objective>
Workspace-integrity hardening batch. Findings W1-W5 and H1-H6 from the
`docs/improvement-backlog.md` "Workspace-integrity survey (2026-07-06)" section
identified that test suites, milestone records, and policy drift exist ONLY on
this machine (unpushed / untracked), a gemini-nano gitlink has no restore URL,
and root clutter causes CRLF churn and mis-targeting.

Purpose: Make the workspace durable and truthful. Every artifact that matters
must be in git and pushed. Commit messages must be honest (this batch exists
partly because root commit b4e0868 claimed to add test suites it did not).

Output: Committed + pushed test suites (gsd-orchestrator, autogen), committed
Promptimprover tooling, gitignored gemini-nano refiner dirs, a worktrees audit
report, and a fully committed + pushed root repo with .gitattributes,
.gitmodules, Pester CI, and corrected GLOBAL_AGENTS.md.

NOTE TO ORCHESTRATOR: Tasks A-E each operate on an INDEPENDENT git repository
and have zero file overlap — fan out one executor per repo in parallel. Task F
operates on the root repo (C:\PersonalRepo) and MUST run AFTER A-E complete,
because it commits the gemini-nano gitlink pointer and references the audit
report produced by Task E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@GLOBAL_AGENTS.md
@docs/improvement-backlog.md

<facts>
Verified environment state (do not re-derive; act on these):

- Each `portfolio/*` dir is its OWN independent git repo. `portfolio/` and
  `worktrees/` are gitignored at the ROOT repo. So root commits can never
  contain sub-repo file contents — sub-repo work must be committed in the
  sub-repo itself.
- portfolio/gsd-orchestrator: branch `ci/dependabot-github-actions`.
  Untracked: `tests/`, `TestResults/`, `src/GsdOrchestrator.Tests/TestResults/`.
  Modified: `.github/workflows/ci.yml`. `.gitignore` does NOT ignore TestResults/.
- portfolio/autogen: branch `ci/dependabot-github-actions`.
  Untracked: `tests/test_autogen.py`, `.coverage`.
  Modified: `.github/workflows/ci.yml`, `autogen_dashboard/app.py`.
  `.gitignore` does NOT ignore `.coverage`.
- portfolio/Promptimprover: branch `master`.
  Untracked: `dashboard/`, `package.json`, `run-dashboard.ps1`.
- gemini-nano: branch `master`, remote
  https://github.com/Coding-Autopilot-System/gemini-nano.git.
  Untracked: `.refiner/`, `api-server/.refiner/`, `chrome-bridge/.refiner/`.
  Has a `.gitignore` already. Root tracks it as gitlink 160000
  (commit ee7b97497fb9adaf78a446502f801917c37777ff) with NO .gitmodules.
- worktrees/: contains 14 dirs that ARE registered git worktrees, but of
  DIFFERENT owning repos (gitdir links point into
  `portfolio/<repo>/.git/worktrees/...` or root `.git/worktrees/...`).
  `cas-workstation-audit` = branch audit/enterprise-hardening-20260611, clean,
  last commit 2026-06-11 (stale). `pr-*` dirs are copies of already-merged PRs.
  Auditing requires querying each worktree against its OWNING repo, not root.
- Root repo (C:\PersonalRepo): branch `master`, remote
  OgeonX-Ai/cas-workstation, 10 commits ahead of origin/master, unpushed.
  Large untracked + modified set (see Task F for the exact grouping).
- Root clutter present at C:\PersonalRepo: scripts/Cas.Workstation.psm1.bak,
  scripts/classify-engineering-task.ps1.bak, rollback_phase26.ps1, rules.json,
  test-local-llm.js, test-mcp.js, test-tool.js, "deep-research-report (4).md",
  "ChatGPT Image Jun 30, 2026, 12_32_59 PM.png", scratch/ (already exists).
- Root has tests/Loop.Pilot.Tests.ps1 and tests/Workstation.Contract.Tests.ps1
  (Pester) but NO .github/workflows/ — they never run in CI.

RULES:
- Conventional Commits. Messages MUST truthfully describe committed content.
- Do NOT create branches. Commit on each repo's CURRENT branch.
- rules.json → move to scratch/, NEVER commit (leftover API payload).
- Never commit secrets. Root .refiner/blackboard.json is already tracked +
  modified; it may be committed.
- Only prune worktrees that are fully merged AND clean. Report on the rest.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task A: gsd-orchestrator — commit + push test suite and CI</name>
  <files>portfolio/gsd-orchestrator/.gitignore, portfolio/gsd-orchestrator/tests/**, portfolio/gsd-orchestrator/.github/workflows/ci.yml</files>
  <action>
    Operate ONLY inside C:\PersonalRepo\portfolio\gsd-orchestrator (its own git
    repo, current branch ci/dependabot-github-actions — do NOT switch branches).
    Addresses W1 + H2.
    1. Append `TestResults/` to `.gitignore` (also covers
       `src/GsdOrchestrator.Tests/TestResults/` — verify the pattern matches
       nested dirs; add `**/TestResults/` if the plain pattern does not catch
       the nested path). Confirm TestResults/ is no longer listed by
       `git status --short`.
    2. Stage the untracked `tests/` directory and the modified
       `.github/workflows/ci.yml` and the `.gitignore` change. Do NOT stage
       TestResults/ dirs.
    3. Commit with a truthful message, e.g.:
       `test(gsd-orchestrator): track test suite; ignore TestResults; update ci`
       Body should state what the suite covers at a high level.
    4. Push the current branch to origin (`git push`). If push fails on auth or
       a missing upstream, set upstream (`git push -u origin HEAD`) and report
       the exact error if it still fails — do not silently swallow it.
  </action>
  <verify>
    <automated>cd portfolio/gsd-orchestrator && git ls-files tests | grep -q . && (git status --short | grep -q "TestResults" && echo FAIL || echo OK)</automated>
  </verify>
  <done>tests/ tracked in git, TestResults/ ignored (absent from status), ci.yml committed, branch pushed to origin (or exact push error reported).</done>
</task>

<task type="auto">
  <name>Task B: autogen — commit + push test suite, dashboard fix, CI</name>
  <files>portfolio/autogen/.gitignore, portfolio/autogen/tests/test_autogen.py, portfolio/autogen/autogen_dashboard/app.py, portfolio/autogen/.github/workflows/ci.yml</files>
  <action>
    Operate ONLY inside C:\PersonalRepo\portfolio\autogen (own git repo, current
    branch ci/dependabot-github-actions — do NOT switch branches).
    Addresses W1 + H2.
    1. Append `.coverage` to `.gitignore`. Confirm `.coverage` no longer appears
       in `git status --short`.
    2. Stage untracked `tests/test_autogen.py`, modified `autogen_dashboard/app.py`,
       modified `.github/workflows/ci.yml`, and the `.gitignore` change. Do NOT
       stage `.coverage`.
    3. Review the `app.py` diff briefly (`git diff autogen_dashboard/app.py`) so
       the commit message truthfully describes it. If the app.py change is
       unrelated to the test suite, use two commits: one
       `test(autogen): track test_autogen.py; ignore .coverage; update ci` and
       one `<type>(autogen): <describe app.py change>`.
    4. Push the current branch to origin. On failure, set upstream and report the
       exact error.
  </action>
  <verify>
    <automated>cd portfolio/autogen && git ls-files tests/test_autogen.py | grep -q . && (git status --short | grep -q "^?? .coverage" && echo FAIL || echo OK)</automated>
  </verify>
  <done>tests/test_autogen.py tracked, .coverage ignored, app.py + ci.yml committed with a truthful message, branch pushed (or error reported).</done>
</task>

<task type="auto">
  <name>Task C: Promptimprover — commit + push dashboard tooling</name>
  <files>portfolio/Promptimprover/dashboard/**, portfolio/Promptimprover/package.json, portfolio/Promptimprover/run-dashboard.ps1</files>
  <action>
    Operate ONLY inside C:\PersonalRepo\portfolio\Promptimprover (own git repo,
    current branch master — do NOT switch branches). Addresses H2.
    1. Review the untracked set: `dashboard/` (View-SwarmDashboard.ps1, index.html,
       server.js), `package.json`, `run-dashboard.ps1`. Confirm none contain
       secrets or local-only env values (grep for obvious token/key/password
       literals). If a file holds a secret or is clearly local-only junk, exclude
       it and note why in the SUMMARY; otherwise commit deliberately.
    2. Stage the reviewed files and commit:
       `feat(promptimprover): add swarm dashboard tooling and package manifest`
       (adjust the type/scope to match the actual content).
    3. Push master to origin. On failure, report the exact error.
  </action>
  <verify>
    <automated>cd portfolio/Promptimprover && git ls-files run-dashboard.ps1 package.json | grep -q . && echo OK || echo FAIL</automated>
  </verify>
  <done>dashboard tooling + package.json + run-dashboard.ps1 committed (minus any excluded secret/junk, noted in SUMMARY), master pushed (or error reported).</done>
</task>

<task type="auto">
  <name>Task D: gemini-nano — gitignore .refiner, clean, push</name>
  <files>gemini-nano/.gitignore</files>
  <action>
    Operate ONLY inside C:\PersonalRepo\gemini-nano (own git repo, current branch
    master, remote Coding-Autopilot-System/gemini-nano). Addresses W3 (repo side).
    1. Add ignore patterns for refiner working dirs to the existing `.gitignore`:
       a top-level `.refiner/` plus a recursive `**/.refiner/` so
       `api-server/.refiner/` and `chrome-bridge/.refiner/` are also ignored.
       Confirm all three `.refiner/` entries disappear from `git status --short`.
    2. Commit: `chore(gemini-nano): ignore .refiner working directories`.
    3. Attempt `git push`. The remote may be unreachable (private/experimental).
       If push fails on network/auth, do NOT treat it as task failure — record
       the commit SHA and the exact push error in the SUMMARY so the root Task F
       can still capture the updated gitlink pointer. If push succeeds, note the
       new pushed SHA (Task F will want the gitlink to point at a pushed commit).
  </action>
  <verify>
    <automated>cd gemini-nano && git status --short | grep -q "refiner" && echo FAIL || echo OK</automated>
  </verify>
  <done>.refiner/ dirs ignored (absent from status), ignore commit made, push attempted with result (SHA or exact error) recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task E: worktrees audit — report + safe prune only</name>
  <files>.planning/quick/260706-h8b-workspace-integrity-hardening-batch/worktrees-audit.md</files>
  <action>
    Addresses H3. This task is REPORT-FIRST. Do NOT delete anything that is not
    provably safe.
    1. Enumerate every dir under C:\PersonalRepo\worktrees\. For each, read its
       `.git` file to find the OWNING repo (gitdir path points into
       `portfolio/<repo>/.git/worktrees/...` or root `.git/worktrees/...`).
    2. For each worktree, using the OWNING repo, determine:
       - branch name (or detached HEAD)
       - dirty? (`git -C <worktree> status --porcelain` — any output = dirty)
       - last commit date
       - is the worktree's branch fully merged into the owning repo's default
         branch (main/master) AND already pushed? (check merge-base / branch
         contains, and whether the branch exists on origin as merged).
    3. Write `worktrees-audit.md` as a table: worktree | owning repo | branch |
       dirty | last commit | merged? | recommendation (PRUNE / KEEP / MANUAL).
       Classify as PRUNE only when: clean (zero uncommitted changes) AND branch
       fully merged AND pushed. Everything else = KEEP or MANUAL with the reason.
    4. Safe-prune ONLY the PRUNE-classified worktrees: for each, run
       `git -C <owning-repo> worktree remove <path>`, then
       `git -C <owning-repo> worktree prune`. Never force-remove a dirty tree.
       Log each prune action (or skip reason) in the report.
    5. Do NOT commit anything in this task; the report lives under .planning/
       (root repo) and will be committed by Task F.
  </action>
  <verify>
    <automated>test -f .planning/quick/260706-h8b-workspace-integrity-hardening-batch/worktrees-audit.md && grep -qi "recommendation" .planning/quick/260706-h8b-workspace-integrity-hardening-batch/worktrees-audit.md && echo OK || echo FAIL</automated>
  </verify>
  <done>worktrees-audit.md exists with per-worktree owning-repo/branch/dirty/merged/recommendation; only clean+merged+pushed worktrees pruned; all others reported with reasons.</done>
</task>

<task type="auto">
  <name>Task F: root repo — gitattributes, gitmodules, CI, doc drift, clutter, atomic commits, push (SEQUENTIAL, after A-E)</name>
  <files>.gitattributes, .gitmodules, .github/workflows/ci.yml, .gitignore, GLOBAL_AGENTS.md</files>
  <action>
    Operate in C:\PersonalRepo (root repo, branch master, remote
    OgeonX-Ai/cas-workstation). MUST run after Tasks A-E. Addresses W2, W3(root
    side), W5, H1, H4, H5, H6. Do the following in order, then make LOGICAL
    ATOMIC commits (grouped, not one giant commit), then push.

    STEP 1 — .gitattributes (H5): Create `.gitattributes` with `* text=auto` as
    the baseline, plus explicit rules: LF (`eol=lf`) for
    `*.json *.md *.yml *.yaml *.js *.ts`, and CRLF (`eol=crlf`) for
    `*.ps1 *.psm1`. Mark binaries (`*.png *.pdb *.dll *.exe`) as `binary`.

    STEP 2 — .gitmodules (W3): Register gemini-nano as a submodule so fresh
    clones can restore it. Create `.gitmodules` with a `[submodule "gemini-nano"]`
    entry: `path = gemini-nano`,
    `url = https://github.com/Coding-Autopilot-System/gemini-nano.git`. Ensure
    the gitlink pointer (160000) is staged so the recorded commit matches the
    latest PUSHED gemini-nano commit (use the SHA Task D reported; if Task D could
    not push, note in SUMMARY that the pointer references an unpushed commit and
    flag for follow-up).

    STEP 3 — Root Pester CI (H1): Create `.github/workflows/ci.yml` — a minimal
    workflow: `on: [push, pull_request]`, single job on `windows-latest`, least-
    privilege `permissions: contents: read`, `timeout-minutes` set, that installs
    Pester and runs `Invoke-Pester` against `tests/*.Tests.ps1`
    (Loop.Pilot.Tests.ps1, Workstation.Contract.Tests.ps1). Keep it minimal but
    valid.

    STEP 4 — Clutter relocation (H4): Move transient artifacts into `scratch/`
    (already exists): `scripts/Cas.Workstation.psm1.bak`,
    `scripts/classify-engineering-task.ps1.bak`, `rollback_phase26.ps1`,
    `test-local-llm.js`, `test-mcp.js`, `test-tool.js`,
    `"deep-research-report (4).md"`, `"ChatGPT Image Jun 30, 2026, 12_32_59 PM.png"`.
    Move `rules.json` into scratch/ as well but it must NEVER be committed.
    Add `scratch/` to root `.gitignore` (so nothing under scratch/ is tracked).

    STEP 5 — GLOBAL_AGENTS.md drift (H6): Update the "Workspace Layout" section
    so its portfolio listing matches CLAUDE.md's 14-entry context chain (currently
    it lists only 5). Add the missing repos: cas-evals, cas-contracts, cas-platform,
    autopilot-core, autopilot-demo, ci-autopilot, cas-workstation, org-dotgithub.
    Add a note documenting that the ROOT repo pushes to
    `OgeonX-Ai/cas-workstation` while `portfolio/cas-workstation` is
    `Coding-Autopilot-System/cas-workstation` (same name, two orgs — avoid
    mis-targeting).

    STEP 6 — ATOMIC COMMITS of the full untracked/modified root set. Group into
    truthful conventional commits (do NOT lump everything into one). Suggested
    grouping (adjust to reality):
      - `chore(git): add .gitattributes and .gitmodules; ignore scratch/`
        (.gitattributes, .gitmodules + gemini-nano gitlink, .gitignore scratch/)
      - `ci(workstation): add Pester CI workflow` (.github/workflows/ci.yml)
      - `docs(agents): correct GLOBAL_AGENTS workspace layout and repo targets`
        (GLOBAL_AGENTS.md)
      - `chore(engineering-os): commit policy drift` (W5 — OPERATING-CONTRACT.md,
        router/ollama-policy.json, router/ollama-benchmark.json,
        compatibility/tool-matrix.json, models/codex.json, engineering-os/personas/,
        engineering-os/scripts/, scripts/classify-engineering-task.ps1)
      - `docs(planning): commit v1.3 milestone records` (.planning/26-CONTEXT.md,
        REQUIREMENTS.md, milestones/v1.3-*, phases/22-25, traces.jsonl, and the
        Task E worktrees-audit.md under this quick dir)
      - `docs: add improvement-backlog, azure-rollout-plan, requirements.txt`
      - `feat(scripts): add pilot + gsd-progress + ci runner scripts`
        (scripts/gsd-progress.ps1, pilot-setup.ps1, run-v1.3-pilot.ps1,
        run_gsd_orchestrator_ci.ps1)
      - `chore(refiner): update blackboard state` (.refiner/blackboard.json)
      Before EACH commit: verify `rules.json` is NOT staged, and scan the staged
      diff for token/key/password literals; abort that commit if a secret shows.

    STEP 7 — PUSH: `git push origin master`. Confirm
    `git rev-list --count origin/master..HEAD` is 0 afterward. If push fails on
    auth, report the exact error and STOP (do not force).
  </action>
  <verify>
    <automated>cd "C:/PersonalRepo" && test -f .gitattributes && test -f .gitmodules && grep -q "path = gemini-nano" .gitmodules && test -f .github/workflows/ci.yml && grep -q "scratch/" .gitignore && (git ls-files --error-unmatch rules.json 2>/dev/null && echo FAIL || echo OK)</automated>
  </verify>
  <done>.gitattributes + .gitmodules (gemini-nano) + root Pester ci.yml exist; scratch/ ignored; rules.json untracked; GLOBAL_AGENTS.md lists all portfolio repos + org note; all root untracked/modified content committed in grouped truthful commits; master pushed (origin/master..HEAD == 0) or exact push error reported.</done>
</task>

</tasks>

<verification>
Batch-level checks after all tasks:
- Each sub-repo (gsd-orchestrator, autogen, Promptimprover, gemini-nano) has a
  clean-ish `git status` for the targeted files and its intended commit(s)
  pushed (or a recorded push error for gemini-nano if remote unreachable).
- `worktrees-audit.md` exists and every worktree has a recommendation; any
  pruned worktree was clean + merged + pushed.
- Root repo: `.gitattributes`, `.gitmodules` (gemini-nano), `.github/workflows/ci.yml`
  present; `scratch/` in `.gitignore`; `rules.json` untracked; no clutter files
  left at root (moved to scratch/).
- `git -C C:/PersonalRepo rev-list --count origin/master..HEAD` == 0 (pushed),
  or an explicit auth error is reported.
- No commit message overstates its content (truthfulness gate — this batch
  exists because a prior commit lied).
</verification>

<success_criteria>
- W1 closed: gsd-orchestrator + autogen test suites tracked in git and pushed.
- W2 closed: root untracked/modified set committed in atomic truthful commits
  and master pushed to OgeonX-Ai/cas-workstation.
- W3 closed: `.gitmodules` registers gemini-nano with its restore URL; repo
  `.refiner/` dirs ignored.
- W5 closed: engineering-os policy drift committed.
- H1 closed: root Pester CI workflow added.
- H2 closed: TestResults/ + .coverage ignored; Promptimprover tooling committed.
- H3 closed: worktrees audit report produced; only safe worktrees pruned.
- H4 closed: clutter moved to gitignored scratch/; rules.json moved (not committed).
- H5 closed: `.gitattributes` normalizes line endings.
- H6 closed: GLOBAL_AGENTS.md workspace layout matches reality + org note added.
</success_criteria>

<output>
Create `.planning/quick/260706-h8b-workspace-integrity-hardening-batch/260706-h8b-SUMMARY.md` when done.
Per-repo executors should each record: files committed, commit SHAs, push
result (success or exact error). Task F should record the final
`origin/master..HEAD` count and the gemini-nano gitlink SHA it captured.
</output>
