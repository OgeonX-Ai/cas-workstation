# vNEXT-SEEDS.md — Live Fact-Check Round 1

**Method:** every checkable claim in `vNEXT-SEEDS.md` (v1.6 phases 43-47, v1.7 48-51), `37-CONTEXT.md`, and `docs/improvement-backlog.md` verified against live git/gh state on 2026-07-12. Training-knowledge claims are marked `[ASSUMED]`; everything else was run.

## HEADLINE FINDING (changes the plan more than any single row below)

**v1.5 (phases 38-42) is not a future milestone — it is already planned, executed, and audited in git.** `.planning/phases/38-merge-flow-and-backfill/` through `42-v1.5-audit/` exist, are committed (`bcde4b2`, `2e4b3ee`, `3ddcc2b`, etc.), and `42-VERIFICATION.md` contains a dated (2026-07-11T14:00Z) audit with an explicit operator checklist. Nearly every item on that checklist has since been merged (root PRs #14-#24 all MERGED or superseded-CLOSED as of this session). **The only unmerged blocker across the entire v1.5 milestone is org PR `Coding-Autopilot-System/.github#17`** ("review-bot auto-merge mechanism") — still OPEN, gated on a manual operator step (create the `cas-review-bot` GitHub App, install it, store `REVIEW_BOT_APP_ID`/`REVIEW_BOT_PRIVATE_KEY` as org secrets). STATE.md's "Operator Next Steps" still says `/gsd:new-milestone — v1.5...` as if v1.5 hasn't started, which is now stale/misleading given the audit already ran.

**Plan impact:** `/gsd:new-milestone` for v1.5 is largely moot — v1.5 needs one operator action (App creation) + `/gsd:complete-milestone v1.5`, not fresh planning. v1.6 seed research should be re-scoped as "what's left after v1.5 closes," and the vNEXT-SEEDS.md document itself needs a status refresh before it's used as planning input again.

---

## Claim-by-claim fact-check

| # | Claim (source) | Live finding | Verdict | Plan-impact |
|---|---|---|---|---|
| 1 | v1.5 phases 38-42 are unplanned future work (vNEXT-SEEDS.md framing) | `.planning/phases/38..42` fully populated with PLAN/SUMMARY/LEARNINGS/VERIFICATION, all committed; audit ran 2026-07-11 | **WRONG** | Skip re-planning; close out via operator action + `/gsd:complete-milestone` |
| 2 | 43 Provenance: `git verify-commit HEAD` should pass on new default-branch commits | Local repo has **no** `user.signingkey`/`commit.gpgsign`/`gpg.format` set (global or local — both exit 1, empty). The one "verified" commit checked via `gh api .../commits/main` is a **GitHub-web-UI-generated merge commit** signed by GitHub's own key (`committer GitHub <noreply@github.com>`), not an agent/operator identity. No gitsign or SSH-signing configured anywhere. | **STALE/not started** | Phase 43 requirement is real and un-met — needs its own setup task, not assumed-done |
| 3 | 43: gitsign keyless signing is Windows-viable `[ASSUMED→now CITED]` | WebSearch: gitsign v0.16.0 (May 2026) ships Windows `.exe`/`gitsign-credential-cache.exe` binaries; Chocolatey package exists. **[CITED: github.com/sigstore/gitsign, community.chocolatey.org/packages/gitsign]** | HOLDS | Gitsign is a viable option for 43; SSH-signing (`gpg.format ssh`, `ssh-keygen`, `allowed_signers`) is also viable and simpler on Windows (`ssh-keygen` already present on this box) — recommend SSH-signing over gitsign for lower setup friction |
| 4 | 44 Secrets & Identity: secret-scanning gates "unverified" (backlog E6) | `gh api repos/.../security_and_analysis` on all 12 org repos: **`secret_scanning: enabled`, `secret_scanning_push_protection: enabled`** org-wide already, via GitHub-native scanning. No `gitleaks` CI workflow found in any repo (only `release-please.yml` present). No PAT rotation runbook or token-inventory doc found in the repo. | **STALE** (more done than backlog implies for scanning; less done for token inventory) | Phase 44 scope should shrink to: PAT inventory + rotation runbook + (optional) gitleaks-in-CI as defense-in-depth. A "seeded canary secret is blocked" falsifier test should still be run once to confirm push-protection actually blocks (never empirically triggered) |
| 5 | 45 CAS Metrics: DORA dashboard from `traces.jsonl` | `traces.jsonl` = **18 lines total**, spanning 2026-07-05 to 2026-07-12. Fields present: `timestamp, trace_id, span_id, action_type, persona, metrics{tokens_prompt,tokens_completion,total_tokens}, reasoning_path`, optional `tool_call`. **Missing:** repo, PR number, commit SHA, phase/task ID, merge outcome, duration/lead-time, pass/fail/revert flag — none of the 4 DORA metrics (lead time, deploy frequency, change-failure rate, MTTR) are derivable from this file alone. 14 of 18 entries have `tokens_prompt/completion = 0` (real work sessions aren't token-instrumented; only 5 early synthetic/demo entries from 2026-07-05 have non-zero tokens). | **WRONG as stated** | Phase 45 cannot build a DORA dashboard from `traces.jsonl` as-is — needs a schema extension (repo/PR/outcome/duration fields) or a pivot to `gh api` as primary source with `traces.jsonl` for token-spend only, and even token-spend is mostly zero-valued today |
| 6 | 46 Test Depth: Stryker.NET / mutmut version compatibility | **Stryker.NET** (`dotnet-stryker` 4.16.0, released 2026-07-03) explicitly supports .NET 8.0+ with computed compatibility for .NET 9.0/10.0 **[CITED: nuget.org/packages/dotnet-stryker]**. Local SDK is `10.0.203` — compatible. **mutmut** PyPI listing supports Python 3.10-3.14 explicitly, latest 3.6.0 (2026-06-06) **[CITED: pypi.org/project/mutmut]**. Local `autogen` venv uses Python 3.12.10 — compatible. | HOLDS (compat) but see #7 | Tooling choice is sound; feasibility is blocked by suite health, not tool compat |
| 7 | 46: current suite runtimes as mutation feasibility signal | `gsd-orchestrator`: `dotnet test --no-restore` fails immediately — **`NETSDK1064: Package Microsoft.Extensions.Configuration.Binder, version 10.0.9 was not found`** — restore is currently broken, so no runtime could be measured. `autogen`: `pytest --collect-only` on the full suite produced **25 collection errors** across 13 files (157 of ~182 tests collected); the same file collected cleanly (10/10) when run in isolation, suggesting cross-file collection-state pollution rather than per-file breakage — not fully root-caused this session. | **NEW FINDING (not in original claim set)** | Phase 46 (mutation testing) has a hard precondition unmet: fix the `.NET` restore and diagnose the `pytest` collection-error class first, or mutation runs will fail/mis-scope from a broken baseline |
| 8 | Releases NOW — did release-please fire after the pin fixes? Any v-tags? | `gsd-orchestrator`: tags v1.0.0/v3.0.0/v4.0.0 (latest 2026-06-15) — **pre-date** release-please wiring; repin PR **#25 still OPEN**. `autogen`: repin PR **#24 MERGED** (2026-07-11T15:31) but **zero tags/releases exist** — no release-please "chore(main): release" PR has fired yet even post-merge. `Promptimprover`: repin PR **#32 still OPEN**, zero tags. `cas-contracts`: repin PR **#23 still OPEN**; tags v0.1.0/v1.1.0/v1.1.1 exist but **only v0.1.0 has a GitHub Release object** (v1.1.0/v1.1.1 are bare tags, not release-please output — likely predate the wiring). | **WRONG** ("after the pin fixes" — only 1 of 12 cross-repo pin-fix PRs is merged; none has produced a release-please release PR yet) | Phase 39 is NOT release-ready; 11 more pin-fix PRs need merging before release-please can fire its first automated release anywhere. This matches 42-VERIFICATION.md's own checklist item 2 ("merge the 12 remaining pin-fix PRs") — audit already knew this |
| 9 | Backlog W1-W5 workspace-integrity items | STATE.md shows a completed quick-task `260706-h8b Workspace-integrity hardening batch (W1-W5, H1-H6)` dated 2026-07-06, commit `784eb2f` | **DONE** (batched, not itemized) | No further action; re-survey only if re-opening workspace-integrity concerns |
| 10 | Scheduled tasks CAS-WorkspaceHealth + CAS-PilotCadence still Ready | `Get-ScheduledTask` confirms both **State: Ready** | **HOLDS** | No action |
| 11 | 37-CONTEXT.md: Phase 37 "planned for after Phase 35 audit" | Root repo now on phase 38-42 (v1.5), well past 35; marketing site not yet built | **STALE (context, not wrong)** — precondition long since satisfied | Phase 37 is unblocked and overdue relative to its own trigger condition; sequencing note should be dropped |
| 12 | M1 showcase site (mkdocs-material on GH Pages) | Only GH Pages instance found is `Coding-Autopilot-System/.github` → `coding-autopilot-system.github.io/.github/` (org profile, `build_type: workflow`) — **not** a dedicated mkdocs-material showcase site; no `mkdocs.yml` or Feature Card/Story Page generator found in root repo | **OPEN** (not started) | Phase 37 scope as designed still needed in full |
| 13 | M3 "in progress — started 2026-07-08" (image placeholders) | `docs/VISION.md`: 1 remaining `codex:generate-image` placeholder, 4 embedded images already present. `docs/wiki/Agent-Hierarchy.md`: 1 remaining placeholder. Root PR #24 "fix(docs): commit VISION.md's missing generated visuals" MERGED 2026-07-11 — matches backlog's "in progress" framing, and 42-VERIFICATION.md notes a Codex session "actively iterating on further visuals (architecture-diagram, cas-workstation-hub, incident-lifecycle)" | **HOLDS** | No re-plan needed; M3 genuinely mid-flight as stated |
| 14 | E11: REQUIREMENTS.md format blocks `requirements.mark-complete` | `.planning/REQUIREMENTS.md` root file is still markdown-bullet format (`### REQ-STAB-01` + bullet list), **zero** pipe-table rows (`grep -c "^|"` = 0); no `traceability` reference found in phase 38 SUMMARY/PLAN files | **OPEN, confirmed** | Still a valid quick-fix candidate; not resolved by the 38-42 work despite E11 being tagged "v1.5/38 (quick fix eligible)" |
| 15 | E1 merge flow: 40-PR human bottleneck / needs merge queue + auto-merge + real second reviewer | Mechanism (`feat(38-01): review-bot auto-merge mechanism`) is built and open as org PR #17, **not yet merged**; blocked on manual GitHub App creation + 2 org secrets (operator-only action, cannot be automated by an agent with current token scope: org-secrets API returned 403 needing `admin:org`) | **OPEN — one manual step from DONE** | Not a planning gap, an execution/operator gap; surface explicitly as a checkpoint:human-verify blocker, not new design work |
| 16 | E2 releases: no per-repo SemVer/changelog discipline | `release-please.yml` present on **all 12 org repos**; wiring PRs (`ci(release): wire release-please...`) merged everywhere; repin PRs mostly still open (see #8) | **STALE/PARTIALLY DONE** | Mechanism built; execution blocked on merging remaining repin PRs, not on new design |
| 17 | E3 pilots/fault-injections not scheduled | `worktrees/phase-40-pilot-cadence`, `worktrees/cas-workstation-pilot-cadence` (evidence branch `evidence/pilot-cadence-2026-07-10`) exist; `.planning/phases/40-pilot-cadence/40-LEARNINGS.md` committed; Task Scheduler `CAS-PilotCadence` is Ready | **DONE** (built + at least one evidence run captured 2026-07-10) | Verify a second consecutive week's run exists before closing REQ-1.5.4 per 42-VERIFICATION checklist item 5 (next fire due 2026-07-17) |
| 18 | E4 learnings extraction not institutionalized | `38-LEARNINGS.md`, `39-LEARNINGS.md`, `40-LEARNINGS.md`, `41-LEARNINGS.md` all exist and are committed; phase 41 (`41-02: repeatable backlog-survey script + baseline evidence`) merged | **DONE** | No action |
| 19 | E5 no commit/tag signing, provenance, SBOMs | Confirmed — see row 2; no SBOM tooling (CycloneDX) found in any repo's CI | **HOLDS (still open)** | Phase 43 fully unstarted, as originally scoped |
| 20 | E6 secret-scanning gates unverified | See row 4 — GH-native scanning + push protection already ON org-wide; never empirically triggered with a canary | **PARTIALLY STALE** | Shrink phase 44 scope; add one falsifier test |
| 21 | E7 no self-measurement (DORA/token/health trends) | See row 5 | **HOLDS (still open)**, but the specific mechanism (traces.jsonl) needs a schema fix first | Phase 45 needs a data-model task before a dashboard task |
| 22 | E8 test quality unmeasured (mutation, property-based) | See rows 6-7 — tooling fine, suite health is the actual blocker | **HOLDS (still open)**, new precondition discovered | Add "fix restore/collection health" as Wave 0 of phase 46 |
| 23 | E9 clean-machine bootstrap unproven | Not tested this round (out of time-box; no clean VM available to this session) | **NOT EVALUATED** | Re-verify in a future research pass before phase 48 planning |
| 24 | E10 no disaster-restore drill | Not tested this round | **NOT EVALUATED** | Re-verify before phase 50 planning |

---

## Open-threads inventory

**Org-wide open PR count:** 32 (via `gh search prs --owner Coding-Autopilot-System --state open`, deduplicated JSON count). Dominated by two batch classes fired 2026-07-11: `chore: ecosystem-wide sync of uncommitted changes` (Codex ecosystem-sync bot, ~1 per repo) and `docs: add AI generated conceptual visualization to README` (image-generation bot, ~1 per repo), plus the 11 still-open `fix(ci): repin release-please...` PRs and org `.github#17` (review-bot).

**Root repo (`OgeonX-Ai/cas-workstation`) open PRs:** **0** — everything through #28 is MERGED or superseded-CLOSED as of this session. Default branch is `master` (not `main`; explains an earlier 404 probing `branches/main/protection`).

**The 4 audit-rejected root PRs (#12/#14/#19/#25):** all **CLOSED, not merged** (`mergedAt: null` for all four), closed 2026-07-11 15:32-15:36Z, within ~5 minutes of each other — a batch-close, not four independent rejections:
- **#12** `docs(41-01): institutionalize phase-close learning extraction` — superseded by the actual 41-01/41-02 work that later landed and merged under different PR numbers (LEARNINGS.md files are present and committed per row 18).
- **#14** `feat(40-01): pilot-cadence runner + regression issue filer` — superseded by phase 40 work that landed via the `feat/phase-40-pilot-cadence` worktree/evidence branch.
- **#19** `docs(39-03): release wiring summary + state bookkeeping` — release-please wiring merged via different PRs (`ci(release): wire release-please...` #17/#22-series per repo); this doc/bookkeeping PR became redundant.
- **#25** `docs: add AI generated conceptual visualization to README` — superseded by #24 (`fix(docs): commit VISION.md's missing generated visuals`), which fixed a stranded-asset bug the #25 content didn't address.

**Artifacts needing honest re-landing:** none of the four closed PRs' *content* appears lost — each has a superseding merged PR or an equivalent LEARNINGS/SUMMARY file already in git. No re-landing work identified this round; flag for a second pass only if a future audit finds a specific REQ un-satisfied by the superseding PRs (not confirmed either way in this session — content diff of superseding PRs vs the closed four was not exhaustively compared).

**Codex ecosystem-sync PR pattern (read `gsd-orchestrator` #27):** 10 files changed — `.gitignore` (+6), `.planning/REQUIREMENTS.md` (+13/-13), `.planning/ROADMAP.md` (+7/-7), `Dockerfile` (+15, new), `README.md` (+23/-226, large deletion), `WIKI/Architecture.md` (+19, new), `WIKI/Home.md` (+6, new), `docs/assets/concept.png` (binary), `src/GsdOrchestrator/Checkpointing/FileCheckpointStore.cs` (+5/-1), `src/GsdOrchestrator/Program.cs` (+4). **Scope/risk:** mixes low-risk doc/wiki sync with a real source-code change to a checkpointing store and `Program.cs` inside a PR titled generically "ecosystem-wide sync of uncommitted changes" — the title does not disclose that source code is touched, which is a review-hygiene risk (a reviewer skimming the title could rubber-stamp a code change as a docs sync). Recommend: this bot's PR title/description should split code changes into their own commit message or PR when it happens again.

---

## FACTCHECK R1 COMPLETE

**Counts:** 10 HOLDS · 9 STALE · 3 WRONG · 2 NOT EVALUATED (out of 24 rows checked) — plus 1 new finding outside the original claim set (dotnet restore + pytest collection-error blockers for phase 46).

**Top 3 plan-impacts:**
1. **v1.5 (phases 38-42) is already planned and audited, not a future milestone** — `/gsd:new-milestone` for v1.5 would duplicate committed work; the real next step is one operator action (create `cas-review-bot` GitHub App + 2 org secrets to merge org `.github#17`) followed by `/gsd:complete-milestone v1.5`. Re-scope v1.6 seed planning around "what's left after v1.5 closes," not the current vNEXT-SEEDS.md text.
2. **Phase 45 (CAS Metrics) cannot build the promised DORA dashboard from `traces.jsonl` as-is** — the file has 18 entries, no repo/PR/outcome/duration fields, and 14/18 entries carry zero token counts. This needs a schema-extension task before any dashboard-rendering task, or a pivot to `gh api` as the primary DORA data source.
3. **Phase 39 (releases) and Phase 44 (secrets) are both further along than the backlog implies, in opposite directions** — release-please is wired everywhere but blocked on 11 more repin-PR merges before it fires once; secret-scanning + push protection are already ON org-wide (no gitleaks needed as a first cut), so phase 44's real remaining scope is PAT inventory/rotation, not scanning setup. Both phase scopes should shrink from what vNEXT-SEEDS.md currently describes.

**File:** `C:\PersonalRepo\.planning\research\SEEDS-FACTCHECK-R1.md` (not committed, per instruction).
