# Architecture Memory

- CAS is a Windows-first workstation bundle with multiple repos under `portfolio/`.
- `gsd-orchestrator` is the local control-plane engine for autonomous issue-to-PR workflows.
- `Promptimprover` is the prompt-governance and memory layer.
- `autogen` is the MAF runtime and operator dashboard layer.
- SDLC guidance should remain versioned, explicit, and verifier-led.

